---
phase: 01-auth-and-navigation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-03
files_modified: []
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - NAV-05
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "User can tap PIN on touchscreen numpad to log in"
    - "App pre-selects last logged-in user"
    - "Operator sees Run tab only; Setup sees Run+Axes+Params; Admin sees all 4"
    - "Setup unlock via PIN overlay on restricted tab"
    - "E-STOP, connection status, user/role visible on every screen"
    - "Auto-lock after 30 min returns to Operator view"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the complete auth and navigation system. Human confirms all flows work correctly on screen.

Purpose: Gate before moving to Phase 2. Catch visual/layout issues that automated tests cannot detect.
Output: Approval to proceed or list of issues to fix.
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-auth-and-navigation/01-CONTEXT.md
@.planning/phases/01-auth-and-navigation/01-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete auth and navigation system</name>
  <files></files>
  <action>
    Human verifies the complete PIN authentication system, role-aware tab navigation, persistent status bar with E-STOP, connection status, and user info by launching the app and testing all flows described below.
  </action>
  <verify>
    Launch the app:
    ```
    cd "C:/Users/danii/OneDrive/Desktop/PythonDMCGUI" && python -m dmccodegui
    ```

    **1. Login flow:**
    - App should show fullscreen dark overlay with PIN numpad
    - "Operator" should be pre-selected as username
    - Enter wrong PIN (e.g., "0000") -> numpad card should shake, dots clear, "Invalid PIN" appears briefly
    - Enter correct PIN "1234" -> overlay dismisses

    **2. Operator view:**
    - Only "Run" tab visible in tab bar
    - StatusBar shows: connection status (left), "Operator" + role badge (left-center), banner ticker (center), red E-STOP (right)
    - E-STOP button is red, clearly visible, top-right area
    - All text readable against dark background

    **3. User switching:**
    - Tap user name/role area in status bar -> PIN overlay opens
    - Select "Setup" from user list, enter "5678" -> overlay dismisses
    - Now Run, Axes Setup, and Parameters tabs are visible
    - Tap each tab -> placeholder screen appears instantly (no slide animation)

    **4. Admin access:**
    - Switch to "Admin" user (PIN: 0000)
    - All 4 tabs visible: Run, Axes Setup, Parameters, Diagnostics

    **5. Visual quality:**
    - Dark theme consistent across all screens
    - All buttons look tappable and are at least 44dp tall
    - Tab bar icons and labels visible
    - No visual glitches, overlapping text, or clipped elements
    - Layout looks reasonable at 1920x1080 window size

    **6. E-STOP test:**
    - Tap E-STOP button -> should disconnect (returns to connection/setup screen)
  </verify>
  <done>Human types "approved" confirming all 6 areas pass, or describes issues for fixing.</done>
</task>

</tasks>

<verification>
Human visual and functional verification. No automated verification for this plan.
</verification>

<success_criteria>
- Human confirms all 6 verification areas pass
- No blocking visual or functional issues
- Ready to proceed to Phase 2 (RUN page)
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-and-navigation/01-04-SUMMARY.md`
</output>
