---
phase: 05-csv-profile-system
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: []
autonomous: false
requirements:
  - CSV-01
  - CSV-02
  - CSV-03
  - CSV-04
  - CSV-05

must_haves:
  truths:
    - "Human has verified the Profiles tab appears for Setup role and is hidden for Operator"
    - "Human has verified Export writes a readable CSV to profiles/ directory"
    - "Human has verified Import shows FileChooser, diff dialog, and applies values"
    - "Human has verified Import button is greyed out during active cycle"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the complete CSV Profile System.

Purpose: Human confirms all import/export flows work correctly on the real app with real UI interactions.
Output: Verification pass/fail for all CSV requirements.
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-csv-profile-system/05-CONTEXT.md
@.planning/phases/05-csv-profile-system/05-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual and functional verification of CSV Profile System</name>
  <files></files>
  <action>
    Present the complete CSV Profile System to the user for manual verification. The system was built across Plans 01 and 02: pure CSV engine (export, parse, diff, validate) plus ProfilesScreen UI with FileChooser, diff dialog, export popup, tab bar integration, and safety interlocks.

    What was built:
    - Profiles tab with Import/Export for Setup/Admin roles
    - FileChooser modal for file selection
    - Diff confirmation dialog showing only changed values
    - Machine-type validation blocking mismatched imports
    - Cycle-running safety interlock disabling Import button

    How to verify — launch the app: `python src/dmccodegui/main.py`

    1. Role gating (CSV-05):
       - Log in as Operator — verify Profiles tab is NOT visible in tab bar
       - Log in as Setup or Admin — verify Profiles tab IS visible
       - Navigate to Profiles tab

    2. Export flow (CSV-01):
       - Press Export button — verify a popup asks for a profile name
       - Enter a name (e.g., "TestKnife") and press Save
       - Verify a file profiles/TestKnife.csv was created
       - Open the CSV in a text editor or Excel — verify it has metadata rows (_machine_type, _export_date, _profile_name), scalar parameter rows, and array rows
       - Press Export again with the same name — verify an overwrite confirmation appears

    3. Import flow (CSV-02, CSV-03):
       - Press Import button — verify FileChooser modal opens showing profiles/ directory
       - Verify only .csv files are visible in the file list
       - Select the CSV you just exported and press Open
       - Verify a diff dialog appears showing changed values (or "no changes" if values match)
       - Press Cancel to dismiss without applying
       - Edit the CSV to change a scalar value (e.g., change knfThk to 99.0)
       - Import again — verify the diff shows the changed value
       - Press Apply — verify values are sent to controller

    4. Machine-type validation (CSV-03):
       - Edit the CSV _machine_type row to a different value (e.g., "3-Axes Serration Grind")
       - Try to import — verify an error dialog blocks the import before diff is shown

    5. Cycle-running interlock (CSV-04):
       - If possible, set cycle_running=True (start a cycle or mock it)
       - Navigate to Profiles tab — verify Import button is greyed out / disabled
       - Verify Export button remains enabled
  </action>
  <verify>User confirms all 5 verification steps pass</verify>
  <done>All CSV requirements (CSV-01 through CSV-05) verified by human interaction with the running application</done>
</task>

</tasks>

<verification>
All 5 verification steps pass with human confirmation.
</verification>

<success_criteria>
- CSV-01: Export creates human-readable, Excel-compatible CSV
- CSV-02: Import parses CSV, shows diff, applies on confirmation
- CSV-03: Machine-type mismatch blocks import with clear error
- CSV-04: Import disabled during active cycle
- CSV-05: Profiles tab visible only for Setup/Admin
</success_criteria>

<output>
After completion, create `.planning/phases/05-csv-profile-system/05-03-SUMMARY.md`
</output>
