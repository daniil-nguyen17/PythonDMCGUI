---
phase: 01-auth-and-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dmccodegui/auth/__init__.py
  - src/dmccodegui/auth/auth_manager.py
  - src/dmccodegui/app_state.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_auth_manager.py
  - tests/test_app_state.py
  - pyproject.toml
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - AUTH-06

must_haves:
  truths:
    - "AuthManager loads users.json or creates defaults (Admin/0000, Operator/1234, Setup/5678)"
    - "AuthManager.validate_pin returns role string on correct PIN, None on wrong PIN"
    - "AuthManager persists last_user to users.json on successful login"
    - "MachineState.set_auth() sets current_user, current_role, setup_unlocked and notifies listeners"
    - "MachineState.lock_setup() clears setup_unlocked and notifies listeners"
  artifacts:
    - path: "src/dmccodegui/auth/__init__.py"
      provides: "Package init with AuthManager export"
    - path: "src/dmccodegui/auth/auth_manager.py"
      provides: "AuthManager class: load, save, validate_pin, user_names, last_user"
      min_lines: 40
    - path: "src/dmccodegui/app_state.py"
      provides: "MachineState with current_user, current_role, setup_unlocked, set_auth(), lock_setup()"
    - path: "tests/test_auth_manager.py"
      provides: "Unit tests for AuthManager"
      min_lines: 30
    - path: "tests/test_app_state.py"
      provides: "Unit tests for MachineState auth fields"
      min_lines: 20
  key_links:
    - from: "src/dmccodegui/auth/auth_manager.py"
      to: "users.json"
      via: "json.load/json.dump"
      pattern: "json\\.(load|dump)"
    - from: "src/dmccodegui/app_state.py"
      to: "listeners"
      via: "notify() after auth state change"
      pattern: "self\\.notify\\(\\)"
---

<objective>
Create the AuthManager class and extend MachineState with auth fields. This is the pure Python foundation that all auth and navigation UI depends on. No Kivy code in this plan.

Purpose: Establish the data layer for PIN validation, user storage, role tracking, and setup lock state before any UI is built.
Output: auth_manager.py, extended app_state.py, and passing unit tests.
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-and-navigation/01-CONTEXT.md
@.planning/phases/01-auth-and-navigation/01-RESEARCH.md

@src/dmccodegui/app_state.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AuthManager class with tests</name>
  <files>src/dmccodegui/auth/__init__.py, src/dmccodegui/auth/auth_manager.py, tests/__init__.py, tests/conftest.py, tests/test_auth_manager.py, pyproject.toml</files>
  <behavior>
    - test_default_users_created: When users.json does not exist, AuthManager creates it with Admin(0000), Operator(1234), Setup(5678)
    - test_validate_pin_correct: validate_pin("Admin", "0000") returns "admin"
    - test_validate_pin_wrong: validate_pin("Admin", "9999") returns None
    - test_validate_pin_unknown_user: validate_pin("Nobody", "0000") returns None
    - test_last_user_default: On first boot, last_user is "Operator"
    - test_last_user_persistence: After successful validate_pin("Admin", "0000"), last_user is "Admin" and persists across reload
    - test_user_names: user_names returns list of all user names
    - test_existing_file_loads: If users.json already exists with custom data, AuthManager loads it without overwriting
  </behavior>
  <action>
    1. Create `src/dmccodegui/auth/__init__.py` with `from .auth_manager import AuthManager` export.
    2. Create `src/dmccodegui/auth/auth_manager.py` following the exact pattern from 01-RESEARCH.md Pattern 1:
       - `DEFAULT_USERS` dict with Admin/0000/admin, Operator/1234/operator, Setup/5678/setup
       - `__init__(self, users_path: str)` — loads or creates users.json
       - `_load()` — reads JSON or creates defaults with last_user="Operator"
       - `_save()` — writes JSON with indent=2
       - `user_names` property — list of user name strings
       - `last_user` property — string
       - `validate_pin(username, pin)` — returns role string or None; updates last_user on success
       - `get_role(username)` — returns role string or None
    3. Create `tests/__init__.py` (empty).
    4. Create `tests/conftest.py` with a `tmp_users_path` fixture using `tmp_path / "users.json"`.
    5. Create `tests/test_auth_manager.py` with all tests from behavior block above.
    6. Add `[tool.pytest.ini_options]` to pyproject.toml with `testpaths = ["tests"]` if not already present.

    Use `os.path.dirname(os.path.abspath(__file__))` pattern for default path resolution, matching the existing codebase pattern from main.py.
  </action>
  <verify>
    <automated>cd "C:/Users/danii/OneDrive/Desktop/PythonDMCGUI" && python -m pytest tests/test_auth_manager.py -x -v</automated>
  </verify>
  <done>All 8+ AuthManager tests pass. users.json is created with correct defaults. PIN validation works. last_user persists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MachineState auth extension with tests</name>
  <files>src/dmccodegui/app_state.py, tests/test_app_state.py</files>
  <behavior>
    - test_set_auth: set_auth("Admin", "admin") sets current_user="Admin", current_role="admin", setup_unlocked=True, and triggers notify
    - test_set_auth_operator: set_auth("Op", "operator") sets setup_unlocked=False
    - test_set_auth_setup: set_auth("Setup", "setup") sets setup_unlocked=True
    - test_lock_setup: After set_auth("Setup", "setup"), lock_setup() sets setup_unlocked=False and triggers notify
    - test_lock_setup_operator_noop: lock_setup() on operator role still sets setup_unlocked=False (no crash)
    - test_initial_auth_fields: Fresh MachineState has current_user="", current_role="", setup_unlocked=False
  </behavior>
  <action>
    1. Read current `app_state.py` (already loaded in context).
    2. Add three new fields to the `MachineState` dataclass:
       - `current_user: str = ""`
       - `current_role: str = ""`  (values: "operator" | "setup" | "admin" | "")
       - `setup_unlocked: bool = False`
    3. Add `set_auth(self, user: str, role: str) -> None` method:
       - Sets current_user, current_role
       - Sets setup_unlocked = (role in ("setup", "admin"))
       - Calls self.notify()
    4. Add `lock_setup(self) -> None` method:
       - Sets setup_unlocked = False
       - Calls self.notify()
    5. Create `tests/test_app_state.py` with all tests from behavior block.
       - Tests should verify notify() is called by subscribing a mock listener.

    IMPORTANT: Do not break existing MachineState functionality. Preserve all existing fields and methods exactly.
  </action>
  <verify>
    <automated>cd "C:/Users/danii/OneDrive/Desktop/PythonDMCGUI" && python -m pytest tests/test_app_state.py -x -v</automated>
  </verify>
  <done>MachineState has auth fields. set_auth and lock_setup work correctly. All tests pass. Existing fields/methods unchanged.</done>
</task>

</tasks>

<verification>
Run full test suite to confirm nothing breaks:
```
cd "C:/Users/danii/OneDrive/Desktop/PythonDMCGUI" && python -m pytest tests/ -v
```
All tests green.
</verification>

<success_criteria>
- AuthManager creates default users.json on first boot
- PIN validation returns correct role or None
- last_user persists across AuthManager reloads
- MachineState has current_user, current_role, setup_unlocked fields
- set_auth() and lock_setup() notify listeners
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-and-navigation/01-01-SUMMARY.md`
</output>
