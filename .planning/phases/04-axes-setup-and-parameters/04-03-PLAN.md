---
phase: 04-axes-setup-and-parameters
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified: []
autonomous: false
requirements: [AXES-01, AXES-02, AXES-03, AXES-04, AXES-05, AXES-06, PARAM-01, PARAM-02, PARAM-03, PARAM-04, PARAM-05, PARAM-06, PARAM-07]

must_haves:
  truths:
    - "Both screens load and display correctly in the app"
    - "Axes Setup sidebar selects axes and updates the view"
    - "Parameters grouped cards render with correct labels and inputs"
    - "Full test suite passes with no regressions"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of both new screens. Run the full test suite, launch the app, and verify both AxesSetupScreen and ParametersScreen render correctly with proper navigation, styling, and interactivity.

Purpose: Catch visual/layout issues and integration problems that unit tests cannot detect.
Output: Verified working screens ready for Phase 5.
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-axes-setup-and-parameters/04-01-SUMMARY.md
@.planning/phases/04-axes-setup-and-parameters/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and fix any regressions</name>
  <files></files>
  <action>
    Run `pytest tests/ -v` and verify all tests pass — both new (test_axes_setup.py, test_parameters.py) and existing tests. If any test fails, diagnose and fix. Common issues to watch for:
    - KV parse errors from the new .kv files (check widget names match class names)
    - Import errors if screen class names changed
    - Existing tests broken by new imports in __init__.py
  </action>
  <verify>
    <automated>cd C:/Users/danii/OneDrive/Desktop/PythonDMCGUI && python -m pytest tests/ -v</automated>
  </verify>
  <done>All tests pass — zero failures, zero errors. Both new test files and all existing tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of both screens</name>
  <files></files>
  <action>
    Present both screens to user for visual verification. Launch the app and guide the user through checking both AxesSetupScreen and ParametersScreen.
  </action>
  <verify>User confirms screens look correct and function as expected</verify>
  <done>User approves both screens</done>
  <what-built>Two new screens replacing placeholders: AxesSetupScreen (sidebar axis selection, jog controls, teach buttons, quick actions) and ParametersScreen (grouped parameter cards with validation, dirty tracking, apply/read buttons)</what-built>
  <how-to-verify>
    1. Launch the app: `cd C:/Users/danii/OneDrive/Desktop/PythonDMCGUI && python -m dmccodegui`
    2. Log in as a Setup user
    3. Navigate to "Axes Setup" tab:
       - Verify sidebar shows A (Feed), B (Lift), C (Cross), D (Rotation) buttons
       - Tap each axis button — view should update to show that axis
       - Verify position cards (Rest Point, Start Point, Current Position) are visible
       - Verify jog arrow buttons and step toggle (10/5/1) are visible
       - Verify Teach buttons and quick action buttons are visible
       - D axis should show "deg" unit label instead of "mm"
    4. Navigate to "Parameters" tab:
       - Verify grouped cards: Geometry, Feedrates, Calibration
       - Each row shows: label, DMC code, input field, unit
       - Bottom bar shows change counter and Apply button
       - If controller not connected, fields should show empty/default
    5. Log out and log in as Operator:
       - Verify Axes Setup tab is NOT visible (role-gated)
       - Navigate to Parameters (if visible): verify fields are readonly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Full test suite green
- Both screens visually correct and navigable
- Role gating works (Operator cannot access Axes Setup)
- No crashes on screen transitions
</verification>

<success_criteria>
- User confirms both screens look correct and function as expected
- All tests pass
- Phase 4 complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-axes-setup-and-parameters/04-03-SUMMARY.md`
</output>
