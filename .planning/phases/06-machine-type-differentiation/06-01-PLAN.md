---
phase: 06-machine-type-differentiation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/dmccodegui/machine_config.py
  - src/dmccodegui/app_state.py
  - tests/test_machine_config.py
autonomous: true
requirements: [MACH-01, MACH-03]

must_haves:
  truths:
    - "Registry contains all 3 machine types with correct axis lists and param defs"
    - "Active machine type can be set, persisted to settings.json, and read back after re-init"
    - "get_axis_list returns ['A','B','C'] for Serration and ['A','B','C','D'] for Flat/Convex"
    - "is_serration returns True only for 3-Axes Serration Grind"
    - "set_active_type raises ValueError for unknown machine type strings"
    - "MachineState.machine_type field propagates via notify()"
  artifacts:
    - path: "src/dmccodegui/machine_config.py"
      provides: "Single source of truth for machine type data — registry, persistence, API"
      exports: ["MACHINE_TYPES", "init", "get_active_type", "set_active_type", "is_configured", "get_param_defs", "get_axis_list", "is_serration"]
    - path: "src/dmccodegui/app_state.py"
      provides: "MachineState with machine_type field"
      contains: "machine_type: str"
    - path: "tests/test_machine_config.py"
      provides: "Unit tests for machine_config module"
      min_lines: 60
  key_links:
    - from: "src/dmccodegui/machine_config.py"
      to: "src/dmccodegui/auth/settings.json"
      via: "json.load/json.dump persistence"
      pattern: "json\\.(load|dump)"
    - from: "src/dmccodegui/app_state.py"
      to: "src/dmccodegui/machine_config.py"
      via: "machine_type field mirrors active type"
      pattern: "machine_type"
---

<objective>
Create the machine_config module — the single source of truth for machine-type data — and extend MachineState with a machine_type field. This is the foundation all subsequent Phase 6 plans build on.

Purpose: Every screen in the app needs to query "what machine type am I on?" to decide which axes, parameters, and widgets to show. This plan builds that query layer and proves it works with tests before any screen touches it.

Output: `machine_config.py` module with registry, persistence, and API. `app_state.py` extended with `machine_type` field. Full test suite.
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-machine-type-differentiation/06-CONTEXT.md
@.planning/phases/06-machine-type-differentiation/06-RESEARCH.md

@src/dmccodegui/app_state.py
@src/dmccodegui/screens/parameters.py (first 50 lines — PARAM_DEFS structure)
@src/dmccodegui/auth/auth_manager.py (JSON persistence pattern to mirror)

<interfaces>
<!-- Existing interfaces this plan must preserve -->

From src/dmccodegui/app_state.py:
```python
@dataclass
class MachineState:
    # ... existing fields ...
    _listeners: List[ChangeListener] = field(default_factory=list, repr=False)
    def subscribe(self, fn: ChangeListener) -> Callable[[], None]: ...
    def notify(self) -> None: ...
```

From src/dmccodegui/screens/parameters.py:
```python
PARAM_DEFS = [
    {"label": "Knife Thickness", "var": "knfThk", "unit": "mm", "group": "Geometry", "min": 0.1, "max": 50.0},
    # ... more entries with same structure
]
```
</interfaces>
</context>

<feature>
  <name>Machine Config Registry + MachineState Extension</name>
  <files>src/dmccodegui/machine_config.py, src/dmccodegui/app_state.py, tests/test_machine_config.py</files>
  <behavior>
    - test_registry_has_all_types: MACHINE_TYPES list contains exactly ["4-Axes Flat Grind", "4-Axes Convex Grind", "3-Axes Serration Grind"]
    - test_flat_axis_list: get_axis_list("4-Axes Flat Grind") returns ["A", "B", "C", "D"]
    - test_convex_axis_list: get_axis_list("4-Axes Convex Grind") returns ["A", "B", "C", "D"]
    - test_serration_axis_list: get_axis_list("3-Axes Serration Grind") returns ["A", "B", "C"]
    - test_is_serration_true: is_serration("3-Axes Serration Grind") returns True
    - test_is_serration_false_flat: is_serration("4-Axes Flat Grind") returns False
    - test_unknown_type_raises: set_active_type("bogus") raises ValueError
    - test_persistence_roundtrip: set_active_type writes to tmp settings.json; new init() reads it back correctly
    - test_not_configured_on_fresh_init: init(nonexistent_path) -> is_configured() returns False
    - test_param_defs_per_type: get_param_defs returns a list of dicts, each with keys "label", "var", "unit", "group", "min", "max"
    - test_flat_param_defs_match_existing: get_param_defs("4-Axes Flat Grind") returns entries matching current PARAM_DEFS from parameters.py
    - test_has_bcomp_serration: Registry entry for Serration has "has_bcomp": True
    - test_has_bcomp_flat_convex: Registry entries for Flat and Convex have "has_bcomp": False
    - test_machine_state_machine_type_field: MachineState has a machine_type field defaulting to ""
    - test_machine_state_notify_on_type_change: Setting machine_type and calling notify() triggers subscriber
  </behavior>
  <implementation>
    1. Create tests/test_machine_config.py with all tests above (RED phase — all fail).
    2. Create src/dmccodegui/machine_config.py following the registry pattern from RESEARCH.md:
       - MACHINE_TYPES list with all 3 type strings
       - _REGISTRY dict keyed by type string, each containing: axes (list), has_bcomp (bool), param_defs (list of dicts)
       - Flat Grind param_defs: copy the exact PARAM_DEFS list from parameters.py
       - Convex Grind param_defs: same as Flat for now (user said "specific DMC variable lists will be provided later")
       - Serration Grind param_defs: same as Flat minus D-axis entries (fdD, pitchD, ratioD, ctsRevD) for now — placeholder
       - init(settings_path), get_active_type(), set_active_type(mtype), is_configured()
       - get_param_defs(mtype=None), get_axis_list(mtype=None), is_serration(mtype=None)
       - _save() using json.dump — mirrors AuthManager pattern
       - Settings file path: src/dmccodegui/auth/settings.json (alongside users.json)
    3. Extend MachineState in app_state.py: add `machine_type: str = ""` field (with default to preserve backward compatibility)
    4. Run tests (GREEN phase — all pass).

    IMPORTANT design decisions per CONTEXT.md:
    - param_defs structure is plug-in friendly — each type gets its own complete list, NOT shared groups with type filters
    - Convex and Serration param_defs are stubs (copies/subsets of Flat) with clear TODO comments — real variable lists come later
    - Group color scheme consistent by group name: orange=Geometry, cyan=Feedrates, purple=Calibration (colors not stored in machine_config — already in parameters.py GROUP_COLORS)
    - CPM defaults same across all types (A=1200, B=1200, C=800, D=500) — not stored in machine_config
  </implementation>
</feature>

<verification>
<automated>pytest tests/test_machine_config.py -x</automated>
</verification>

<success_criteria>
- All 15 tests pass
- machine_config.py exports all 8 public functions
- MachineState.machine_type field exists with "" default
- Settings roundtrip works (write + re-init reads same type)
- No Kivy dependency in machine_config.py (pure Python, testable headless)
</success_criteria>

<output>
After completion, create `.planning/phases/06-machine-type-differentiation/06-01-SUMMARY.md`
</output>
