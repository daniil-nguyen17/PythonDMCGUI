---
phase: 06-machine-type-differentiation
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/dmccodegui/screens/run.py
  - src/dmccodegui/ui/run.kv
  - src/dmccodegui/screens/axes_setup.py
  - src/dmccodegui/ui/axes_setup.kv
  - src/dmccodegui/screens/parameters.py
autonomous: true
requirements: [MACH-01, MACH-02]

must_haves:
  truths:
    - "On Flat Grind: RUN page shows Delta-C bar chart, all 4 axis positions, no tooth/pass/depth in cycle status"
    - "On Convex Grind: same layout as Flat Grind"
    - "On Serration Grind: RUN page shows bComp bar chart instead of Delta-C, hides D axis position, shows tooth/pass/depth in cycle status"
    - "On Serration Grind: Axes Setup sidebar shows only A, B, C — D button is hidden (not greyed out)"
    - "On Flat/Convex: Axes Setup sidebar shows all 4 axes"
    - "ParametersScreen builds cards from machine_config.get_param_defs() per active type"
    - "Hot-swap: changing machine type immediately updates screens without app restart"
    - "Teach methods only write axes that exist for current machine type"
  artifacts:
    - path: "src/dmccodegui/screens/run.py"
      provides: "BCompBarChart widget, machine-type-aware panel switching, no module-level MACHINE_TYPE/IS_SERRATION"
      contains: "BCompBarChart"
    - path: "src/dmccodegui/ui/run.kv"
      provides: "bComp panel layout (hidden on Flat/Convex), D axis row hideable"
      contains: "bcomp_panel"
    - path: "src/dmccodegui/screens/axes_setup.py"
      provides: "Axis sidebar filtering, teach method axis-aware filtering"
      contains: "mc.get_axis_list"
    - path: "src/dmccodegui/screens/parameters.py"
      provides: "Dynamic PARAM_DEFS from machine_config, rebuild on type change"
      contains: "mc.get_param_defs"
  key_links:
    - from: "src/dmccodegui/screens/run.py"
      to: "src/dmccodegui/machine_config.py"
      via: "mc.is_serration() at runtime decision points"
      pattern: "mc\\.is_serration"
    - from: "src/dmccodegui/screens/axes_setup.py"
      to: "src/dmccodegui/machine_config.py"
      via: "mc.get_axis_list() for sidebar and teach filtering"
      pattern: "mc\\.get_axis_list"
    - from: "src/dmccodegui/screens/parameters.py"
      to: "src/dmccodegui/machine_config.py"
      via: "mc.get_param_defs() for card building"
      pattern: "mc\\.get_param_defs"
---

<objective>
Adapt all three content screens (RunScreen, AxesSetupScreen, ParametersScreen) to dynamically reconfigure based on the active machine type. This is the core "differentiation" work — making the UI change shape per machine.

Purpose: Without this plan, the app looks the same regardless of machine type. After this plan, Serration hides D axis and shows bComp; Flat/Convex shows all 4 axes and Delta-C; parameters rebuild per type.

Output: All screens respond to machine type changes. BCompBarChart widget created. Module-level MACHINE_TYPE/IS_SERRATION constants removed.
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-machine-type-differentiation/06-CONTEXT.md
@.planning/phases/06-machine-type-differentiation/06-RESEARCH.md
@.planning/phases/06-machine-type-differentiation/06-01-SUMMARY.md

@src/dmccodegui/screens/run.py
@src/dmccodegui/ui/run.kv
@src/dmccodegui/screens/axes_setup.py
@src/dmccodegui/ui/axes_setup.kv
@src/dmccodegui/screens/parameters.py
@src/dmccodegui/ui/parameters.kv

<interfaces>
<!-- From Plan 01 (machine_config module) -->

From src/dmccodegui/machine_config.py:
```python
MACHINE_TYPES: list[str]  # ["4-Axes Flat Grind", "4-Axes Convex Grind", "3-Axes Serration Grind"]

def get_active_type() -> Optional[str]: ...
def set_active_type(mtype: str) -> None: ...
def is_configured() -> bool: ...
def get_param_defs(mtype: Optional[str] = None) -> list[dict]: ...
def get_axis_list(mtype: Optional[str] = None) -> list[str]: ...
def is_serration(mtype: Optional[str] = None) -> bool: ...
```

From src/dmccodegui/screens/run.py (existing):
```python
class DeltaCBarChart(Widget):
    offsets = ListProperty([])
    selected_index = NumericProperty(-1)
    max_offset = NumericProperty(500)
    # DELTA_C_STEP = 50
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RunScreen — BCompBarChart, Delta-C/bComp toggle, remove stale constants</name>
  <files>src/dmccodegui/screens/run.py, src/dmccodegui/ui/run.kv</files>
  <action>
  **Remove stale module-level constants from run.py:**
  1. Delete `MACHINE_TYPE = "4axis_flat"` and `IS_SERRATION = MACHINE_TYPE == "3axis_serration"`
  2. Replace ALL references to `IS_SERRATION` with `mc.is_serration()` called at the decision point (import `dmccodegui.machine_config as mc` at top of file)
  3. If `is_serration` is currently a BooleanProperty on RunScreen initialized from the module constant, change it to be set dynamically in `on_pre_enter` or `on_kv_post` via `self.is_serration = mc.is_serration()`

  **Create BCompBarChart widget in run.py:**
  1. Create `BCompBarChart(Widget)` class — mirrors `DeltaCBarChart` exactly in structure:
     - `offsets = ListProperty([])`
     - `selected_index = NumericProperty(-1)`
     - `max_offset = NumericProperty(500)`
     - `BCOMP_STEP = 50` (constant, same as DELTA_C_STEP)
     - `_draw()` method — identical bar chart rendering logic as DeltaCBarChart (zero baseline, per-bar colored rectangles, selection highlight)
     - `on_touch_down()` — identical tap-to-select logic
     - DMC array name: `bComp` (not `deltaC`)
     - Array size driven by `numSerr` variable (number of serrations)
  2. Alternatively, if the two charts are identical except for the array name and step constant, extract a shared base class `_BaseBarChart(Widget)` and have both `DeltaCBarChart` and `BCompBarChart` inherit from it. Use Claude's judgment on whether the duplication is worth abstracting.

  **Add bComp panel to run.kv:**
  1. In the same container where the Delta-C panel lives, add a `bcomp_panel` BoxLayout with `id: bcomp_panel` containing the BCompBarChart widget and Up/Down adjustment buttons (same layout as Delta-C panel)
  2. The bcomp_panel starts with `opacity: 0` and `disabled: True` (hidden by default — Flat Grind is the most common first use)
  3. Give the existing Delta-C container an `id: delta_c_panel` if it doesn't already have one

  **Add D axis position row visibility:**
  1. Give the D axis position label/row in run.kv an `id: pos_d_row` if it doesn't already have one

  **Add _apply_machine_type_widgets() to RunScreen:**
  1. Create `_apply_machine_type_widgets(self)` method:
     - `serration = mc.is_serration()`
     - Delta-C panel: `opacity = 0 if serration else 1`, `disabled = serration`
     - bComp panel: `opacity = 1 if serration else 0`, `disabled = not serration`
     - D axis position row: `opacity = 0 if serration else 1`
     - Update `self.is_serration` BooleanProperty if it exists (for cycle status field visibility in KV)
  2. Call `_apply_machine_type_widgets()` from `on_pre_enter()` — this ensures the correct layout on every screen entry, including after a hot-swap

  **bComp controller integration stubs:**
  1. In the RunScreen methods that read/write `deltaC` array from controller, add parallel logic for `bComp`:
     - Read: if serration, read `bComp` array (size from `numSerr` variable) instead of `deltaC`
     - Write: if serration, write to `bComp` array instead of `deltaC`
     - If the existing Delta-C read/write methods are tightly coupled, add separate `_read_bcomp()` / `_write_bcomp()` methods
  2. Use opacity/disabled swap (NOT widget add/remove) for panel visibility — preserves KV ids (per Phase 01-03 lesson)
  </action>
  <verify>
    <automated>pytest tests/test_run_screen.py -x 2>/dev/null; python -c "from dmccodegui.screens.run import BCompBarChart, DeltaCBarChart; print('Both chart classes importable: OK')"</automated>
  </verify>
  <done>
  - MACHINE_TYPE and IS_SERRATION module constants removed from run.py
  - BCompBarChart widget exists with same interface as DeltaCBarChart
  - run.kv has both delta_c_panel and bcomp_panel with ids
  - _apply_machine_type_widgets correctly toggles panels based on mc.is_serration()
  - D axis position row hidden on Serration
  - Existing run_screen tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: AxesSetupScreen sidebar filtering and teach method axis awareness</name>
  <files>src/dmccodegui/screens/axes_setup.py, src/dmccodegui/ui/axes_setup.kv</files>
  <action>
  **Axis sidebar filtering:**
  1. Add `import dmccodegui.machine_config as mc` at top of axes_setup.py
  2. Create `_rebuild_axis_sidebar(self)` method:
     - Get `axis_list = mc.get_axis_list()` (returns ["A","B","C"] for Serration, ["A","B","C","D"] for others)
     - For each axis button in the sidebar (A, B, C, D): set `opacity = 1 if axis in axis_list else 0` and `disabled = axis not in axis_list`
     - The KV sidebar buttons need `id` attributes for programmatic access. Check if axes_setup.kv already has ids like `axis_btn_a`. If not, add them: `id: axis_btn_a`, `id: axis_btn_b`, `id: axis_btn_c`, `id: axis_btn_d`
     - If the selected axis is now hidden (e.g., D selected but switched to Serration), auto-select the first visible axis
  3. Call `_rebuild_axis_sidebar()` from `on_pre_enter()` — ensures correct sidebar on every entry

  **Teach method axis filtering (per RESEARCH.md Pitfall 4):**
  1. In `teach_rest_point()` and `teach_start_point()`:
     - Get `axis_list = mc.get_axis_list()`
     - Only read positions for axes in the list: `MG _TD{axis}` for each axis in axis_list
     - Only write scalar vars for axes in the list: build the semicolon-separated command dynamically from axis_list (e.g., `restPtA=...;restPtB=...;restPtC=...` for Serration, without restPtD)
     - BV burn command is still sent regardless (applies to whatever was written)
  2. Guard: if `axis_list` is empty (shouldn't happen but defensive), log error and return

  **Quick actions:**
  - `go_to_rest_all()`, `go_to_start_all()`, `home_all()` — these send single-variable commands (swGoRest=1 etc.) that the controller program handles internally. No axis filtering needed — the controller program knows which axes to move. Leave these unchanged.

  **Polling:**
  - In the 3 Hz poll callback, only read positions for axes in `mc.get_axis_list()`. Skip D axis position read on Serration to avoid controller errors for undefined variables.
  </action>
  <verify>
    <automated>pytest tests/test_axes_setup.py -x 2>/dev/null || echo "axes tests run"</automated>
  </verify>
  <done>
  - Serration hides D axis button (opacity=0, disabled=True), not greyed out
  - Flat/Convex shows all 4 axis buttons
  - teach_rest_point/teach_start_point only write axes in mc.get_axis_list()
  - Polling only reads positions for active axes
  - Switching from D-selected to Serration auto-selects first visible axis
  </done>
</task>

<task type="auto">
  <name>Task 3: ParametersScreen dynamic PARAM_DEFS from machine_config</name>
  <files>src/dmccodegui/screens/parameters.py</files>
  <action>
  **Replace module-level PARAM_DEFS with dynamic loading:**
  1. Keep the existing `PARAM_DEFS` list in parameters.py but rename it to `_FLAT_GRIND_PARAM_DEFS` (or simply remove it — machine_config.py now owns it). The module-level `PARAM_DEFS` variable should NO LONGER be the authoritative source.
  2. Remove or update the module-level `_PARAM_BY_VAR` dict — it should NOT be computed at import time from a static list. Instead, compute it inside methods that need it, from `mc.get_param_defs()`.
  3. Add `import dmccodegui.machine_config as mc` at top of parameters.py

  **Add _rebuild_for_machine_type() method:**
  1. Create `_rebuild_for_machine_type(self)`:
     - `param_defs = mc.get_param_defs()`
     - `self._param_defs = {p["var"]: p for p in param_defs}` (replaces old _PARAM_BY_VAR)
     - Clear `self._field_widgets` dict
     - Clear `self._dirty` set
     - Set `self.pending_count = 0`
     - Call `self.build_param_cards()` to rebuild all parameter cards from the new param_defs
  2. Call `_rebuild_for_machine_type()` at the START of `on_pre_enter()`, before `_apply_role_mode()` and `read_from_controller()`

  **Update build_param_cards():**
  1. Modify `build_param_cards()` to read from `mc.get_param_defs()` instead of the module-level `PARAM_DEFS`
  2. Clear the cards container before rebuilding (to handle hot-swap without duplicates)
  3. `GROUP_COLORS` dict stays in parameters.py — it applies by group name and is consistent across types per CONTEXT.md decision

  **Update validation and apply methods:**
  1. Any method that references `_PARAM_BY_VAR` should use `self._param_defs` (the instance-level dict built in `_rebuild_for_machine_type`)
  2. `apply_to_controller()` and `read_from_controller()` should iterate `mc.get_param_defs()` (or `self._param_defs`) to know which vars to read/write — not a static module-level list

  IMPORTANT: Do NOT remove GROUP_COLORS or the validation constants (BORDER_NORMAL, BORDER_AMBER, BORDER_RED, _ZERO_REJECT_GROUPS, _NEGATIVE_REJECT_GROUPS) — these are shared across all machine types.
  </action>
  <verify>
    <automated>pytest tests/test_parameters.py -x 2>/dev/null || echo "parameters tests run"</automated>
  </verify>
  <done>
  - parameters.py no longer has a module-level PARAM_DEFS as the authoritative source
  - build_param_cards reads from mc.get_param_defs() dynamically
  - _rebuild_for_machine_type clears and rebuilds cards on every on_pre_enter
  - Hot-swap: changing machine type and entering ParametersScreen shows the correct parameters for the new type
  - GROUP_COLORS and validation constants preserved
  </done>
</task>

</tasks>

<verification>
- On Flat Grind: all 4 axes in sidebar, Delta-C chart visible, all 4 axis positions on RUN page
- On Serration: only A/B/C in sidebar, bComp chart visible, D axis position hidden, tooth/pass/depth shown
- On Convex: same as Flat
- Switching machine type and navigating to any screen shows correct layout
- pytest tests/ -x passes (all existing tests still green)
</verification>

<success_criteria>
- All three screens dynamically adapt to machine type
- No module-level MACHINE_TYPE or IS_SERRATION constants remain
- BCompBarChart widget functional for Serration machines
- Axes sidebar hides D on Serration (opacity/disabled, not removed)
- Teach methods only write axes for current machine type
- Parameters rebuild from machine_config on every screen entry
</success_criteria>

<output>
After completion, create `.planning/phases/06-machine-type-differentiation/06-03-SUMMARY.md`
</output>
