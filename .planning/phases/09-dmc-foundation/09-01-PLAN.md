---
phase: 09-dmc-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dmccodegui/hmi/__init__.py
  - src/dmccodegui/hmi/dmc_vars.py
  - src/dmccodegui/app_state.py
  - tests/test_dmc_vars.py
autonomous: true
requirements: [DMC-01, DMC-06]

must_haves:
  truths:
    - "Importing from dmccodegui.hmi.dmc_vars returns correct string constants for all 8 HMI trigger variables"
    - "All DMC variable name constants are 8 characters or fewer"
    - "MachineState has a dmc_state integer field defaulting to 0"
    - "hmiState integer constants match the encoding: IDLE=1, GRINDING=2, SETUP=3, HOMING=4"
    - "Position variable constants (RESTPT_*, STARTPT_*) exactly match DMC-declared names"
  artifacts:
    - path: "src/dmccodegui/hmi/__init__.py"
      provides: "HMI package init"
    - path: "src/dmccodegui/hmi/dmc_vars.py"
      provides: "All DMC variable name constants and hmiState encoding"
      exports: ["HMI_GRND", "HMI_SETP", "HMI_MORE", "HMI_LESS", "HMI_NEWS", "HMI_HOME", "HMI_JOG", "HMI_CALC", "HMI_STATE_VAR", "STATE_IDLE", "STATE_GRINDING", "STATE_SETUP", "STATE_HOMING", "RESTPT_A", "STARTPT_A", "RESTPT_VARS", "STARTPT_VARS", "RESTPT_BY_AXIS", "STARTPT_BY_AXIS"]
    - path: "src/dmccodegui/app_state.py"
      provides: "dmc_state field on MachineState"
      contains: "dmc_state"
    - path: "tests/test_dmc_vars.py"
      provides: "Unit tests for constants module and app_state field"
  key_links:
    - from: "src/dmccodegui/hmi/dmc_vars.py"
      to: "DMC program variable names"
      via: "string constant values"
      pattern: "hmiGrnd|hmiSetp|hmiMore|hmiLess|hmiNewS|hmiHome|hmiJog|hmiCalc"
    - from: "src/dmccodegui/app_state.py"
      to: "src/dmccodegui/hmi/dmc_vars.py"
      via: "dmc_state field stores polled hmiState value"
      pattern: "dmc_state.*int.*0"
---

<objective>
Create the HMI-controller integration contract: the dmc_vars.py constants module and MachineState.dmc_state field.

Purpose: This module is the single source of truth for all DMC variable names used by Python code. Every screen file that communicates with the controller will import from here instead of using raw string literals. This prevents 8-char name typos and ensures Python and DMC stay in sync.

Output: src/dmccodegui/hmi/dmc_vars.py, hmi/__init__.py, updated app_state.py, tests/test_dmc_vars.py
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dmc-foundation/09-CONTEXT.md
@.planning/phases/09-dmc-foundation/09-RESEARCH.md

@src/dmccodegui/app_state.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create hmi/dmc_vars.py constants module and tests</name>
  <files>src/dmccodegui/hmi/__init__.py, src/dmccodegui/hmi/dmc_vars.py, tests/test_dmc_vars.py</files>
  <behavior>
    - Test: All 8 HMI trigger constant values are strings of 8 chars or fewer
    - Test: HMI_GRND == "hmiGrnd", HMI_SETP == "hmiSetp", etc. (exact DMC names)
    - Test: HMI_TRIGGER_DEFAULT == 1, HMI_TRIGGER_FIRE == 0
    - Test: STATE_IDLE == 1, STATE_GRINDING == 2, STATE_SETUP == 3, STATE_HOMING == 4, STATE_UNINITIALIZED == 0
    - Test: HMI_STATE_VAR == "hmiState" and len <= 8
    - Test: RESTPT_A == "restPtA", RESTPT_B == "restPtB", etc.
    - Test: STARTPT_A == "startPtA", STARTPT_B == "startPtB", etc.
    - Test: All position var names are 8 chars or fewer
    - Test: RESTPT_VARS is list of 4 constants in A,B,C,D order
    - Test: STARTPT_VARS is list of 4 constants in A,B,C,D order
    - Test: RESTPT_BY_AXIS["A"] == RESTPT_A, etc.
    - Test: STARTPT_BY_AXIS["A"] == STARTPT_A, etc.
    - Test: No raw "StartPnt" or "RestPnt" strings in any screen file under src/dmccodegui/screens/ (grep-style test)
  </behavior>
  <action>
    1. Create src/dmccodegui/hmi/ directory with __init__.py:
       - Minimal docstring, `from . import dmc_vars` re-export

    2. Create src/dmccodegui/hmi/dmc_vars.py following EXACTLY the Pattern 1 from 09-RESEARCH.md:
       - HMI trigger variables: HMI_GRND="hmiGrnd", HMI_SETP="hmiSetp", HMI_MORE="hmiMore", HMI_LESS="hmiLess", HMI_NEWS="hmiNewS", HMI_HOME="hmiHome", HMI_JOG="hmiJog", HMI_CALC="hmiCalc"
       - Trigger defaults: HMI_TRIGGER_DEFAULT=1, HMI_TRIGGER_FIRE=0
       - State var: HMI_STATE_VAR="hmiState"
       - State encoding: STATE_UNINITIALIZED=0, STATE_IDLE=1, STATE_GRINDING=2, STATE_SETUP=3, STATE_HOMING=4
       - Position vars: RESTPT_A="restPtA" through RESTPT_D, STARTPT_A="startPtA" through STARTPT_D
       - Ordered lists: RESTPT_VARS=[RESTPT_A, ...], STARTPT_VARS=[STARTPT_A, ...]
       - Axis maps: RESTPT_BY_AXIS={"A": RESTPT_A, ...}, STARTPT_BY_AXIS={"A": STARTPT_A, ...}
       - ALL_HMI_TRIGGERS list for batch operations

    3. Create tests/test_dmc_vars.py with all behavior tests listed above. Include a test that globs src/dmccodegui/screens/*.py and asserts no file contains raw "RestPnt" or "StartPnt" string literals in non-comment Python code. NOTE: This "no stale strings" test will FAIL initially (expected — screen files not yet migrated). Mark it with @pytest.mark.xfail(reason="Screen migration in plan 09-03") so the test suite stays green.

    Write tests FIRST (RED), then create the constants module (GREEN).
  </action>
  <verify>
    <automated>pytest tests/test_dmc_vars.py -x -v</automated>
  </verify>
  <done>All tests pass. hmi/dmc_vars.py exports all constants with correct values. The xfail test for stale strings is expected to fail until Plan 03.</done>
</task>

<task type="auto">
  <name>Task 2: Add dmc_state field to MachineState dataclass</name>
  <files>src/dmccodegui/app_state.py, tests/test_dmc_vars.py</files>
  <action>
    1. In src/dmccodegui/app_state.py, add `dmc_state: int = 0` field to the MachineState dataclass, after the `cycle_completion_pct` field. Add comment: `# hmiState from controller; 0=uninitialized, 1=IDLE, 2=GRINDING, 3=SETUP, 4=HOMING`

    2. Add a test in tests/test_dmc_vars.py (or append to existing):
       - `test_app_state_dmc_state`: Import MachineState, create instance, assert `ms.dmc_state == 0`, assert field exists as int.
  </action>
  <verify>
    <automated>pytest tests/test_dmc_vars.py tests/test_app_state.py -x -v</automated>
  </verify>
  <done>MachineState.dmc_state exists with default 0. All existing app_state tests still pass. New test confirms the field.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_dmc_vars.py -x -v` — all tests pass (stale-string test xfail is expected)
- `pytest tests/test_app_state.py -x -v` — existing tests still pass
- `python -c "from dmccodegui.hmi.dmc_vars import HMI_GRND; print(HMI_GRND)"` prints "hmiGrnd"
- `python -c "from dmccodegui.app_state import MachineState; print(MachineState().dmc_state)"` prints "0"
</verification>

<success_criteria>
- hmi/dmc_vars.py exists with all 8 trigger constants, 5 state constants, 8 position constants, ordered lists, and axis maps
- All constant values are exact DMC names, all 8 chars or fewer
- MachineState.dmc_state field exists, defaults to 0
- All tests pass (except xfail stale-string test)
- Full test suite (`pytest tests/ -x`) still green
</success_criteria>

<output>
After completion, create `.planning/phases/09-dmc-foundation/09-01-SUMMARY.md`
</output>
