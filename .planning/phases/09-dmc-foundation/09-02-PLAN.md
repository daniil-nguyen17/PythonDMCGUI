---
phase: 09-dmc-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "4 Axis Stainless grind.dmc"
autonomous: true
requirements: [DMC-02, DMC-03, DMC-04, DMC-05]

must_haves:
  truths:
    - "Every physical-button IF block in #WtAtRt has an OR condition with its corresponding HMI variable"
    - "Every physical-button IF block in #SULOOP has an OR condition with its corresponding HMI variable"
    - "Each HMI variable is reset to 1 as the FIRST line inside its triggered block, before any SB/JS/motion command"
    - "hmiState is set to a distinct nonzero integer at each state boundary: 2 before #GRIND, 3 in #SETUP, 4 at start of #HOME, 1 on return to idle"
    - "startPt[]/restPt[] DM array declarations are removed and replaced with 8 individual scalar variables"
    - "hmiBtn[40] DM array declaration is removed"
    - "All subroutines that used startPt[n] or restPt[n] now use the corresponding scalar (startPtA, restPtC, etc.)"
  artifacts:
    - path: "4 Axis Stainless grind.dmc"
      provides: "Modified DMC program with HMI integration"
      contains: "hmiGrnd|hmiState|restPtA|startPtA"
  key_links:
    - from: "4 Axis Stainless grind.dmc #PARAMS"
      to: "src/dmccodegui/hmi/dmc_vars.py"
      via: "Variable names declared in DMC must match Python constants"
      pattern: "hmiGrnd = 1|hmiState = 1|startPtA = 0|restPtA = 0"
    - from: "4 Axis Stainless grind.dmc #WtAtRt"
      to: "HMI trigger pattern"
      via: "OR conditions enable Python to trigger actions"
      pattern: "hmiGrnd = 0|hmiSetp = 0|hmiNewS = 0|hmiMore = 0|hmiLess = 0"
    - from: "4 Axis Stainless grind.dmc #SULOOP"
      to: "HMI trigger pattern"
      via: "OR conditions enable Python to trigger setup actions"
      pattern: "hmiJog = 0|hmiHome = 0|hmiCalc = 0"
---

<objective>
Modify the DMC program file to add HMI trigger variables, OR conditions in polling loops, hmiState tracking, and convert position arrays to individual scalar variables.

Purpose: This makes the controller respond to both physical buttons AND Python-sent variable writes. Without these modifications, no HMI button can trigger any machine action. This is the hard prerequisite for all v2.0 wiring phases.

Output: Modified `4 Axis Stainless grind.dmc` at repo root
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dmc-foundation/09-CONTEXT.md
@.planning/phases/09-dmc-foundation/09-RESEARCH.md

@4 Axis Stainless grind.dmc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modify #PARAMS — declare HMI variables and convert arrays to scalars</name>
  <files>4 Axis Stainless grind.dmc</files>
  <action>
    In the #PARAMS block (around line 448-473):

    1. REMOVE these three lines:
       - `DM startPt[4]` (and its comment)
       - `DM restPt[4]` (and its comment)
       - `DM hmiBtn[40]`

    2. ADD after existing scalar variable assignments (but before EN or any other block boundary):
       ```
       ' --- HMI trigger variables (default=1, send 0 to trigger) ---
       hmiGrnd = 1;  hmiSetp = 1;  hmiMore = 1;  hmiLess = 1
       hmiNewS = 1;  hmiHome = 1;  hmiJog  = 1;  hmiCalc = 1
       ' --- Machine state (1=IDLE 2=GRINDING 3=SETUP 4=HOMING) ---
       hmiState = 1
       ' --- Position scalars (4 axes, unused hold 0) ---
       startPtA = 0; startPtB = 0; startPtC = 0; startPtD = 0
       restPtA  = 0; restPtB  = 0; restPtC  = 0; restPtD  = 0
       ```

    3. Keep ALL existing DM declarations for indexed arrays (deltaD[], deltaC[], bComp[], etc.) — only remove the three listed above.

    4. Leave all existing header comments untouched.

    Do NOT add BV calls anywhere in this task.
  </action>
  <verify>
    <automated>python -c "
import re
with open('4 Axis Stainless grind.dmc', 'r') as f: content = f.read()
# Verify removals
assert 'DM startPt[' not in content, 'startPt array still declared'
assert 'DM restPt[' not in content, 'restPt array still declared'
assert 'DM hmiBtn[' not in content, 'hmiBtn array still declared'
# Verify additions
assert 'hmiGrnd = 1' in content, 'hmiGrnd not declared'
assert 'hmiState = 1' in content, 'hmiState not declared'
assert 'startPtA = 0' in content, 'startPtA not declared'
assert 'restPtA' in content and 'restPtD' in content, 'restPt scalars missing'
# Verify indexed arrays preserved
assert 'DM deltaD[' in content, 'deltaD array lost'
print('PARAMS modifications verified')
"
    </automated>
  </verify>
  <done>
    - DM startPt[4], DM restPt[4], DM hmiBtn[40] removed
    - 8 HMI trigger variables declared with default=1
    - hmiState declared with default=1
    - 8 position scalar variables declared (startPtA-D, restPtA-D)
    - All other DM array declarations preserved
  </done>
</task>

<task type="auto">
  <name>Task 2: Modify #WtAtRt, #SULOOP, subroutines — add OR conditions, hmiState, and scalar refs</name>
  <files>4 Axis Stainless grind.dmc</files>
  <action>
    Read the full DMC file. Apply ALL of the following modifications in a single pass. Reference the "Complete DMC Modification Map" in 09-RESEARCH.md for exact line locations.

    **#WtAtRt block (5 IF blocks):**
    For each IF block, add `| (hmiVar = 0)` to the condition, add `hmiVar = 1` as FIRST line inside the block (before any SB or JS), and add hmiState transitions:

    1. @IN[29]=0 (GRIND): Add `| (hmiGrnd = 0)`. Inside: `hmiGrnd = 1` then `hmiState = 2` BEFORE `SB 1; JS #GRIND`. After `JS #GRIND` returns: `hmiState = 1` then `JP #WtAtRt`.
    2. @IN[41]=0 (SETUP): Add `| (hmiSetp = 0)`. Inside: `hmiSetp = 1` then `hmiState = 3` BEFORE `JS #SETUP`. After `JS #SETUP` returns: `hmiState = 1` then `JP #WtAtRt`.
    3. @IN[44]=0 (NEWSESS): Add `| (hmiNewS = 0)`. Inside: `hmiNewS = 1` BEFORE `JS #NEWSESS`. (hmiState stays IDLE=1 during new session — it's a parameter reset, not a motion state.)
    4. @IN[33]=0 (MOREGRI): Add `| (hmiMore = 0)`. Inside: `hmiMore = 1` as first line.
    5. @IN[36]=0 (LESSGRI): Add `| (hmiLess = 0)`. Inside: `hmiLess = 1` as first line.

    **#SULOOP block (3 IF blocks — NOT 4, because exit-setup @IN[32] gets NO HMI variable per CONTEXT.md):**
    1. @IN[23]=0 (JOG): Add `| (hmiJog = 0)`. Inside: `hmiJog = 1` as first line.
    2. @IN[25]=0 (HOME): Add `| (hmiHome = 0)`. Inside: `hmiHome = 1` as first line. Then `hmiState = 4` BEFORE `JS #HOME`. After `JS #HOME` returns: `hmiState = 3` (back to SETUP, since we're inside SULOOP).
    3. @IN[26]=0 (VARCALC): Add `| (hmiCalc = 0)`. Inside: `hmiCalc = 1` as first line.

    **#HOME subroutine:**
    - At the VERY START of #HOME (before any SH/SP/BG commands): add `hmiState = 4`
    - At the END of #HOME (before EN): add `hmiState = 1` (caller overrides if needed — #SULOOP sets 3 after JS #HOME returns)
    NOTE: The hmiState=4 at start of #HOME and hmiState=1 at end covers the standalone call from #AUTO. The hmiState=3 set after JS #HOME in #SULOOP covers the setup-context call.

    **#SETREST subroutine:**
    Replace all `restPt[0]`, `restPt[1]`, `restPt[2]`, `restPt[3]` with `restPtA`, `restPtB`, `restPtC`, `restPtD` respectively. Update MG debug print lines to match.

    **#SETSTR subroutine:**
    Replace all `startPt[0]`, `startPt[1]`, `startPt[2]`, `startPt[3]` with `startPtA`, `startPtB`, `startPtC`, `startPtD`. Update MG debug print lines.

    **#GOREST subroutine:**
    Replace all `restPt[n]` indexed references with scalar names: `restPt[0]`->`restPtA`, `restPt[1]`->`restPtB`, `restPt[2]`->`restPtC`, `restPt[3]`->`restPtD`. Update PA commands and MG debug lines.

    **#GOSTR subroutine:**
    Same pattern as #GOREST but for startPt: `startPt[0]`->`startPtA`, `startPt[1]`->`startPtB`, `startPt[2]`->`startPtC`, `startPt[3]`->`startPtD`.

    **#MOREGRI subroutine:**
    - Add `hmiMore = 1` reset (already handled by #WtAtRt OR block)
    - Replace `startPt[3]` with `startPtD`

    **#LESSGRI subroutine:**
    - Add `hmiLess = 1` reset (already handled by #WtAtRt OR block)
    - Replace `startPt[3]` with `startPtD`

    **IMPORTANT CONSTRAINTS:**
    - Do NOT add BV (variable save to flash) anywhere except inside #SETREST and #SETSTR where it already exists
    - Do NOT add XQ calls — all triggers through the HMI variable one-shot pattern only
    - Do NOT modify header comments or program structure
    - Do NOT touch computed sequence arrays (deltaD[], deltaC[], bComp[]) — they remain indexed
    - The exit-setup button (@IN[32]) does NOT get an HMI variable (that's Phase 13 SETP-08)
  </action>
  <verify>
    <automated>python -c "
import re
with open('4 Axis Stainless grind.dmc', 'r') as f: content = f.read()

# Verify OR conditions in #WtAtRt
assert 'hmiGrnd = 0' in content, 'Missing hmiGrnd OR condition'
assert 'hmiSetp = 0' in content, 'Missing hmiSetp OR condition'
assert 'hmiNewS = 0' in content, 'Missing hmiNewS OR condition'
assert 'hmiMore = 0' in content, 'Missing hmiMore OR condition'
assert 'hmiLess = 0' in content, 'Missing hmiLess OR condition'

# Verify OR conditions in #SULOOP
assert 'hmiJog = 0' in content, 'Missing hmiJog OR condition'
assert 'hmiHome = 0' in content, 'Missing hmiHome OR condition'
assert 'hmiCalc = 0' in content, 'Missing hmiCalc OR condition'

# Verify resets exist (hmiVar = 1 pattern)
for var in ['hmiGrnd', 'hmiSetp', 'hmiMore', 'hmiLess', 'hmiNewS', 'hmiHome', 'hmiJog', 'hmiCalc']:
    assert f'{var} = 1' in content or f'{var}=1' in content, f'Missing reset for {var}'

# Verify hmiState transitions
assert 'hmiState = 2' in content, 'Missing GRINDING state'
assert 'hmiState = 3' in content, 'Missing SETUP state'
assert 'hmiState = 4' in content, 'Missing HOMING state'

# Verify no old array references remain (except in comments and computed arrays)
# Check functional code only — startPt[n] and restPt[n] should be gone
lines = content.split('\n')
for i, line in enumerate(lines):
    stripped = line.split(\"'\")[0]  # remove comments
    if re.search(r'startPt\[\d\]', stripped) or re.search(r'restPt\[\d\]', stripped):
        raise AssertionError(f'Line {i+1} still has array index: {line.strip()}')

print('All DMC modifications verified')
"
    </automated>
  </verify>
  <done>
    - All 5 #WtAtRt IF blocks have OR conditions with HMI variables
    - All 3 #SULOOP IF blocks (jog, home, varcalc) have OR conditions
    - Every HMI variable is reset to 1 as first line inside its block
    - hmiState set to 2 (GRINDING), 3 (SETUP), 4 (HOMING) at appropriate boundaries
    - hmiState returns to 1 (IDLE) after grind, setup, and home complete
    - All startPt[n]/restPt[n] array references replaced with scalar names
    - No BV calls added outside existing teach operations
    - Computed sequence arrays (deltaD[], deltaC[], bComp[]) untouched
  </done>
</task>

</tasks>

<verification>
- Python verification script (in Task 2 verify) confirms all OR conditions, resets, state transitions, and scalar conversions
- No `startPt[` or `restPt[` indexed references remain in functional code
- No `DM startPt[` or `DM restPt[` or `DM hmiBtn[` declarations remain
- `DM deltaD[`, `DM deltaC[`, `DM bComp[` still present (computed arrays preserved)
- Full hardware validation deferred to controller upload (manual, post-phase)
</verification>

<success_criteria>
- DMC file has all 8 HMI trigger variables declared with default=1
- DMC file has hmiState declared with default=1
- All 8 position scalars declared (startPtA-D, restPtA-D)
- 5 OR conditions in #WtAtRt, 3 in #SULOOP
- hmiState set at every state boundary
- All subroutines use scalar variable names, no array indexing for position points
- Verification script passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-dmc-foundation/09-02-SUMMARY.md`
</output>
