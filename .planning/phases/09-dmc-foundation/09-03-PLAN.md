---
phase: 09-dmc-foundation
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/dmccodegui/screens/rest.py
  - src/dmccodegui/screens/start.py
  - src/dmccodegui/screens/axisDSetup.py
  - src/dmccodegui/screens/parameters_setup.py
autonomous: true
requirements: [DMC-06]

must_haves:
  truths:
    - "rest.py reads/writes rest point positions using individual MG queries and semicolon-joined assignments via dmc_vars constants, not upload_array/download_array"
    - "start.py reads/writes start point positions using individual MG queries and semicolon-joined assignments via dmc_vars constants, not upload_array/download_array"
    - "axisDSetup.py reads/writes D-axis angle positions using individual MG queries and semicolon-joined assignments via dmc_vars constants, not upload_array/download_array"
    - "parameters_setup.py reads/writes rest point positions using individual MG queries and semicolon-joined assignments via dmc_vars constants, not upload_array/download_array"
    - "No Python screen file contains raw 'RestPnt' or 'StartPnt' string literals in functional code"
  artifacts:
    - path: "src/dmccodegui/screens/rest.py"
      provides: "Rest point screen using dmc_vars constants"
      contains: "from dmccodegui.hmi.dmc_vars import"
    - path: "src/dmccodegui/screens/start.py"
      provides: "Start point screen using dmc_vars constants"
      contains: "from dmccodegui.hmi.dmc_vars import"
    - path: "src/dmccodegui/screens/axisDSetup.py"
      provides: "D-axis setup screen using dmc_vars constants"
      contains: "from dmccodegui.hmi.dmc_vars import"
    - path: "src/dmccodegui/screens/parameters_setup.py"
      provides: "Parameters setup screen using dmc_vars constants"
      contains: "from dmccodegui.hmi.dmc_vars import"
  key_links:
    - from: "src/dmccodegui/screens/rest.py"
      to: "src/dmccodegui/hmi/dmc_vars.py"
      via: "import RESTPT_BY_AXIS for individual variable reads/writes"
      pattern: "from dmccodegui.hmi.dmc_vars import.*RESTPT"
    - from: "src/dmccodegui/screens/start.py"
      to: "src/dmccodegui/hmi/dmc_vars.py"
      via: "import STARTPT_BY_AXIS for individual variable reads/writes"
      pattern: "from dmccodegui.hmi.dmc_vars import.*STARTPT"
    - from: "src/dmccodegui/screens/axisDSetup.py"
      to: "src/dmccodegui/hmi/dmc_vars.py"
      via: "import RESTPT_BY_AXIS for D-axis angle reads/writes"
      pattern: "from dmccodegui.hmi.dmc_vars import.*RESTPT"
    - from: "src/dmccodegui/screens/parameters_setup.py"
      to: "src/dmccodegui/hmi/dmc_vars.py"
      via: "import RESTPT_BY_AXIS for rest point reads/writes"
      pattern: "from dmccodegui.hmi.dmc_vars import.*RESTPT"
---

<objective>
Migrate all 4 Python screen files from stale RestPnt/StartPnt array operations to individual variable reads/writes using dmc_vars.py constants.

Purpose: After Plan 02 removes the DM startPt[4]/restPt[4] arrays from the DMC program, these screen files' upload_array/download_array calls would fail at runtime. This migration switches them to the individual MG-query pattern that axes_setup.py already uses, importing variable names from the new constants module.

Output: 4 migrated screen files with no stale array references
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dmc-foundation/09-CONTEXT.md
@.planning/phases/09-dmc-foundation/09-RESEARCH.md

@src/dmccodegui/hmi/dmc_vars.py
@src/dmccodegui/screens/axes_setup.py
@src/dmccodegui/screens/rest.py
@src/dmccodegui/screens/start.py
@src/dmccodegui/screens/axisDSetup.py
@src/dmccodegui/screens/parameters_setup.py
</context>

<interfaces>
<!-- From Plan 01 output — dmc_vars.py constants the executor needs -->
From src/dmccodegui/hmi/dmc_vars.py:
```python
RESTPT_A  = "restPtA"
RESTPT_B  = "restPtB"
RESTPT_C  = "restPtC"
RESTPT_D  = "restPtD"
STARTPT_A = "startPtA"
STARTPT_B = "startPtB"
STARTPT_C = "startPtC"
STARTPT_D = "startPtD"
RESTPT_VARS  = [RESTPT_A, RESTPT_B, RESTPT_C, RESTPT_D]
STARTPT_VARS = [STARTPT_A, STARTPT_B, STARTPT_C, STARTPT_D]
RESTPT_BY_AXIS  = {"A": RESTPT_A, "B": RESTPT_B, "C": RESTPT_C, "D": RESTPT_D}
STARTPT_BY_AXIS = {"A": STARTPT_A, "B": STARTPT_B, "C": STARTPT_C, "D": STARTPT_D}
```

From src/dmccodegui/screens/axes_setup.py (REFERENCE PATTERN — already uses individual vars):
```python
# Write pattern (line ~291):
parts = [f"restPt{axis}={vals[axis]}" for axis in axis_list]
write_cmd = ";".join(parts)
ctrl.cmd(write_cmd)
ctrl.cmd("BV")

# Read pattern (line ~457):
raw = ctrl.cmd(f"MG restPt{axis}").strip()
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate rest.py and axisDSetup.py from RestPnt array to individual scalar variables</name>
  <files>src/dmccodegui/screens/rest.py, src/dmccodegui/screens/axisDSetup.py</files>
  <action>
    Both files use the same pattern: `upload_array("RestPnt", 0, 2)` to read and `download_array("RestPnt", 0, vals)` to write. Migrate both to the individual variable pattern using dmc_vars constants.

    **For rest.py:**
    1. Add import: `from dmccodegui.hmi.dmc_vars import RESTPT_A, RESTPT_B, RESTPT_C, RESTPT_BY_AXIS`
    2. Replace `upload_array("RestPnt", 0, 2)` read pattern with individual MG queries:
       ```python
       vals = []
       for axis in ["A", "B", "C"]:
           raw = self.controller.cmd(f"MG {RESTPT_BY_AXIS[axis]}").strip()
           vals.append(float(raw))
       ```
       Note: rest.py only uses 3 axes (A, B, C) — not D. Keep this behavior.
    3. Replace `download_array("RestPnt", 0, self.rest_vals)` write pattern with semicolon-joined assignment:
       ```python
       axes = ["A", "B", "C"]
       parts = [f"{RESTPT_BY_AXIS[axes[i]]}={self.rest_vals[i]}" for i in range(len(axes))]
       self.controller.cmd(";".join(parts))
       ```
    4. Update all docstrings and comments referencing "RestPnt array" to say "restPt individual variables". Remove references to array indices.
    5. The BV call after writes should remain if it already exists.

    **For axisDSetup.py:**
    1. Add import: `from dmccodegui.hmi.dmc_vars import RESTPT_A, RESTPT_B, RESTPT_C, RESTPT_BY_AXIS`
    2. Same read migration as rest.py — axisDSetup uses RestPnt[0..2] for A, B, C angles.
    3. Same write migration as rest.py.
    4. Update docstrings — this file has extensive comments about "RestPnt array" that must be updated. The module-level docstring references RestPnt heavily; update to reflect individual variables.

    **Reference:** axes_setup.py (lines 291, 344, 457, 462) already uses the correct individual-variable pattern. Follow that pattern exactly.

    **IMPORTANT:** The `MG varName` response from gclib typically returns a string like " 1234.0000\r\n" — use `.strip()` and `float()` to parse. The existing axes_setup.py pattern handles this correctly.
  </action>
  <verify>
    <automated>python -c "
import ast, sys
for fn in ['src/dmccodegui/screens/rest.py', 'src/dmccodegui/screens/axisDSetup.py']:
    with open(fn) as f: src = f.read()
    ast.parse(src)  # syntax valid
    assert 'upload_array' not in src.split(\"'\")[0] or 'RestPnt' not in src, f'{fn} still uses upload_array RestPnt'
    # Check no functional RestPnt references (allow in comments only)
    for i, line in enumerate(src.split('\n'), 1):
        code = line.split('#')[0]  # strip inline comments
        if '\"RestPnt\"' in code or \"'RestPnt'\" in code:
            print(f'ERROR: {fn}:{i} has RestPnt in code: {line.strip()}', file=sys.stderr)
            sys.exit(1)
    assert 'from dmccodegui.hmi.dmc_vars import' in src, f'{fn} missing dmc_vars import'
print('rest.py and axisDSetup.py migration verified')
"
    </automated>
  </verify>
  <done>
    - rest.py reads rest points via individual MG queries using RESTPT_BY_AXIS constants
    - rest.py writes rest points via semicolon-joined assignments using RESTPT_BY_AXIS constants
    - axisDSetup.py reads/writes using same pattern
    - No "RestPnt" string literal in functional code of either file
    - Both files import from dmccodegui.hmi.dmc_vars
    - Both files are valid Python (ast.parse succeeds)
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate start.py and parameters_setup.py, then verify no stale strings remain</name>
  <files>src/dmccodegui/screens/start.py, src/dmccodegui/screens/parameters_setup.py, tests/test_dmc_vars.py</files>
  <action>
    **For start.py:**
    1. Add import: `from dmccodegui.hmi.dmc_vars import STARTPT_A, STARTPT_B, STARTPT_C, STARTPT_D, STARTPT_BY_AXIS`
    2. Replace `upload_array("StartPnt", 0, 3)` read pattern with individual MG queries for all 4 axes (A, B, C, D — start.py uses all 4 unlike rest.py which uses 3):
       ```python
       vals = []
       for axis in ["A", "B", "C", "D"]:
           raw = self.controller.cmd(f"MG {STARTPT_BY_AXIS[axis]}").strip()
           vals.append(float(raw))
       ```
    3. Replace `download_array("StartPnt", 0, self.start_vals)` write pattern with semicolon-joined assignment.
    4. Update docstrings and comments. Note: start.py has a "D-axis (index 3)" concept mapped to the 4th value — update comments to reflect startPtD instead of StartPnt[3].
    5. Keep BV call after writes if it exists.

    **For parameters_setup.py:**
    1. Add import: `from dmccodegui.hmi.dmc_vars import RESTPT_BY_AXIS`
    2. Replace the 3 occurrences of `upload_array("RestPnt", ...)` and `download_array("RestPnt", ...)` with individual variable reads/writes (A, B, C only — same as rest.py).
    3. Update error messages and print statements that reference "RestPnt".

    **After both migrations, update the xfail test:**
    In tests/test_dmc_vars.py, find the test marked `@pytest.mark.xfail(reason="Screen migration in plan 09-03")` (the "no stale strings" test) and REMOVE the xfail marker so it runs as a normal test. It should now pass since all 4 screen files have been migrated.

    **Final sweep:** Search all files in src/dmccodegui/screens/ for any remaining `"RestPnt"` or `"StartPnt"` string literals in functional (non-comment, non-docstring) code. The only acceptable references are in docstrings/comments explaining the migration history, or in serration_knife.py which uses these names in DMC pseudocode documentation strings (not functional calls).
  </action>
  <verify>
    <automated>pytest tests/test_dmc_vars.py -x -v && python -c "
import ast, sys
for fn in ['src/dmccodegui/screens/start.py', 'src/dmccodegui/screens/parameters_setup.py']:
    with open(fn) as f: src = f.read()
    ast.parse(src)
    for i, line in enumerate(src.split('\n'), 1):
        code = line.split('#')[0]
        if '\"StartPnt\"' in code or \"'StartPnt'\" in code or '\"RestPnt\"' in code or \"'RestPnt'\" in code:
            print(f'ERROR: {fn}:{i} has stale name in code: {line.strip()}', file=sys.stderr)
            sys.exit(1)
    assert 'from dmccodegui.hmi.dmc_vars import' in src, f'{fn} missing dmc_vars import'
print('start.py and parameters_setup.py migration verified')
"
    </automated>
  </verify>
  <done>
    - start.py uses STARTPT_BY_AXIS constants for all 4-axis reads/writes
    - parameters_setup.py uses RESTPT_BY_AXIS constants for rest point reads/writes
    - The "no stale strings" test in test_dmc_vars.py passes (xfail removed)
    - No screen file contains functional "RestPnt" or "StartPnt" string literals
    - Full test suite passes: `pytest tests/ -x`
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_dmc_vars.py -x -v` — ALL tests pass including the "no stale strings" test (xfail removed)
- `pytest tests/ -x` — full test suite green
- No `upload_array("RestPnt"` or `upload_array("StartPnt"` in any screen file
- No `download_array("RestPnt"` or `download_array("StartPnt"` in any screen file
- All 4 migrated files have `from dmccodegui.hmi.dmc_vars import` at top
- `python -c "from dmccodegui.screens.rest import RestScreen"` succeeds (import chain works)
</verification>

<success_criteria>
- All 4 screen files (rest.py, start.py, axisDSetup.py, parameters_setup.py) migrated to individual variable pattern
- All screen files import constants from dmccodegui.hmi.dmc_vars
- Zero raw "RestPnt" or "StartPnt" string literals in functional Python code
- The stale-string detection test passes (not xfail)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-dmc-foundation/09-03-SUMMARY.md`
</output>
