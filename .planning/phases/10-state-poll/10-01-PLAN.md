---
phase: 10-state-poll
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "4 Axis Stainless grind.dmc"
  - src/dmccodegui/hmi/dmc_vars.py
  - src/dmccodegui/app_state.py
  - tests/test_dmc_vars.py
  - tests/test_app_state.py
autonomous: true
requirements: [POLL-01, POLL-04]

must_haves:
  truths:
    - "dmc_vars.py exports CT_SES_KNI and CT_STN_KNI constants matching 8-char DMC names"
    - "MachineState has session_knife_count and stone_knife_count integer fields defaulting to 0"
    - "MachineState.cycle_running is a @property derived from dmc_state == STATE_GRINDING, not a stored bool"
    - "DMC program declares ctStnKni in #PARAMS, increments it alongside ctSesKni at grind completion, and resets it in #NEWSESS"
    - "DMC program has Thread 2 (#THRD2) that manages hmiState transitions"
  artifacts:
    - path: "src/dmccodegui/hmi/dmc_vars.py"
      provides: "CT_SES_KNI, CT_STN_KNI constants"
      contains: "CT_SES_KNI"
    - path: "src/dmccodegui/app_state.py"
      provides: "session_knife_count, stone_knife_count fields and cycle_running property"
      contains: "session_knife_count"
    - path: "4 Axis Stainless grind.dmc"
      provides: "Thread 2 label and ctStnKni variable"
      contains: "#THRD2"
    - path: "tests/test_dmc_vars.py"
      provides: "Tests for knife count constants"
      contains: "CT_SES_KNI"
    - path: "tests/test_app_state.py"
      provides: "Tests for knife count fields and cycle_running property"
      contains: "test_cycle_running_derived"
  key_links:
    - from: "src/dmccodegui/hmi/dmc_vars.py"
      to: "src/dmccodegui/app_state.py"
      via: "STATE_GRINDING import for cycle_running property"
      pattern: "from.*dmc_vars.*import.*STATE_GRINDING"
---

<objective>
Extend the DMC program, dmc_vars.py, and MachineState with the foundation needed for Phase 10 polling.

Purpose: The poller (Plan 02) and RunScreen (Plan 03) both need knife count constants, MachineState fields, and a derived cycle_running property. The DMC program needs Thread 2 for hmiState management and the ctStnKni variable for stone knife counting. All of this must exist before the poller or UI can reference it.

Output: Extended dmc_vars.py with knife count constants, extended MachineState with knife count fields and derived cycle_running, updated DMC program with Thread 2 and ctStnKni, passing tests.
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-state-poll/10-CONTEXT.md
@.planning/phases/10-state-poll/10-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/dmccodegui/hmi/dmc_vars.py (current exports to extend):
```python
HMI_STATE_VAR: str = "hmiState"
STATE_UNINITIALIZED: int = 0
STATE_IDLE: int = 1
STATE_GRINDING: int = 2
STATE_SETUP: int = 3
STATE_HOMING: int = 4
HMI_TRIGGER_DEFAULT: int = 1
HMI_TRIGGER_FIRE: int = 0
```

From src/dmccodegui/app_state.py (MachineState fields to extend):
```python
@dataclass
class MachineState:
    # ... existing fields ...
    cycle_running: bool = False        # REPLACE with @property
    cycle_completion_pct: float = 0.0
    dmc_state: int = 0
    # ADD: session_knife_count: int = 0
    # ADD: stone_knife_count: int = 0
```

From tests/test_machine_state_cycle.py (existing test to preserve):
```python
def test_machine_state_has_cycle_running():
    s = MachineState()
    assert hasattr(s, 'cycle_running'), "Missing cycle_running field"
    assert s.cycle_running == False
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend dmc_vars.py and MachineState with knife count support</name>
  <files>src/dmccodegui/hmi/dmc_vars.py, src/dmccodegui/app_state.py, tests/test_dmc_vars.py, tests/test_app_state.py</files>
  <behavior>
    - test_ct_ses_kni_constant: dmc_vars.CT_SES_KNI == "ctSesKni"
    - test_ct_stn_kni_constant: dmc_vars.CT_STN_KNI == "ctStnKni"
    - test_ct_ses_kni_length: len(CT_SES_KNI) <= 8
    - test_ct_stn_kni_length: len(CT_STN_KNI) <= 8
    - test_knife_count_fields_exist: MachineState() has session_knife_count == 0 and stone_knife_count == 0
    - test_cycle_running_derived_from_dmc_state: MachineState(dmc_state=2).cycle_running == True; MachineState(dmc_state=0).cycle_running == False; MachineState(dmc_state=1).cycle_running == False
    - test_machine_state_has_cycle_running (existing): s = MachineState(); s.cycle_running == False -- must still pass
    - test_cycle_running_not_assignable: assigning s.cycle_running = True raises AttributeError (it is a @property, not a stored field)
  </behavior>
  <action>
    1. Write tests FIRST in tests/test_dmc_vars.py: add a TestKnifeCountConstants class with tests for CT_SES_KNI and CT_STN_KNI constants (value, 8-char limit).
    2. Write tests FIRST in tests/test_app_state.py: add test_knife_count_fields, test_cycle_running_derived_from_dmc_state, test_cycle_running_not_assignable.
    3. Run tests -- they MUST fail (RED).
    4. Add to dmc_vars.py: `CT_SES_KNI: str = "ctSesKni"` and `CT_STN_KNI: str = "ctStnKni"` in a new "Knife count variable names" section after position variables.
    5. Modify app_state.py:
       - Add `from .hmi.dmc_vars import STATE_GRINDING` at the top
       - Add `session_knife_count: int = 0` and `stone_knife_count: int = 0` fields after dmc_state
       - Remove the stored `cycle_running: bool = False` dataclass field
       - Add a `@property` for `cycle_running` that returns `self.dmc_state == STATE_GRINDING`
       - IMPORTANT: The @property must go AFTER the dataclass field definitions but the stored field `cycle_running` must be removed. Use a property defined in the class body after the dataclass fields.
    6. Run tests -- they MUST pass (GREEN).

    CRITICAL: The existing test_machine_state_has_cycle_running checks `s.cycle_running == False`. With dmc_state=0 (default), the property returns `0 == STATE_GRINDING(2)` = False. This test passes without modification.

    CRITICAL: The existing RunScreen has `cycle_running = BooleanProperty(False)` which is a Kivy property on the Screen, separate from MachineState.cycle_running. Do NOT touch RunScreen in this task.

    CRITICAL: Since cycle_running becomes a @property, it can no longer be passed as a keyword argument to MachineState(). Check that no test does `MachineState(cycle_running=True)`. If any do, update them to set dmc_state instead.
  </action>
  <verify>
    <automated>cd C:/Users/danii/OneDrive/Desktop/PythonDMCGUI && python -m pytest tests/test_dmc_vars.py tests/test_app_state.py tests/test_machine_state_cycle.py -x -v</automated>
  </verify>
  <done>
    - CT_SES_KNI and CT_STN_KNI constants exist in dmc_vars.py with correct values and 8-char names
    - MachineState has session_knife_count and stone_knife_count int fields defaulting to 0
    - MachineState.cycle_running is a @property returning dmc_state == STATE_GRINDING
    - All existing tests pass, new tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Thread 2 and ctStnKni to DMC program</name>
  <files>4 Axis Stainless grind.dmc</files>
  <action>
    Modify "4 Axis Stainless grind.dmc" to add:

    1. In #PARAMS section (after ctSesKni declaration):
       - Add `ctStnKni = 0` (stone knife count, resets on new session)
       - Add `lastSt = 0` (Thread 2 state tracking variable)

    2. In #AUTO section (after `JS #HOME` and before `WT 500`):
       - Add `XQ #THRD2, 1` to start Thread 2

    3. At the end of the grind completion block (after `JS #GOREST` on line ~152, before `ST ABCD`):
       - Add `ctSesKni = ctSesKni + 1` (session knife count increment)
       - Add `ctStnKni = ctStnKni + 1` (stone knife count increment)
       - NOTE: The CONTEXT.md says Thread 2 handles counting via state transitions, but the RESEARCH.md notes inline at grind completion is simpler and within Claude's Discretion. Use INLINE incrementing at grind completion (#DONE block) for simplicity and reliability. Thread 2 will observe state for hmiState management only.

    4. In #NEWSESS subroutine (after `ctSesKni = 0` on line ~327):
       - Add `ctStnKni = 0` (reset stone count on new session)

    5. Add new #THRD2 label at the end of the file (before EN or after last subroutine):
       ```
       ' ============================================================
       '  THREAD 2 - hmiState observer
       '  Runs on Galil thread 2 for continuous state monitoring
       '  Started by XQ #THRD2,1 in #AUTO
       ' ============================================================
       #THRD2
       #T2LOOP
       WT 100
       ' --- Thread 2 observes hmiState for future requirements ---
       ' Currently a passive observer; state transitions are set
       ' by the main thread subroutines.
       JP #T2LOOP
       EN
       ```

    IMPORTANT: Preserve all existing code. Only add new lines. Use DMC comment style (single quote prefix). Keep variable names within 8-char limit.

    IMPORTANT: hmiState transitions are already set by the main thread subroutines (e.g., #GRIND sets hmiState=2, #HOME sets hmiState=4, etc. from Phase 9). Thread 2 is a placeholder for future background monitoring needs.
  </action>
  <verify>
    <automated>cd C:/Users/danii/OneDrive/Desktop/PythonDMCGUI && grep -c "ctStnKni" "4 Axis Stainless grind.dmc" && grep -c "#THRD2" "4 Axis Stainless grind.dmc" && grep -c "lastSt" "4 Axis Stainless grind.dmc"</automated>
  </verify>
  <done>
    - ctStnKni declared in #PARAMS with value 0
    - ctStnKni incremented alongside ctSesKni at grind completion
    - ctStnKni reset to 0 in #NEWSESS
    - lastSt declared in #PARAMS
    - #THRD2 label exists, started by XQ #THRD2,1 in #AUTO
    - Thread 2 runs a WT 100 loop (10 Hz on controller)
  </done>
</task>

</tasks>

<verification>
All existing tests pass: `python -m pytest tests/ -x`
New dmc_vars constants and MachineState fields are importable and correct.
DMC program has ctStnKni and Thread 2.
</verification>

<success_criteria>
1. dmc_vars.py has CT_SES_KNI = "ctSesKni" and CT_STN_KNI = "ctStnKni"
2. MachineState has session_knife_count and stone_knife_count fields
3. MachineState.cycle_running is derived from dmc_state, not stored
4. DMC program declares ctStnKni, increments at grind completion, resets in #NEWSESS
5. DMC program has #THRD2 label running on thread 2
6. All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/10-state-poll/10-01-SUMMARY.md`
</output>
