---
phase: 13-setup-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "4 Axis Stainless grind.dmc"
  - src/dmccodegui/hmi/dmc_vars.py
  - tests/test_dmc_vars.py
autonomous: true
requirements: [SETP-08]

must_haves:
  truths:
    - "DMC #PARAMS declares hmiGoRs, hmiGoSt, hmiExSt with default=1"
    - "DMC #SULOOP has OR conditions for hmiGoRs, hmiGoSt, and hmiExSt alongside existing physical button checks"
    - "Python constants HMI_GO_REST, HMI_GO_START, HMI_EXIT_SETUP exist in dmc_vars.py with correct 7-char names"
  artifacts:
    - path: "4 Axis Stainless grind.dmc"
      provides: "Three new HMI trigger variables and OR conditions in #SULOOP"
      contains: "hmiGoRs"
    - path: src/dmccodegui/hmi/dmc_vars.py
      provides: "Three new HMI trigger constants"
      exports: ["HMI_GO_REST", "HMI_GO_START", "HMI_EXIT_SETUP"]
    - path: tests/test_dmc_vars.py
      provides: "Tests for new constants"
  key_links:
    - from: src/dmccodegui/hmi/dmc_vars.py
      to: "4 Axis Stainless grind.dmc"
      via: "constant string values match DMC variable names"
      pattern: "hmiGoRs|hmiGoSt|hmiExSt"
---

<objective>
Add three new HMI trigger variables to the DMC program (#PARAMS and #SULOOP) and corresponding Python constants to dmc_vars.py, with tests.

Purpose: Foundation for Python-side setup wiring in Plans 02 and 03 -- without these DMC variables and Python constants, no setup screen can fire Go To Rest, Go To Start, or Exit Setup.

Output: Modified DMC program, extended dmc_vars.py, extended test_dmc_vars.py
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-setup-loop/13-CONTEXT.md
@.planning/phases/13-setup-loop/13-RESEARCH.md

<interfaces>
<!-- From src/dmccodegui/hmi/dmc_vars.py — existing constants pattern -->
HMI_GRND: str = "hmiGrnd"
HMI_SETP: str = "hmiSetp"
HMI_MORE: str = "hmiMore"
HMI_LESS: str = "hmiLess"
HMI_NEWS: str = "hmiNewS"
HMI_HOME: str = "hmiHome"
HMI_JOG: str  = "hmiJog"
HMI_CALC: str = "hmiCalc"

ALL_HMI_TRIGGERS: list[str] = [HMI_GRND, HMI_SETP, HMI_MORE, HMI_LESS, HMI_NEWS, HMI_HOME, HMI_JOG, HMI_CALC]

HMI_TRIGGER_DEFAULT: int = 1
HMI_TRIGGER_FIRE: int = 0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add three HMI trigger variables to DMC program</name>
  <files>4 Axis Stainless grind.dmc</files>
  <action>
1. In `#PARAMS` block, add three new variable declarations after the existing HMI trigger block:
   ```
   hmiGoRs = 1;   ' HMI trigger: go to rest position (in setup)
   hmiGoSt = 1;   ' HMI trigger: go to start position (in setup)
   hmiExSt = 1;   ' HMI trigger: exit setup, return to #MAIN
   ```

2. In `#SULOOP`, add two new IF blocks for Go To Rest and Go To Start BEFORE the existing exit-setup check. Place them after the existing hmiCalc/varcalc block:
   ```
   ' --- HMI: GO TO REST ---
   IF (hmiGoRs = 0)
     hmiGoRs = 1
     MG "HMI: GOING TO REST"
     JS #GOREST
     JS #W_REL
   ENDIF

   ' --- HMI: GO TO START ---
   IF (hmiGoSt = 0)
     hmiGoSt = 1
     MG "HMI: GOING TO START"
     JS #GOSTR
     JS #W_REL
   ENDIF
   ```

3. Replace the existing `IF (@IN[32] = 0)` exit-setup block in `#SULOOP` with a combined OR condition:
   ```
   ' --- BUTTON 32 OR HMI EXIT SETUP ---
   IF (@IN[32] = 0) | (hmiExSt = 0)
     hmiExSt = 1
     MG "RETURNING TO MAIN MENU"
     JS #W_REL
     EN
   ENDIF
   ```
   The `hmiExSt = 1` reset is the first line inside the block (one-shot pattern). The existing EN (return from #SETUP to #MAIN) stays.

CRITICAL: Do NOT add hmiGoRs/hmiGoSt to #WtAtRt — per discretion decision in RESEARCH.md, these only go in #SULOOP to avoid state-machine ambiguity.
  </action>
  <verify>
    <automated>grep -c "hmiGoRs\|hmiGoSt\|hmiExSt" "4 Axis Stainless grind.dmc"</automated>
  </verify>
  <done>DMC program has hmiGoRs, hmiGoSt, hmiExSt declared in #PARAMS with default=1; #SULOOP has IF blocks for all three with one-shot reset pattern; @IN[32] exit check uses combined OR with hmiExSt</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Python constants and tests for new DMC variables</name>
  <files>src/dmccodegui/hmi/dmc_vars.py, tests/test_dmc_vars.py</files>
  <behavior>
    - Test: HMI_GO_REST equals "hmiGoRs" (7 chars, within 8-char limit)
    - Test: HMI_GO_START equals "hmiGoSt" (7 chars, within 8-char limit)
    - Test: HMI_EXIT_SETUP equals "hmiExSt" (7 chars, within 8-char limit)
    - Test: All three are in ALL_HMI_TRIGGERS list
    - Test: All names in ALL_HMI_TRIGGERS are <= 8 characters
  </behavior>
  <action>
1. In `dmc_vars.py`, add three new constants after the existing HMI trigger block (before ALL_HMI_TRIGGERS):
   ```python
   HMI_GO_REST: str   = "hmiGoRs"   # Trigger: go to rest position (in setup)
   HMI_GO_START: str  = "hmiGoSt"   # Trigger: go to start position (in setup)
   HMI_EXIT_SETUP: str = "hmiExSt"  # Trigger: exit setup, return to #MAIN
   ```

2. Add all three to the `ALL_HMI_TRIGGERS` list.

3. In `tests/test_dmc_vars.py`, add tests:
   - `test_hmi_go_rest_value`: assert `HMI_GO_REST == "hmiGoRs"` and `len(HMI_GO_REST) <= 8`
   - `test_hmi_go_start_value`: assert `HMI_GO_START == "hmiGoSt"` and `len(HMI_GO_START) <= 8`
   - `test_hmi_exit_setup_value`: assert `HMI_EXIT_SETUP == "hmiExSt"` and `len(HMI_EXIT_SETUP) <= 8`
   - `test_new_triggers_in_all_list`: assert all three in `ALL_HMI_TRIGGERS`
   - `test_all_trigger_names_within_limit`: assert all names in `ALL_HMI_TRIGGERS` have `len(name) <= 8`
  </action>
  <verify>
    <automated>python -m pytest tests/test_dmc_vars.py -x -v</automated>
  </verify>
  <done>HMI_GO_REST, HMI_GO_START, HMI_EXIT_SETUP constants exist in dmc_vars.py with correct string values; all three appear in ALL_HMI_TRIGGERS; all tests pass including name-length validation</done>
</task>

</tasks>

<verification>
1. `grep -c "hmiGoRs\|hmiGoSt\|hmiExSt" "4 Axis Stainless grind.dmc"` returns >= 6 (declarations + IF blocks)
2. `python -m pytest tests/test_dmc_vars.py -x -v` passes all tests
3. `python -m pytest tests/ -x` full suite still green
</verification>

<success_criteria>
- DMC program declares hmiGoRs, hmiGoSt, hmiExSt in #PARAMS with default=1
- DMC #SULOOP has IF blocks for Go To Rest, Go To Start, and combined exit-setup OR
- Python constants match DMC names exactly, all <= 8 chars
- All existing + new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-setup-loop/13-01-SUMMARY.md`
</output>
