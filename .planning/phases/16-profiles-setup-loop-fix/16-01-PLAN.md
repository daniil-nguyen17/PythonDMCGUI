---
phase: 16-profiles-setup-loop-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dmccodegui/screens/profiles.py
  - tests/test_profiles.py
autonomous: true
requirements:
  - SETP-01
  - SETP-08

must_haves:
  truths:
    - "Navigating to ProfilesScreen while already in STATE_SETUP does not re-send hmiSetp=0"
    - "Navigating to ProfilesScreen while NOT in STATE_SETUP sends hmiSetp=0 to enter setup"
    - "Leaving ProfilesScreen sends hmiExSt=0 to exit setup mode"
    - "Leaving ProfilesScreen never sends hmiSetp=1 (old bug eliminated)"
  artifacts:
    - path: "src/dmccodegui/screens/profiles.py"
      provides: "Fixed on_pre_enter and on_leave methods"
      contains: "HMI_EXIT_SETUP"
    - path: "tests/test_profiles.py"
      provides: "4 new test functions for smart-enter and exit"
      contains: "test_enter_skips_fire_when_already_setup"
  key_links:
    - from: "src/dmccodegui/screens/profiles.py"
      to: "dmccodegui.hmi.dmc_vars"
      via: "import STATE_SETUP, HMI_EXIT_SETUP, HMI_TRIGGER_FIRE"
      pattern: "HMI_EXIT_SETUP.*HMI_TRIGGER_FIRE"
---

<objective>
Fix ProfilesScreen to correctly enter and exit controller setup mode using the same smart-enter/exit pattern as AxesSetupScreen and ParametersScreen.

Purpose: Two bugs exist — on_pre_enter fires hmiSetp=0 unconditionally (should skip when already in STATE_SETUP), and on_leave sends hmiSetp=1 (should send hmiExSt=0). Both bugs break controller state machine flow.
Output: Fixed profiles.py with 4 new passing tests confirming correct behavior.
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-profiles-setup-loop-fix/16-CONTEXT.md
@.planning/phases/16-profiles-setup-loop-fix/16-RESEARCH.md

@src/dmccodegui/screens/profiles.py
@tests/test_profiles.py

<interfaces>
<!-- Constants from dmc_vars.py needed by executor -->

From src/dmccodegui/hmi/dmc_vars.py:
```python
HMI_SETP = "hmiSetp"          # Enter setup trigger variable
HMI_EXIT_SETUP = "hmiExSt"    # Exit setup trigger variable
HMI_TRIGGER_FIRE = 0          # Value to fire a trigger
HMI_TRIGGER_DEFAULT = 1       # Resting value (DMC-managed)
STATE_SETUP = 3                # hmiState when in setup loop
STATE_IDLE = 1                 # hmiState when idle
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add smart-enter and exit tests to test_profiles.py</name>
  <files>tests/test_profiles.py</files>
  <behavior>
    - test_enter_skips_fire_when_already_setup: on_pre_enter with dmc_state=STATE_SETUP does NOT call controller.cmd with hmiSetp=0
    - test_enter_fires_when_not_in_setup: on_pre_enter with dmc_state=STATE_IDLE calls controller.cmd with hmiSetp=0
    - test_exit_fires_hmi_exit_setup: on_leave calls controller.cmd with hmiExSt=0
    - test_exit_does_not_send_hmiSetp: on_leave does NOT call controller.cmd with hmiSetp=1
  </behavior>
  <action>
Add 4 test functions and a _make_profiles_screen helper to the END of tests/test_profiles.py. The helper creates a ProfilesScreen via __new__ (no Kivy init), attaches a MagicMock controller with is_connected()=True and a MagicMock state with configurable dmc_state. Each test uses this helper and patches Clock to avoid Kivy scheduling.

Pattern reference: tests/test_parameters.py lines 479-567 use the same headless mock approach.

Helper signature:
```python
def _make_profiles_screen(connected: bool, dmc_state: int):
    screen = ProfilesScreen.__new__(ProfilesScreen)
    screen._unsubscribe = None
    screen._pending_parsed = None
    ctrl = MagicMock()
    ctrl.is_connected.return_value = connected
    ctrl.cmd.return_value = ""
    screen.controller = ctrl
    state = MagicMock()
    state.dmc_state = dmc_state
    screen.state = state
    return screen, ctrl
```

After writing tests, run them — they MUST FAIL (red phase) because profiles.py still has the bugs.
  </action>
  <verify>
    <automated>cd "C:/Users/danii/OneDrive/Desktop/PythonDMCGUI" && python -m pytest tests/test_profiles.py::test_enter_skips_fire_when_already_setup tests/test_profiles.py::test_exit_fires_hmi_exit_setup tests/test_profiles.py::test_exit_does_not_send_hmiSetp -x 2>&1 | tail -5</automated>
  </verify>
  <done>4 test functions exist in test_profiles.py. test_enter_skips_fire_when_already_setup and test_exit_fires_hmi_exit_setup and test_exit_does_not_send_hmiSetp FAIL (confirming bugs exist). test_enter_fires_when_not_in_setup PASSES (current code already fires unconditionally).</done>
</task>

<task type="auto">
  <name>Task 2: Fix on_pre_enter and on_leave in profiles.py</name>
  <files>src/dmccodegui/screens/profiles.py</files>
  <action>
Fix both methods in ProfilesScreen. Only profiles.py is modified — do NOT touch axes_setup.py or parameters.py.

**on_pre_enter fix (lines 440-454):**
Replace the unconditional hmiSetp=0 fire with a smart-enter guard:
1. Import STATE_SETUP alongside HMI_SETP and HMI_TRIGGER_FIRE from dmc_vars (move import to top of method, outside try block)
2. Check `already_in_setup = self.state is not None and self.state.dmc_state == STATE_SETUP`
3. Add connection guard: `self.controller is not None and self.controller.is_connected()`
4. Only fire `hmiSetp=0` if connected AND NOT already_in_setup
5. Wrap controller.cmd in try/except with pass (fire-and-forget)
6. Subscribe and _update_import_button run unconditionally AFTER the guard block

**on_leave fix (lines 456-468):**
Replace `hmiSetp=HMI_TRIGGER_DEFAULT` with `hmiExSt=HMI_TRIGGER_FIRE`:
1. Change import from `HMI_SETP, HMI_TRIGGER_DEFAULT` to `HMI_EXIT_SETUP, HMI_TRIGGER_FIRE`
2. Add connection guard: `self.controller is not None and self.controller.is_connected()`
3. Send `f"{HMI_EXIT_SETUP}={HMI_TRIGGER_FIRE}"` (hmiExSt=0)
4. No _SETUP_SCREENS sibling check — profiles always fires exit on leave (per CONTEXT.md decision)
5. Unsubscribe logic remains unchanged after the guard block

After fixing, all 4 new tests MUST pass (green phase). Then run full test suite.
  </action>
  <verify>
    <automated>cd "C:/Users/danii/OneDrive/Desktop/PythonDMCGUI" && python -m pytest tests/test_profiles.py -x -q && python -m pytest tests/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>All 4 new tests pass: test_enter_skips_fire_when_already_setup, test_enter_fires_when_not_in_setup, test_exit_fires_hmi_exit_setup, test_exit_does_not_send_hmiSetp. Full test suite green. profiles.py on_pre_enter uses smart-enter guard. profiles.py on_leave sends hmiExSt=0 instead of hmiSetp=1.</done>
</task>

</tasks>

<verification>
1. `pytest tests/test_profiles.py -x -q` — all tests pass including 4 new ones
2. `pytest tests/ -x -q` — full suite green (no regressions)
3. `grep -n "HMI_EXIT_SETUP" src/dmccodegui/screens/profiles.py` — confirms exit command uses correct constant
4. `grep -n "HMI_TRIGGER_DEFAULT" src/dmccodegui/screens/profiles.py` — should return NO matches (old bug pattern removed)
5. `grep -n "STATE_SETUP" src/dmccodegui/screens/profiles.py` — confirms smart-enter guard checks state
</verification>

<success_criteria>
- ProfilesScreen.on_pre_enter skips hmiSetp=0 when dmc_state is already STATE_SETUP
- ProfilesScreen.on_leave sends hmiExSt=0, never hmiSetp=1
- 4 new tests in test_profiles.py all pass
- Full test suite passes with no regressions
- No modifications to axes_setup.py or parameters.py
</success_criteria>

<output>
After completion, create `.planning/phases/16-profiles-setup-loop-fix/16-01-SUMMARY.md`
</output>
