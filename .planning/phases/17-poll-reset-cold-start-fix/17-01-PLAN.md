---
phase: 17-poll-reset-cold-start-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dmccodegui/hmi/poll.py
  - src/dmccodegui/app_state.py
  - tests/test_poll.py
  - tests/test_status_bar.py
autonomous: true
requirements:
  - POLL-03
  - UI-02

must_haves:
  truths:
    - "After disconnect_and_refresh(), _fail_count is 0 and _disconnect_start is None so reconnection starts clean"
    - "Before the first successful poll, the status bar shows OFFLINE (gray) instead of E-STOP (red)"
    - "After a real E-STOP (program stopped), status bar correctly shows E-STOP"
    - "RECOVER button is disabled at cold-start, disabled after E-STOP disconnect, enabled after reconnect with stopped program"
  artifacts:
    - path: "src/dmccodegui/hmi/poll.py"
      provides: "ControllerPoller.stop() with _fail_count and _disconnect_start reset"
      contains: "_fail_count = 0"
    - path: "src/dmccodegui/app_state.py"
      provides: "MachineState with program_running defaulting to True"
      contains: "program_running: bool = True"
    - path: "tests/test_poll.py"
      provides: "Poller stop reset test and updated default assertion"
      contains: "TestPollerStopResetsFailCount"
    - path: "tests/test_status_bar.py"
      provides: "Cold-start and RECOVER chain tests"
      contains: "test_recover_enabled_chain"
  key_links:
    - from: "src/dmccodegui/hmi/poll.py"
      to: "src/dmccodegui/app_state.py"
      via: "stop() resets poller internals; MachineState default drives StatusBar cold-start"
      pattern: "_fail_count = 0"
    - from: "src/dmccodegui/app_state.py"
      to: "src/dmccodegui/screens/status_bar.py"
      via: "program_running=True default routes StatusBar to OFFLINE on cold-start"
      pattern: "program_running: bool = True"
---

<objective>
Fix two bugs in the poll/status bar connection lifecycle: (1) reset _fail_count and _disconnect_start in ControllerPoller.stop() so reconnection cycles start clean, and (2) change MachineState.program_running default to True so cold-start shows OFFLINE instead of E-STOP.

Purpose: Closes two gap-closure items from v2.0 audit — ensures reconnect reliability (POLL-03) and honest cold-start status (UI-02).
Output: Two one-line production fixes, updated existing test assertion, three new test classes.
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-poll-reset-cold-start-fix/17-CONTEXT.md
@.planning/phases/17-poll-reset-cold-start-fix/17-RESEARCH.md

@src/dmccodegui/hmi/poll.py
@src/dmccodegui/app_state.py
@tests/test_poll.py
@tests/test_status_bar.py

<interfaces>
<!-- Key types and contracts. No codebase exploration needed. -->

From src/dmccodegui/hmi/poll.py (lines 68-72):
```python
def stop(self) -> None:
    """Stop polling. Safe to call even if not started."""
    if self._clock_event is not None:
        self._clock_event.cancel()
        self._clock_event = None
```
Fields to reset (set during __init__):
- self._fail_count (int, starts at 0)
- self._disconnect_start (Optional[float], starts at None)

From src/dmccodegui/app_state.py (line 49):
```python
program_running: bool = False  # Change to True
```

From tests/test_poll.py (lines 308-315):
```python
class TestProgramRunningDefault(unittest.TestCase):
    def test_program_running_default_false(self):
        from dmccodegui.app_state import MachineState
        state = MachineState()
        self.assertFalse(state.program_running)  # Change to assertTrue
```

From tests/test_poll.py helper:
```python
def _make_poller():  # Returns (poller, ctrl_mock, state)
```

From tests/test_status_bar.py helper:
```python
def _make_state(**kwargs):  # Returns SimpleNamespace with defaults
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Production fixes and existing test update</name>
  <files>src/dmccodegui/hmi/poll.py, src/dmccodegui/app_state.py, tests/test_poll.py</files>
  <behavior>
    - Test: ControllerPoller.stop() resets _fail_count to 0 (currently no test — add TestPollerStopResetsFailCount)
    - Test: ControllerPoller.stop() resets _disconnect_start to None (same test class)
    - Test: MachineState().program_running defaults to True (update existing TestProgramRunningDefault assertion from assertFalse to assertTrue)
  </behavior>
  <action>
    1. In tests/test_poll.py, add a new test class TestPollerStopResetsFailCount with one test method test_stop_resets_fail_count_and_disconnect_start:
       - Use _make_poller() to get (poller, ctrl, state)
       - Set poller._fail_count = 5 and poller._disconnect_start = time.monotonic()
       - Patch "dmccodegui.hmi.poll.Clock" and call poller.stop()
       - Assert poller._fail_count == 0 and poller._disconnect_start is None
       - Place the class after the existing poller lifecycle tests (around line 305, before TestProgramRunningDefault)

    2. Run the new test — it MUST FAIL (RED) because stop() does not reset yet.

    3. In src/dmccodegui/hmi/poll.py, add two lines after line 72 (after self._clock_event = None), OUTSIDE the if block (unconditional reset):
       ```python
       self._fail_count = 0
       self._disconnect_start = None
       ```

    4. Run the new test — it MUST PASS (GREEN).

    5. In src/dmccodegui/app_state.py line 49, change:
       `program_running: bool = False` to `program_running: bool = True`

    6. In tests/test_poll.py, update TestProgramRunningDefault.test_program_running_default_false:
       - Change self.assertFalse(state.program_running) to self.assertTrue(state.program_running)
       - Optionally rename the method to test_program_running_default_true for clarity

    7. Run full test suite to confirm no regressions: pytest tests/test_poll.py tests/test_status_bar.py -x -q
  </action>
  <verify>
    <automated>pytest tests/test_poll.py::TestPollerStopResetsFailCount tests/test_poll.py::TestProgramRunningDefault -x -q</automated>
  </verify>
  <done>
    - ControllerPoller.stop() resets _fail_count=0 and _disconnect_start=None unconditionally
    - MachineState.program_running defaults to True
    - Existing TestProgramRunningDefault assertion updated to assertTrue
    - New TestPollerStopResetsFailCount passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cold-start and RECOVER chain tests</name>
  <files>tests/test_status_bar.py, tests/test_poll.py</files>
  <behavior>
    - Test: Fresh MachineState (never polled) causes StatusBar to show "OFFLINE" not "E-STOP"
    - Test: RECOVER chain — cold-start recover_enabled=False, E-STOP disconnect recover_enabled=False, reconnect with stopped program recover_enabled=True
  </behavior>
  <action>
    1. In tests/test_poll.py, add a new test class TestColdStartShowsOffline after TestProgramRunningDefault:
       - Instantiate a real MachineState() (NOT _make_state helper — must test real default)
       - Instantiate a StatusBar()
       - Call sb.update_from_state(state)
       - Assert sb.state_text == "OFFLINE"
       - This verifies the production fix from Task 1 is working end-to-end through StatusBar
       - Import StatusBar: from dmccodegui.screens.status_bar import StatusBar

    2. In tests/test_status_bar.py, add a test function or class for the RECOVER chain test_recover_enabled_chain:
       - Use existing _make_state() helper (it already defaults program_running=True)
       - Cold-start: _make_state(connected=False, program_running=True) -> sb.update_from_state -> assert recover_enabled is False
       - Post E-STOP: _make_state(connected=False, program_running=False) -> sb.update_from_state -> assert recover_enabled is False
       - Reconnect with stopped program: _make_state(connected=True, program_running=False) -> sb.update_from_state -> assert recover_enabled is True
       - Check how recover_enabled is exposed in StatusBar (look at line 87 area) — it may be stored as an attribute or derived. Adjust assertions to match actual implementation.

    3. Run all new and existing tests: pytest tests/test_poll.py tests/test_status_bar.py -x -q

    4. Run full suite to confirm no regressions: pytest tests/ -x -q
  </action>
  <verify>
    <automated>pytest tests/test_poll.py tests/test_status_bar.py -x -q</automated>
  </verify>
  <done>
    - TestColdStartShowsOffline passes: fresh MachineState shows OFFLINE on StatusBar
    - test_recover_enabled_chain passes: three RECOVER states verified
    - Full test suite green
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/test_poll.py tests/test_status_bar.py -x -q` — all new and existing tests pass
2. `pytest tests/ -x -q` — full suite green, no regressions
3. Manual check: grep for `_fail_count = 0` in poll.py stop() method
4. Manual check: grep for `program_running: bool = True` in app_state.py
</verification>

<success_criteria>
- ControllerPoller.stop() resets _fail_count to 0 and _disconnect_start to None
- MachineState.program_running defaults to True (conservative pattern)
- Cold-start StatusBar shows OFFLINE (gray), not E-STOP (red)
- RECOVER button disabled at cold-start, enabled after reconnect with stopped program
- All tests pass including full suite
</success_criteria>

<output>
After completion, create `.planning/phases/17-poll-reset-cold-start-fix/17-01-SUMMARY.md`
</output>
