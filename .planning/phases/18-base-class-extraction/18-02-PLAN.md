---
phase: 18-base-class-extraction
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - src/dmccodegui/screens/run.py
  - src/dmccodegui/screens/axes_setup.py
  - src/dmccodegui/screens/parameters.py
  - src/dmccodegui/screens/__init__.py
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03, ARCH-04]

must_haves:
  truths:
    - "RunScreen inherits from BaseRunScreen and application runs identically to v2.0"
    - "AxesSetupScreen inherits from BaseAxesSetupScreen and application runs identically to v2.0"
    - "ParametersScreen inherits from BaseParametersScreen and application runs identically to v2.0"
    - "Navigating away from any screen unsubscribes all state listeners; navigating back re-subscribes"
    - "Zero duplicate callbacks exist after two enter/leave cycles on any screen"
    - "DeltaCBarChart is imported from flat_grind_widgets in run.py, not defined inline"
    - "_SETUP_SCREENS is not duplicated — only exists in SetupScreenMixin"
  artifacts:
    - path: "src/dmccodegui/screens/run.py"
      provides: "RunScreen(BaseRunScreen) — inherits subscription lifecycle from base"
      contains: "class RunScreen(BaseRunScreen)"
    - path: "src/dmccodegui/screens/axes_setup.py"
      provides: "AxesSetupScreen(BaseAxesSetupScreen) — inherits jog + setup lifecycle from base"
      contains: "class AxesSetupScreen(BaseAxesSetupScreen)"
    - path: "src/dmccodegui/screens/parameters.py"
      provides: "ParametersScreen(BaseParametersScreen) — inherits card builder + dirty tracking from base"
      contains: "class ParametersScreen(BaseParametersScreen)"
    - path: "src/dmccodegui/screens/__init__.py"
      provides: "Exports base classes for downstream phases"
  key_links:
    - from: "src/dmccodegui/screens/run.py"
      to: "src/dmccodegui/screens/base.py"
      via: "class RunScreen(BaseRunScreen)"
      pattern: "from \\.base import BaseRunScreen"
    - from: "src/dmccodegui/screens/run.py"
      to: "src/dmccodegui/screens/flat_grind_widgets.py"
      via: "import DeltaCBarChart from flat_grind_widgets"
      pattern: "from \\.flat_grind_widgets import"
    - from: "src/dmccodegui/screens/axes_setup.py"
      to: "src/dmccodegui/screens/base.py"
      via: "class AxesSetupScreen(BaseAxesSetupScreen)"
      pattern: "from \\.base import BaseAxesSetupScreen"
    - from: "src/dmccodegui/screens/parameters.py"
      to: "src/dmccodegui/screens/base.py"
      via: "class ParametersScreen(BaseParametersScreen)"
      pattern: "from \\.base import BaseParametersScreen"
---

<objective>
Modify the three existing screen classes (RunScreen, AxesSetupScreen, ParametersScreen) to inherit from their new base classes created in Plan 01. Remove duplicated code that now lives in the base. Update imports. Verify zero behavior change.

Purpose: Complete the base class extraction by wiring existing screens to inherit shared behavior, proving the refactor is safe before Phase 19 renames anything.
Output: Modified run.py, axes_setup.py, parameters.py, __init__.py with base class inheritance.
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-base-class-extraction/18-CONTEXT.md
@.planning/phases/18-base-class-extraction/18-RESEARCH.md
@.planning/phases/18-base-class-extraction/18-01-SUMMARY.md

@src/dmccodegui/screens/base.py
@src/dmccodegui/screens/flat_grind_widgets.py
@src/dmccodegui/screens/run.py
@src/dmccodegui/screens/axes_setup.py
@src/dmccodegui/screens/parameters.py
@src/dmccodegui/screens/__init__.py
@tests/test_run_screen.py
@tests/test_axes_setup.py
@tests/test_parameters.py
@tests/test_base_classes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire RunScreen to inherit from BaseRunScreen and extract DeltaC widgets</name>
  <files>src/dmccodegui/screens/run.py</files>
  <action>
Modify run.py to inherit from BaseRunScreen and import DeltaC widgets from flat_grind_widgets.py.

**Import changes:**
- Add: `from .base import BaseRunScreen`
- Add: `from .flat_grind_widgets import DeltaCBarChart, _BaseBarChart, DELTA_C_WRITABLE_START, DELTA_C_WRITABLE_END, DELTA_C_ARRAY_SIZE, DELTA_C_STEP, STONE_SURFACE_MM, STONE_OVERHANG_MM, STEP_MM, STONE_WINDOW_INDICES, stone_window_for_index`
- Remove: `from kivy.uix.screenmanager import Screen` (now inherited via BaseRunScreen)

**Class declaration change:**
- Change `class RunScreen(Screen):` to `class RunScreen(BaseRunScreen):`

**Remove from RunScreen (now in BaseRunScreen):**
- Remove `controller = ObjectProperty(None, allownone=True)` declaration
- Remove `state = ObjectProperty(None, allownone=True)` declaration
- Remove `_state_unsub = None` class-level attribute

**Lifecycle hook changes:**
- `on_pre_enter`: Remove the subscribe block (lines ~525-531 area). Add `super().on_pre_enter(*args)` as the FIRST line. Keep all other on_pre_enter logic (plot setup, pos_poll start, clock scheduling, etc.) after the super() call.
- `on_leave`: Keep all existing cleanup (stop clocks, stop pos_poll, stop mg_reader, cancel scheduled calls) BEFORE calling `super().on_leave(*args)` as the LAST line. This ensures threads are stopped before unsubscribe fires (per pitfall #1 in RESEARCH.md).
- Add `_on_state_change(self, state)` method that delegates to `self._apply_state(state)` (preserving existing behavior).

**Remove widget class definitions (now in flat_grind_widgets.py):**
- Remove `_BaseBarChart` class definition entirely
- Remove `DeltaCBarChart` class definition entirely
- Remove `stone_window_for_index()` function definition
- Remove DELTA_C_* constants (DELTA_C_WRITABLE_START through STONE_WINDOW_INDICES)
- Keep `BCOMP_STEP` and `BCompBarChart` in run.py (Serration-specific, stays per locked decision)

**BCompBarChart inheritance fix:** BCompBarChart currently inherits from `_BaseBarChart`. After extraction, `_BaseBarChart` is imported from flat_grind_widgets.py, so BCompBarChart's inheritance still works via the import. Verify this.

Keep all other RunScreen code untouched — cycle controls, plot logic, bComp panel, position display, etc.
  </action>
  <verify>
    <automated>cd "C:/Users/danii/OneDrive/Desktop/PythonDMCGUI" && python -m pytest tests/test_run_screen.py tests/test_base_classes.py tests/test_delta_c_bar_chart.py -x -v</automated>
  </verify>
  <done>RunScreen inherits from BaseRunScreen. DeltaCBarChart and _BaseBarChart are imported from flat_grind_widgets.py, not defined in run.py. Subscribe/unsubscribe lifecycle is handled by base class. All existing RunScreen tests pass. BCompBarChart still works (inherits _BaseBarChart via import).</done>
</task>

<task type="auto">
  <name>Task 2: Wire AxesSetupScreen and ParametersScreen to inherit from base classes</name>
  <files>src/dmccodegui/screens/axes_setup.py, src/dmccodegui/screens/parameters.py, src/dmccodegui/screens/__init__.py</files>
  <action>
**axes_setup.py changes:**

Import changes:
- Add: `from .base import BaseAxesSetupScreen`
- Remove: `from kivy.uix.screenmanager import Screen`
- Remove imports that are now only used by base (if any — check after extraction)

Class declaration:
- Change `class AxesSetupScreen(Screen):` to `class AxesSetupScreen(BaseAxesSetupScreen):`

Remove from AxesSetupScreen (now in base):
- Remove `controller = ObjectProperty(None, allownone=True)` declaration
- Remove `state = ObjectProperty(None, allownone=True)` declaration
- Remove `_SETUP_SCREENS` frozenset (now in SetupScreenMixin)
- Remove `_enter_setup_if_needed()` method (now in SetupScreenMixin)
- Remove `_exit_setup_if_needed()` method (now in SetupScreenMixin)
- Remove `_state_unsub = None` if present
- Remove jog_axis() method (now in BaseAxesSetupScreen)
- Remove CPM read methods (_read_cpm_for_axis, _schedule_cpm_read) if extracted to base

Lifecycle hook changes:
- `on_pre_enter`: Add `super().on_pre_enter(*args)` as first line. Remove subscribe block and _enter_setup_if_needed() call (both now in base). Keep all other on_pre_enter logic (axis row setup, CPM scheduling, etc.).
- `on_leave`: Keep screen-specific cleanup first. Call `super().on_leave(*args)` as the LAST line (handles _exit_setup + unsubscribe).

Note: AxesSetupScreen currently does NOT subscribe to MachineState. After this change, BaseAxesSetupScreen adds a subscription with a no-op _on_state_change. This is safe — no behavior change (per RESEARCH.md open question #3).

**parameters.py changes:**

Import changes:
- Add: `from .base import BaseParametersScreen`
- Remove: `from kivy.uix.screenmanager import Screen`

Class declaration:
- Change `class ParametersScreen(Screen):` to `class ParametersScreen(BaseParametersScreen):`

Remove from ParametersScreen (now in base):
- Remove `controller = ObjectProperty(None, allownone=True)` declaration
- Remove `state = ObjectProperty(None, allownone=True)` declaration
- Remove `_SETUP_SCREENS` frozenset
- Remove `_enter_setup_if_needed()` method
- Remove `_exit_setup_if_needed()` method
- Remove `_state_unsub = None` if present
- Remove build_param_cards() (now in base)
- Remove validate_field() (now in base)
- Remove dirty tracking methods (_mark_dirty, _clear_dirty, _has_dirty) and the dirty dict initialization (now in base)
- Remove apply_to_controller() (now in base)
- Remove read_from_controller() (now in base)
- Remove _rebuild_for_machine_type() (now in base)
- Remove the subscribe block from on_pre_enter (now in base)
- Remove the unsubscribe block from on_leave (now in base)

Lifecycle hook changes:
- `on_pre_enter`: `super().on_pre_enter(*args)` first. Remove subscribe + _enter_setup_if_needed calls. Keep screen-specific logic.
- `on_leave`: Screen-specific cleanup first. `super().on_leave(*args)` last.

If ParametersScreen has an `_on_state_change`-equivalent (like _apply_state), wire `_on_state_change` to delegate to it, same as RunScreen pattern.

**__init__.py changes:**
- Add exports for base classes:
  ```python
  from .base import BaseRunScreen, BaseAxesSetupScreen, BaseParametersScreen, SetupScreenMixin
  ```
- Add to `__all__`: "BaseRunScreen", "BaseAxesSetupScreen", "BaseParametersScreen", "SetupScreenMixin"
- Add export for flat_grind_widgets:
  ```python
  from .flat_grind_widgets import DeltaCBarChart
  ```
- Add "DeltaCBarChart" to `__all__`
  </action>
  <verify>
    <automated>cd "C:/Users/danii/OneDrive/Desktop/PythonDMCGUI" && python -m pytest tests/test_axes_setup.py tests/test_parameters.py tests/test_base_classes.py -x -v</automated>
  </verify>
  <done>AxesSetupScreen inherits from BaseAxesSetupScreen. ParametersScreen inherits from BaseParametersScreen. _SETUP_SCREENS is not duplicated (only in SetupScreenMixin). Subscribe/unsubscribe lifecycle handled by base classes. All existing tests pass. __init__.py exports base classes.</done>
</task>

<task type="auto">
  <name>Task 3: Full regression test suite and subscription lifecycle verification</name>
  <files>tests/test_base_classes.py</files>
  <action>
Run the FULL test suite to verify zero behavior change from v2.0. Then add regression-specific tests to test_base_classes.py if not already covered:

1. `test_run_screen_inherits_base`: Verify `issubclass(RunScreen, BaseRunScreen)` is True.
2. `test_axes_setup_inherits_base`: Verify `issubclass(AxesSetupScreen, BaseAxesSetupScreen)` is True.
3. `test_parameters_inherits_base`: Verify `issubclass(ParametersScreen, BaseParametersScreen)` is True.
4. `test_no_duplicate_setup_screens_frozenset`: Grep axes_setup.py and parameters.py source for `_SETUP_SCREENS` — should NOT be defined in either file (only in base.py via SetupScreenMixin).
5. `test_run_screen_no_inline_object_properties`: Verify RunScreen does not define controller or state ObjectProperty directly (inherited from base).
6. `test_two_enter_leave_cycles_no_leak`: For each screen class (RunScreen, AxesSetupScreen, ParametersScreen), mock MachineState, do two on_pre_enter/on_leave cycles, verify the listener list has exactly 0 entries after the final on_leave.

Run full suite: `pytest tests/ -x` — every single test must pass. If any existing test fails, fix the root cause (likely an import path change for DeltaCBarChart in test_delta_c_bar_chart.py or a mock setup issue).

Fix test_delta_c_bar_chart.py if needed — it likely imports DeltaCBarChart from run.py. Update to import from flat_grind_widgets.py, or verify it works via run.py re-export.
  </action>
  <verify>
    <automated>cd "C:/Users/danii/OneDrive/Desktop/PythonDMCGUI" && python -m pytest tests/ -x -v</automated>
  </verify>
  <done>Full test suite passes with zero failures. All three screen classes inherit from their base classes. No duplicate _SETUP_SCREENS definition. No duplicate ObjectProperty declarations. Two enter/leave cycles produce zero leaked subscriptions. ARCH-01, ARCH-02, ARCH-03 (partially — base classes enable per-machine classes), and ARCH-04 all verified.</done>
</task>

</tasks>

<verification>
1. Inheritance verified: `python -c "from dmccodegui.screens import RunScreen, AxesSetupScreen, ParametersScreen; from dmccodegui.screens.base import BaseRunScreen, BaseAxesSetupScreen, BaseParametersScreen; assert issubclass(RunScreen, BaseRunScreen); assert issubclass(AxesSetupScreen, BaseAxesSetupScreen); assert issubclass(ParametersScreen, BaseParametersScreen); print('All screens inherit from base classes')"`
2. No duplicated _SETUP_SCREENS: `grep -rn "_SETUP_SCREENS" src/dmccodegui/screens/` should show ONLY base.py
3. Full test suite: `pytest tests/ -x -v` — all tests green
4. DeltaCBarChart import from new location: `python -c "from dmccodegui.screens.flat_grind_widgets import DeltaCBarChart; print('OK')"`
</verification>

<success_criteria>
- RunScreen(BaseRunScreen), AxesSetupScreen(BaseAxesSetupScreen), ParametersScreen(BaseParametersScreen) — all three inherit correctly
- Application runs identically to v2.0 (full test suite passes)
- _SETUP_SCREENS exists only in SetupScreenMixin (screens/base.py)
- DeltaCBarChart imported from flat_grind_widgets.py in run.py
- Two enter/leave cycles on any screen produce zero leaked subscriptions
- All lifecycle hooks are in Python only, not .kv files
- __init__.py exports base classes for downstream phases
- ARCH-03 note: Phase 18 creates base classes that enable per-machine screen classes. Actual 9 subclasses created in Phases 19-22.
</success_criteria>

<output>
After completion, create `.planning/phases/18-base-class-extraction/18-02-SUMMARY.md`
</output>
