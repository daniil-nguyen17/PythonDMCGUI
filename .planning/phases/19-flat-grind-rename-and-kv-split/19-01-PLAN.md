---
phase: 19-flat-grind-rename-and-kv-split
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dmccodegui/screens/flat_grind/__init__.py
  - src/dmccodegui/screens/flat_grind/run.py
  - src/dmccodegui/screens/flat_grind/axes_setup.py
  - src/dmccodegui/screens/flat_grind/parameters.py
  - src/dmccodegui/screens/flat_grind/widgets.py
  - src/dmccodegui/ui/flat_grind/run.kv
  - src/dmccodegui/ui/flat_grind/axes_setup.kv
  - src/dmccodegui/ui/flat_grind/parameters.kv
autonomous: true
requirements: [FLAT-01, FLAT-02, FLAT-03]

must_haves:
  truths:
    - "FlatGrindRunScreen class exists and inherits from BaseRunScreen"
    - "FlatGrindAxesSetupScreen class exists and inherits from BaseAxesSetupScreen"
    - "FlatGrindParametersScreen class exists and inherits from BaseParametersScreen"
    - "Each FlatGrind* screen has its own .kv file in ui/flat_grind/"
    - "Importing screens.flat_grind package loads all three KV files via Builder"
  artifacts:
    - path: "src/dmccodegui/screens/flat_grind/__init__.py"
      provides: "Package initializer — Builder.load_file() + class exports"
      min_lines: 20
    - path: "src/dmccodegui/screens/flat_grind/run.py"
      provides: "FlatGrindRunScreen class"
      contains: "class FlatGrindRunScreen"
    - path: "src/dmccodegui/screens/flat_grind/axes_setup.py"
      provides: "FlatGrindAxesSetupScreen class"
      contains: "class FlatGrindAxesSetupScreen"
    - path: "src/dmccodegui/screens/flat_grind/parameters.py"
      provides: "FlatGrindParametersScreen class"
      contains: "class FlatGrindParametersScreen"
    - path: "src/dmccodegui/screens/flat_grind/widgets.py"
      provides: "DeltaCBarChart, _BaseBarChart, stone geometry constants"
      contains: "class DeltaCBarChart"
    - path: "src/dmccodegui/ui/flat_grind/run.kv"
      provides: "FlatGrindRunScreen KV layout"
      contains: "<FlatGrindRunScreen>"
    - path: "src/dmccodegui/ui/flat_grind/axes_setup.kv"
      provides: "FlatGrindAxesSetupScreen KV layout"
      contains: "<FlatGrindAxesSetupScreen>"
    - path: "src/dmccodegui/ui/flat_grind/parameters.kv"
      provides: "FlatGrindParametersScreen KV layout"
      contains: "<FlatGrindParametersScreen>"
  key_links:
    - from: "src/dmccodegui/screens/flat_grind/__init__.py"
      to: "src/dmccodegui/ui/flat_grind/*.kv"
      via: "Builder.load_file() calls before class imports"
      pattern: "Builder\\.load_file.*ui.*flat_grind"
    - from: "src/dmccodegui/screens/flat_grind/run.py"
      to: "src/dmccodegui/screens/base.py"
      via: "from ..base import BaseRunScreen"
      pattern: "from\\s+\\.\\.base\\s+import\\s+BaseRunScreen"
    - from: "src/dmccodegui/screens/flat_grind/run.py"
      to: "src/dmccodegui/screens/flat_grind/widgets.py"
      via: "from .widgets import DeltaCBarChart"
      pattern: "from\\.widgets\\s+import"
---

<objective>
Create the screens/flat_grind/ Python package and ui/flat_grind/ KV files with renamed FlatGrind* classes.

Purpose: Establishes the per-machine package structure and naming convention. All new files are copies of existing screen code with class names, imports, and KV rule headers renamed. BCompBarChart and BCOMP_* constants removed from run.py (deferred to Phase 21). This plan creates files only — wiring (main.py, base.kv, test imports) happens in Plan 02.

Output: 5 Python files in screens/flat_grind/, 3 KV files in ui/flat_grind/
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-flat-grind-rename-and-kv-split/19-RESEARCH.md

Relevant source files (read these before implementing):
@src/dmccodegui/screens/run.py
@src/dmccodegui/screens/axes_setup.py
@src/dmccodegui/screens/parameters.py
@src/dmccodegui/screens/flat_grind_widgets.py
@src/dmccodegui/screens/base.py
@src/dmccodegui/ui/run.kv
@src/dmccodegui/ui/axes_setup.kv
@src/dmccodegui/ui/parameters.kv

<interfaces>
<!-- From screens/base.py — the base classes these new files inherit from -->
From src/dmccodegui/screens/base.py:
- class BaseRunScreen(Screen) — controller, state ObjectProperties, subscribe/unsubscribe lifecycle
- class BaseAxesSetupScreen(Screen, SetupScreenMixin) — jog_axis, CPM read, setup entry/exit
- class BaseParametersScreen(Screen, SetupScreenMixin) — card builder, dirty tracking, validate/apply/read
- class SetupScreenMixin — _enter_setup_if_needed, _exit_setup_if_needed
- Module-level: from dmccodegui.utils.jobs import submit
- Module-level: import dmccodegui.machine_config as mc

From src/dmccodegui/screens/flat_grind_widgets.py:
- class _BaseBarChart(Widget) — shared bar chart base
- class DeltaCBarChart(_BaseBarChart) — Knife Grind Adjustment bar chart
- DELTA_C_WRITABLE_START, DELTA_C_WRITABLE_END, DELTA_C_ARRAY_SIZE, DELTA_C_STEP
- STONE_SURFACE_MM, STONE_OVERHANG_MM, STEP_MM, STONE_WINDOW_INDICES
- stone_window_for_index(idx) -> str
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create screens/flat_grind/ package with widgets and __init__.py</name>
  <files>
    src/dmccodegui/screens/flat_grind/__init__.py,
    src/dmccodegui/screens/flat_grind/widgets.py
  </files>
  <action>
1. Create directory `src/dmccodegui/screens/flat_grind/`.

2. Create `screens/flat_grind/widgets.py`:
   - Copy entire contents of `screens/flat_grind_widgets.py` verbatim.
   - No changes needed — this file has no relative imports that change depth. Verify: the file uses absolute imports (`from dmccodegui.app_state import ...` or similar), not relative. If it uses relative imports like `from ..app_state`, adjust to `from ...app_state` (one extra dot for the deeper nesting).
   - Confirm DeltaCBarChart, _BaseBarChart, all DELTA_C_* and STONE_* constants, and stone_window_for_index are present.

3. Create `screens/flat_grind/__init__.py`:
   - Use the exact pattern from 19-RESEARCH.md "Package __init__.py as Self-Contained Loader":
     ```python
     """Flat Grind screen package.
     
     Loads KV files and exports screen classes. Importing this package
     automatically registers KV rules for all Flat Grind screens.
     """
     import os
     from kivy.lang import Builder
     
     _PKG_DIR = os.path.dirname(os.path.abspath(__file__))
     _UI_DIR = os.path.normpath(os.path.join(_PKG_DIR, '..', '..', 'ui', 'flat_grind'))
     
     Builder.load_file(os.path.join(_UI_DIR, 'run.kv'))
     Builder.load_file(os.path.join(_UI_DIR, 'axes_setup.kv'))
     Builder.load_file(os.path.join(_UI_DIR, 'parameters.kv'))
     
     from .run import FlatGrindRunScreen  # noqa: E402
     from .axes_setup import FlatGrindAxesSetupScreen  # noqa: E402
     from .parameters import FlatGrindParametersScreen  # noqa: E402
     
     __all__ = [
         "FlatGrindRunScreen",
         "FlatGrindAxesSetupScreen",
         "FlatGrindParametersScreen",
     ]
     ```
   - CRITICAL: Builder.load_file() calls MUST come before class imports to avoid "No rule found" errors.
   - NOTE: The __init__.py will not be importable yet because run.py, axes_setup.py, parameters.py don't exist yet. That's fine — they're created in Task 2. The KV files also don't exist yet — created in Task 3. This task just writes the files.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('src/dmccodegui/screens/flat_grind/widgets.py').read()); ast.parse(open('src/dmccodegui/screens/flat_grind/__init__.py').read()); print('Syntax OK')"</automated>
  </verify>
  <done>screens/flat_grind/ directory exists with __init__.py (Builder.load_file + exports) and widgets.py (DeltaCBarChart, _BaseBarChart, all constants)</done>
</task>

<task type="auto">
  <name>Task 2: Create FlatGrind* screen Python files</name>
  <files>
    src/dmccodegui/screens/flat_grind/run.py,
    src/dmccodegui/screens/flat_grind/axes_setup.py,
    src/dmccodegui/screens/flat_grind/parameters.py
  </files>
  <action>
For each of the three screen files, copy from the existing file and make ONLY these changes:

**A) screens/flat_grind/run.py** (copy from screens/run.py):
1. Rename class: `class RunScreen(BaseRunScreen)` -> `class FlatGrindRunScreen(BaseRunScreen)`
2. Update all internal log prefixes: `[RunScreen]` -> `[FlatGrindRunScreen]`
3. Fix relative imports — files are now one level deeper in screens/flat_grind/:
   - `from ..app_state import MachineState` -> `from ...app_state import MachineState`
   - `from ..controller import GalilController` -> `from ...controller import GalilController`
   - `from ..hmi.dmc_vars import ...` -> `from ...hmi.dmc_vars import ...`
   - `from ..utils import jobs` -> `from ...utils import jobs`
   - `from .base import BaseRunScreen` -> `from ..base import BaseRunScreen`
   - `from .flat_grind_widgets import DeltaCBarChart, _BaseBarChart, ...` -> `from .widgets import DeltaCBarChart, _BaseBarChart, ...`
4. REMOVE BCompBarChart class definition entirely (lines ~78-165 in current run.py). Remove BCOMP_STEP constant.
5. REMOVE all BCompBarChart-related methods in the RunScreen class body — specifically any methods that only manipulate bComp data. Keep methods that handle deltaC.
6. REMOVE any `#:import BCompBarChart` references if they appear in Python (they shouldn't — they're in KV).
7. Keep ALL other code exactly as-is: PLOT_BUFFER_SIZE, PLOT_UPDATE_HZ, CYCLE_VAR_*, matplotlib figure setup, deltaC methods, position plotting, _on_state_change, all button handlers, etc.
8. Update any self-references: `self.__class__.__name__` will auto-reflect new name, but if there are hardcoded `"RunScreen"` strings in log messages, rename to `"FlatGrindRunScreen"`.

**B) screens/flat_grind/axes_setup.py** (copy from screens/axes_setup.py):
1. Rename class: `class AxesSetupScreen(BaseAxesSetupScreen)` -> `class FlatGrindAxesSetupScreen(BaseAxesSetupScreen)`
2. Update log prefixes: `[AxesSetupScreen]` -> `[FlatGrindAxesSetupScreen]`
3. Fix relative imports (one level deeper):
   - `from ..app_state import MachineState` -> `from ...app_state import MachineState` (if present)
   - `from ..controller import GalilController` -> `from ...controller import GalilController` (if present)
   - `from ..hmi.dmc_vars import ...` -> `from ...hmi.dmc_vars import ...`
   - `from .base import BaseAxesSetupScreen` -> `from ..base import BaseAxesSetupScreen`
   - Any other `from ..X` -> `from ...X`
4. Keep ALL constants (AXIS_CPM_DEFAULTS, AXIS_LABELS, AXIS_COLORS, _AXIS_ROW_IDS) and all remaining code exactly as-is.

**C) screens/flat_grind/parameters.py** (copy from screens/parameters.py):
1. Rename class: `class ParametersScreen(BaseParametersScreen)` -> `class FlatGrindParametersScreen(BaseParametersScreen)`
2. Update log prefixes: `[ParametersScreen]` -> `[FlatGrindParametersScreen]`
3. Fix relative imports (one level deeper):
   - `from .base import BaseParametersScreen` -> `from ..base import BaseParametersScreen`
   - `import dmccodegui.machine_config as mc` -> stays the same (absolute import, no change)
   - `from dmccodegui.machine_config import _FLAT_PARAM_DEFS as PARAM_DEFS` -> stays the same (absolute)
   - Any `from ..X` -> `from ...X`
4. Keep ALL constants (PARAM_DEFS re-export, validation constants) and code exactly as-is.

CRITICAL RULE: Every `from ..` in the original becomes `from ...` in the new location. Every `from .` that references a sibling in screens/ becomes `from ..`. The `from .widgets` import in run.py stays as `from .widgets` (same-package reference).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('src/dmccodegui/screens/flat_grind/run.py').read()); ast.parse(open('src/dmccodegui/screens/flat_grind/axes_setup.py').read()); ast.parse(open('src/dmccodegui/screens/flat_grind/parameters.py').read()); print('Syntax OK')"</automated>
  </verify>
  <done>Three FlatGrind* screen classes exist in screens/flat_grind/ with correct inheritance, updated imports (extra dot for deeper nesting), renamed log prefixes, and BCompBarChart removed from run.py</done>
</task>

<task type="auto">
  <name>Task 3: Create ui/flat_grind/ KV files with renamed rule headers</name>
  <files>
    src/dmccodegui/ui/flat_grind/run.kv,
    src/dmccodegui/ui/flat_grind/axes_setup.kv,
    src/dmccodegui/ui/flat_grind/parameters.kv
  </files>
  <action>
1. Create directory `src/dmccodegui/ui/flat_grind/`.

2. Create `ui/flat_grind/run.kv`:
   - Copy entire contents of `ui/run.kv`.
   - Change ONLY:
     a. `#:import DeltaCBarChart dmccodegui.screens.run` -> `#:import DeltaCBarChart dmccodegui.screens.flat_grind.widgets`
     b. `#:import BCompBarChart dmccodegui.screens.run` -> REMOVE this line entirely (BCompBarChart removed per decision)
     c. `#:import run_module dmccodegui.screens.run` -> `#:import run_module dmccodegui.screens.flat_grind.run`
     d. `<RunScreen>:` -> `<FlatGrindRunScreen>:`
   - IMPORTANT: Also search for any BCompBarChart widget references in the KV body. If found, remove the entire BCompBarChart widget subtree from the layout. The bComp panel will be redesigned from scratch in Phase 21.
   - Do NOT change any other layout, widget IDs, property bindings, or style values.

3. Create `ui/flat_grind/axes_setup.kv`:
   - Copy entire contents of `ui/axes_setup.kv`.
   - Change ONLY:
     a. `<AxesSetupScreen>:` -> `<FlatGrindAxesSetupScreen>:`
   - No #:import changes needed (axes_setup.kv imports only theme and dp, which are unchanged).

4. Create `ui/flat_grind/parameters.kv`:
   - Copy entire contents of `ui/parameters.kv`.
   - Change ONLY:
     a. `<ParametersScreen>:` -> `<FlatGrindParametersScreen>:`
   - No #:import changes needed.

VERIFY after creating: grep for `<RunScreen>:` or `<AxesSetupScreen>:` or `<ParametersScreen>:` in the new ui/flat_grind/ files — should return zero matches. grep for `<FlatGrind` — should return exactly 3 matches (one per file).
  </action>
  <verify>
    <automated>grep -rh "^<RunScreen>\|^<AxesSetupScreen>\|^<ParametersScreen>" src/dmccodegui/ui/flat_grind/ | wc -l | xargs test 0 -eq && grep -rh "^<FlatGrind" src/dmccodegui/ui/flat_grind/ | wc -l | xargs test 3 -eq && echo "KV headers OK"</automated>
  </verify>
  <done>Three KV files exist in ui/flat_grind/ with FlatGrind* rule headers, updated #:import directives, BCompBarChart references removed from run.kv</done>
</task>

</tasks>

<verification>
After all three tasks:
1. `python -c "import ast; [ast.parse(open(f).read()) for f in ['src/dmccodegui/screens/flat_grind/__init__.py', 'src/dmccodegui/screens/flat_grind/run.py', 'src/dmccodegui/screens/flat_grind/axes_setup.py', 'src/dmccodegui/screens/flat_grind/parameters.py', 'src/dmccodegui/screens/flat_grind/widgets.py']]; print('All Python files parse')"` — all 5 Python files have valid syntax
2. `grep -rh "^<[A-Z]" src/dmccodegui/ui/flat_grind/ | sort` — returns exactly `<FlatGrindAxesSetupScreen>:`, `<FlatGrindParametersScreen>:`, `<FlatGrindRunScreen>:`
3. `grep -c "BCompBarChart" src/dmccodegui/screens/flat_grind/run.py` — returns 0 (BComp removed)
4. `grep -c "from \\.\\.\\.\\|from \\.\\.base" src/dmccodegui/screens/flat_grind/run.py` — returns >0 (imports use correct depth)
</verification>

<success_criteria>
- 5 new Python files in screens/flat_grind/ (all parse without syntax errors)
- 3 new KV files in ui/flat_grind/ (all have FlatGrind* rule headers)
- FlatGrindRunScreen inherits BaseRunScreen, no BCompBarChart code
- FlatGrindAxesSetupScreen inherits BaseAxesSetupScreen
- FlatGrindParametersScreen inherits BaseParametersScreen
- __init__.py has Builder.load_file() before imports and exports all 3 classes
- All relative imports use correct depth (... for cross-package, .. for parent)
</success_criteria>

<output>
After completion, create `.planning/phases/19-flat-grind-rename-and-kv-split/19-01-SUMMARY.md`
</output>
