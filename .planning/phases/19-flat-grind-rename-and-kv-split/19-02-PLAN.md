---
phase: 19-flat-grind-rename-and-kv-split
plan: "02"
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/dmccodegui/screens/run.py
  - src/dmccodegui/screens/axes_setup.py
  - src/dmccodegui/screens/parameters.py
  - src/dmccodegui/screens/flat_grind_widgets.py
  - src/dmccodegui/screens/__init__.py
  - src/dmccodegui/main.py
  - src/dmccodegui/ui/base.kv
  - tests/test_run_screen.py
  - tests/test_axes_setup.py
  - tests/test_parameters.py
  - tests/test_delta_c_bar_chart.py
  - tests/test_flat_grind_widgets.py
  - tests/test_base_classes.py
autonomous: true
requirements: [FLAT-01, FLAT-02, FLAT-03, FLAT-04]

must_haves:
  truths:
    - "Application runs with FlatGrind* class names and ui/flat_grind/*.kv files"
    - "All v2.0 Flat Grind functionality works without behavior change"
    - "No kv rule name collision across all kv files"
    - "Old import paths still resolve via thin re-export wrappers"
    - "Entire existing test suite passes with updated FlatGrind* imports"
  artifacts:
    - path: "src/dmccodegui/screens/run.py"
      provides: "Thin re-export wrapper: FlatGrindRunScreen as RunScreen"
      contains: "from .flat_grind"
    - path: "src/dmccodegui/screens/axes_setup.py"
      provides: "Thin re-export wrapper: FlatGrindAxesSetupScreen as AxesSetupScreen"
      contains: "from .flat_grind"
    - path: "src/dmccodegui/screens/parameters.py"
      provides: "Thin re-export wrapper: FlatGrindParametersScreen as ParametersScreen"
      contains: "from .flat_grind"
    - path: "src/dmccodegui/screens/__init__.py"
      provides: "Updated exports with FlatGrind* names"
      contains: "FlatGrindRunScreen"
    - path: "src/dmccodegui/ui/base.kv"
      provides: "ScreenManager with FlatGrind* class names"
      contains: "FlatGrindRunScreen"
  key_links:
    - from: "src/dmccodegui/main.py"
      to: "src/dmccodegui/screens/flat_grind/__init__.py"
      via: "KV_FILES list no longer has run.kv/axes_setup.kv/parameters.kv; package import triggers Builder.load_file"
      pattern: "screens\\.flat_grind"
    - from: "src/dmccodegui/ui/base.kv"
      to: "src/dmccodegui/ui/flat_grind/*.kv"
      via: "ScreenManager references FlatGrind* class names matching KV rule headers"
      pattern: "FlatGrindRunScreen.*name.*run"
    - from: "tests/*.py"
      to: "src/dmccodegui/screens/flat_grind/"
      via: "All test imports updated to screens.flat_grind with FlatGrind* names"
      pattern: "from dmccodegui\\.screens\\.flat_grind"
---

<objective>
Wire the application to use FlatGrind* classes from the new package — update main.py, base.kv, make old files thin wrappers, update all test imports, and verify zero behavior change.

Purpose: Completes the rename by switching every consumer to the new classes. Old file paths become thin re-export wrappers as a safety net. The existing test suite validates zero behavior change (FLAT-04).

Output: All production and test code uses FlatGrind* classes; full test suite passes.
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-flat-grind-rename-and-kv-split/19-RESEARCH.md
@.planning/phases/19-flat-grind-rename-and-kv-split/19-01-SUMMARY.md

Relevant source files:
@src/dmccodegui/screens/__init__.py
@src/dmccodegui/main.py
@src/dmccodegui/ui/base.kv
@src/dmccodegui/screens/run.py
@src/dmccodegui/screens/axes_setup.py
@src/dmccodegui/screens/parameters.py
@src/dmccodegui/screens/flat_grind_widgets.py
@tests/test_run_screen.py
@tests/test_axes_setup.py
@tests/test_parameters.py
@tests/test_delta_c_bar_chart.py
@tests/test_flat_grind_widgets.py
@tests/test_base_classes.py

<interfaces>
<!-- From Plan 01 outputs — the new package structure -->
From src/dmccodegui/screens/flat_grind/__init__.py:
- Exports: FlatGrindRunScreen, FlatGrindAxesSetupScreen, FlatGrindParametersScreen
- Builder.load_file() calls for ui/flat_grind/{run,axes_setup,parameters}.kv

From src/dmccodegui/screens/flat_grind/run.py:
- class FlatGrindRunScreen(BaseRunScreen)
- PLOT_BUFFER_SIZE, PLOT_UPDATE_HZ, CYCLE_VAR_TOOTH, CYCLE_VAR_PASS, CYCLE_VAR_DEPTH (module constants)

From src/dmccodegui/screens/flat_grind/axes_setup.py:
- class FlatGrindAxesSetupScreen(BaseAxesSetupScreen)
- AXIS_CPM_DEFAULTS, AXIS_LABELS, AXIS_COLORS, _AXIS_ROW_IDS (module constants)

From src/dmccodegui/screens/flat_grind/parameters.py:
- class FlatGrindParametersScreen(BaseParametersScreen)
- PARAM_DEFS (re-exported from machine_config)

From src/dmccodegui/screens/flat_grind/widgets.py:
- DeltaCBarChart, _BaseBarChart
- DELTA_C_WRITABLE_START, DELTA_C_WRITABLE_END, DELTA_C_ARRAY_SIZE, DELTA_C_STEP
- STONE_SURFACE_MM, STONE_OVERHANG_MM, STEP_MM, STONE_WINDOW_INDICES
- stone_window_for_index
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert old screen files to thin re-export wrappers and update main.py + base.kv</name>
  <files>
    src/dmccodegui/screens/run.py,
    src/dmccodegui/screens/axes_setup.py,
    src/dmccodegui/screens/parameters.py,
    src/dmccodegui/screens/flat_grind_widgets.py,
    src/dmccodegui/screens/__init__.py,
    src/dmccodegui/main.py,
    src/dmccodegui/ui/base.kv
  </files>
  <action>
**A) Replace screens/run.py with thin re-export wrapper:**
Replace the entire file with a backward-compatibility wrapper. Must re-export:
- `FlatGrindRunScreen as RunScreen` (screen class alias)
- `PLOT_BUFFER_SIZE`, `PLOT_UPDATE_HZ` (test imports)
- `CYCLE_VAR_TOOTH`, `CYCLE_VAR_PASS`, `CYCLE_VAR_DEPTH` (test imports)
- Do NOT re-export BCompBarChart or BCOMP_* (removed per decision)
- Also re-export DeltaCBarChart and all DELTA_C_*/STONE_* constants from flat_grind.widgets (tests import these from screens.run)

Pattern from 19-RESEARCH.md:
```python
"""Backward-compatibility wrapper. Real implementation in screens.flat_grind.run."""
from .flat_grind.run import FlatGrindRunScreen as RunScreen  # noqa: F401
from .flat_grind.run import (  # noqa: F401
    PLOT_BUFFER_SIZE, PLOT_UPDATE_HZ,
    CYCLE_VAR_TOOTH, CYCLE_VAR_PASS, CYCLE_VAR_DEPTH,
)
from .flat_grind.widgets import (  # noqa: F401
    DeltaCBarChart, _BaseBarChart,
    DELTA_C_WRITABLE_START, DELTA_C_WRITABLE_END,
    DELTA_C_ARRAY_SIZE, DELTA_C_STEP,
    STONE_SURFACE_MM, STONE_OVERHANG_MM, STEP_MM,
    STONE_WINDOW_INDICES, stone_window_for_index,
)
```

**B) Replace screens/axes_setup.py with thin re-export wrapper:**
Must re-export:
- `FlatGrindAxesSetupScreen as AxesSetupScreen`
- `AXIS_CPM_DEFAULTS`, `AXIS_LABELS`, `AXIS_COLORS`, `_AXIS_ROW_IDS` (test imports)
- Check the actual file for any other module-level constants tests import.

**C) Replace screens/parameters.py with thin re-export wrapper:**
Must re-export:
- `FlatGrindParametersScreen as ParametersScreen`
- `PARAM_DEFS` (test imports)
- Check for any other module-level constants like validation bounds that tests import.

**D) Replace screens/flat_grind_widgets.py with thin re-export wrapper:**
Must re-export everything from `.flat_grind.widgets`:
- `DeltaCBarChart`, `_BaseBarChart`
- All DELTA_C_* and STONE_* constants
- `stone_window_for_index`

**E) Update screens/__init__.py:**
- Change imports to use flat_grind package:
  ```python
  from .flat_grind import FlatGrindRunScreen, FlatGrindAxesSetupScreen, FlatGrindParametersScreen
  ```
- Keep backward-compat aliases in __all__ for anything that still needs them, but primary exports are FlatGrind* names.
- Keep all non-screen imports unchanged (SetupScreen, StatusBar, TabBar, ProfilesScreen, etc.)
- Keep base class exports (BaseRunScreen, BaseAxesSetupScreen, etc.)
- Add FlatGrind* names to __all__. Keep RunScreen, AxesSetupScreen, ParametersScreen in __all__ via the re-export wrappers that still exist.

**F) Update main.py:**
- Remove `"ui/run.kv"`, `"ui/axes_setup.kv"`, `"ui/parameters.kv"` from KV_FILES list.
- Add a comment: `# Flat Grind KV loaded by screens.flat_grind.__init__`
- The import of `screens` package already triggers `screens.__init__.py` which imports `flat_grind` which triggers Builder.load_file. No additional import needed in main.py.
- Verify the screens package import happens BEFORE Builder.load_file loop in main.py's build() — check the existing `from . import screens as _screens` in __init__ section. This import at module level triggers the flat_grind __init__ before build() runs. The KV_FILES loop in build() must NOT load the old run/axes_setup/parameters KV files.

**G) Update ui/base.kv:**
- Change ScreenManager screen declarations:
  - `RunScreen:` -> `FlatGrindRunScreen:`
  - `AxesSetupScreen:` -> `FlatGrindAxesSetupScreen:`
  - `ParametersScreen:` -> `FlatGrindParametersScreen:`
- Keep `name: 'run'`, `name: 'axes_setup'`, `name: 'parameters'` values UNCHANGED.
- This is the most critical wiring change — if base.kv still says `RunScreen:`, Kivy instantiates the wrapper alias which works but KV rule matching uses `__class__.__name__` = `FlatGrindRunScreen`, which correctly matches `<FlatGrindRunScreen>:` in the new KV. However, to be explicit and avoid confusion, update to FlatGrind* names.
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(f).read()) for f in ['src/dmccodegui/screens/run.py','src/dmccodegui/screens/axes_setup.py','src/dmccodegui/screens/parameters.py','src/dmccodegui/screens/flat_grind_widgets.py','src/dmccodegui/screens/__init__.py','src/dmccodegui/main.py']]; print('All wrappers parse OK')"</automated>
  </verify>
  <done>Old screen files are thin re-export wrappers; main.py KV_FILES excludes old kv paths; base.kv uses FlatGrind* class names with canonical screen names preserved; screens/__init__.py exports FlatGrind* classes</done>
</task>

<task type="auto">
  <name>Task 2: Update all test imports to FlatGrind* names and verify full suite passes</name>
  <files>
    tests/test_run_screen.py,
    tests/test_axes_setup.py,
    tests/test_parameters.py,
    tests/test_delta_c_bar_chart.py,
    tests/test_flat_grind_widgets.py,
    tests/test_base_classes.py
  </files>
  <action>
Update all test files to import from the new canonical locations using FlatGrind* class names. Follow the import migration map from 19-RESEARCH.md.

**For each test file:**

1. **test_run_screen.py** (~25 import statements):
   - `from dmccodegui.screens.run import RunScreen` -> `from dmccodegui.screens.flat_grind.run import FlatGrindRunScreen`
   - All test functions/classes that reference `RunScreen` -> `FlatGrindRunScreen`
   - `from dmccodegui.screens.run import PLOT_BUFFER_SIZE` -> `from dmccodegui.screens.flat_grind.run import PLOT_BUFFER_SIZE`
   - Any `from dmccodegui.screens.run import DELTA_C_ARRAY_SIZE` -> `from dmccodegui.screens.flat_grind.widgets import DELTA_C_ARRAY_SIZE`
   - Patch targets: `dmccodegui.screens.run.X` -> `dmccodegui.screens.flat_grind.run.X` (for any patches using the old module path)
   - CRITICAL: Check for `@patch('dmccodegui.screens.run.jobs')` or similar — these patch targets reference the module where the name is LOOKED UP at runtime. Since FlatGrindRunScreen now lives in flat_grind.run, patches must target `dmccodegui.screens.flat_grind.run.X`. HOWEVER, if the patched name (like `submit`) comes from base.py module-level import, the patch target is still `dmccodegui.screens.base.submit` — verify each patch target.

2. **test_axes_setup.py** (~30 import statements):
   - `from dmccodegui.screens.axes_setup import AxesSetupScreen` -> `from dmccodegui.screens.flat_grind.axes_setup import FlatGrindAxesSetupScreen`
   - All `AxesSetupScreen` references -> `FlatGrindAxesSetupScreen`
   - Constants: `AXIS_CPM_DEFAULTS`, `AXIS_LABELS`, `AXIS_COLORS`, `_AXIS_ROW_IDS` -> from `dmccodegui.screens.flat_grind.axes_setup`
   - Patch targets that reference `dmccodegui.screens.axes_setup.X` -> update if the name is defined in the flat_grind module. Patch targets referencing `dmccodegui.screens.base.submit` or `dmccodegui.screens.base.mc` stay unchanged.

3. **test_parameters.py** (~20 import statements):
   - `from dmccodegui.screens.parameters import ParametersScreen, PARAM_DEFS` -> `from dmccodegui.screens.flat_grind.parameters import FlatGrindParametersScreen, PARAM_DEFS`
   - All `ParametersScreen` references -> `FlatGrindParametersScreen`
   - Patch targets referencing `dmccodegui.screens.base.submit` and `dmccodegui.screens.base.mc` stay unchanged (base module-level imports).

4. **test_delta_c_bar_chart.py** (~6 import statements):
   - `from dmccodegui.screens.run import DELTA_C_ARRAY_SIZE, DELTA_C_STEP` -> `from dmccodegui.screens.flat_grind.widgets import DELTA_C_ARRAY_SIZE, DELTA_C_STEP`
   - `from dmccodegui.screens.flat_grind_widgets import DeltaCBarChart` -> `from dmccodegui.screens.flat_grind.widgets import DeltaCBarChart`

5. **test_flat_grind_widgets.py** (~3 import statements):
   - `from dmccodegui.screens.flat_grind_widgets import ...` -> `from dmccodegui.screens.flat_grind.widgets import ...`

6. **test_base_classes.py** (~8 import statements):
   - Update any imports that reference `dmccodegui.screens.run.RunScreen` -> `dmccodegui.screens.flat_grind.run.FlatGrindRunScreen`
   - Same for AxesSetupScreen and ParametersScreen references
   - Base class imports from `dmccodegui.screens.base` stay unchanged

**After updating all imports, run the full test suite:**
```bash
python -m pytest tests/ -x --tb=short
```

Fix any import errors or patch target mismatches iteratively. The re-export wrappers from Task 1 ensure old paths still resolve, but tests should use canonical new paths per the locked decision.

**Add new tests for Phase 19 verification:**
- Add a test (in test_flat_grind_widgets.py or test_base_classes.py) that greps all .kv files under ui/ for duplicate `<ClassName>:` headers and asserts zero duplicates found.
- Add a test that verifies FlatGrindRunScreen, FlatGrindAxesSetupScreen, FlatGrindParametersScreen can each be imported from screens.flat_grind.
  </action>
  <verify>
    <automated>python -m pytest tests/ -x --tb=short</automated>
  </verify>
  <done>All test files import from screens.flat_grind with FlatGrind* names; full test suite passes (same pass/fail count as before Phase 19); new KV collision test passes; no duplicate KV rule headers across all .kv files</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/ -x --tb=short` — full suite passes (same baseline as pre-Phase 19)
2. `grep -rh "^<[A-Z]" src/dmccodegui/ui/ | sort | uniq -d` — returns empty (no duplicate KV rule headers)
3. `python -c "from dmccodegui.screens.flat_grind import FlatGrindRunScreen, FlatGrindAxesSetupScreen, FlatGrindParametersScreen; print('Package imports OK')"` — succeeds
4. `python -c "from dmccodegui.screens.run import RunScreen; print(RunScreen.__name__)"` — prints "FlatGrindRunScreen" (re-export works, name reflects real class)
5. `grep -c "ui/run.kv\|ui/axes_setup.kv\|ui/parameters.kv" src/dmccodegui/main.py` — returns 0 (old KV paths removed from KV_FILES)
6. `grep "FlatGrindRunScreen" src/dmccodegui/ui/base.kv` — returns match (base.kv uses new name)
</verification>

<success_criteria>
- Old screen .py files are thin re-export wrappers (< 20 lines each)
- main.py KV_FILES does not include run.kv, axes_setup.kv, or parameters.kv
- base.kv ScreenManager uses FlatGrind* class names with canonical name= values
- screens/__init__.py exports FlatGrind* classes
- All 6 test files import from screens.flat_grind with FlatGrind* names
- Full test suite passes with same pass/fail baseline as before Phase 19
- No duplicate KV rule headers across all .kv files (grep confirms)
- Re-export wrappers allow old import paths to still resolve (safety net)
</success_criteria>

<output>
After completion, create `.planning/phases/19-flat-grind-rename-and-kv-split/19-02-SUMMARY.md`
</output>
