---
phase: 21-serration-screen-set
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dmccodegui/screens/serration/__init__.py
  - src/dmccodegui/screens/serration/axes_setup.py
  - src/dmccodegui/screens/serration/parameters.py
  - src/dmccodegui/screens/serration/widgets.py
  - src/dmccodegui/ui/serration/axes_setup.kv
  - src/dmccodegui/ui/serration/parameters.kv
  - src/dmccodegui/machine_config.py
  - src/dmccodegui/hmi/dmc_vars.py
  - tests/test_serration_screens.py
autonomous: true
requirements:
  - SERR-02
  - SERR-03

must_haves:
  truths:
    - "SerrationAxesSetupScreen shows only A, B, C axis controls — D-axis absent"
    - "SerrationParametersScreen writes only _SERRATION_PARAM_DEFS vars including numSerr"
    - "Serration registry points to real Serration classes (not FlatGrind placeholders)"
  artifacts:
    - path: "src/dmccodegui/screens/serration/__init__.py"
      provides: "Package init with load_kv() and class exports"
      contains: "load_kv"
    - path: "src/dmccodegui/screens/serration/axes_setup.py"
      provides: "SerrationAxesSetupScreen thin subclass"
      contains: "class SerrationAxesSetupScreen"
    - path: "src/dmccodegui/screens/serration/parameters.py"
      provides: "SerrationParametersScreen thin subclass"
      contains: "class SerrationParametersScreen"
    - path: "src/dmccodegui/screens/serration/widgets.py"
      provides: "BCompPanel widget skeleton"
      contains: "class BCompPanel"
    - path: "src/dmccodegui/ui/serration/axes_setup.kv"
      provides: "Serration axes setup KV layout (3-axis, no D)"
      contains: "SerrationAxesSetupScreen"
    - path: "src/dmccodegui/ui/serration/parameters.kv"
      provides: "Serration parameters KV layout"
      contains: "SerrationParametersScreen"
    - path: "tests/test_serration_screens.py"
      provides: "Test scaffold for all SERR requirements"
  key_links:
    - from: "src/dmccodegui/machine_config.py"
      to: "src/dmccodegui/screens/serration/__init__.py"
      via: "registry screen_classes dotted paths"
      pattern: "dmccodegui\\.screens\\.serration"
    - from: "src/dmccodegui/screens/serration/axes_setup.py"
      to: "src/dmccodegui/screens/base.py"
      via: "class inheritance"
      pattern: "BaseAxesSetupScreen"
    - from: "src/dmccodegui/screens/serration/parameters.py"
      to: "src/dmccodegui/screens/base.py"
      via: "class inheritance"
      pattern: "BaseParametersScreen"
---

<objective>
Create the Serration screen package skeleton with thin subclasses for AxesSetup and Parameters, the BCompPanel widget skeleton, KV files, registry update, and test scaffold.

Purpose: Establish the `screens/serration/` package structure mirroring `screens/flat_grind/`, wire the registry to real Serration classes, and create the test scaffold that Plan 02 (Run screen) depends on. AxesSetup and Parameters are trivially thin subclasses that inherit nearly everything from base classes.

Output: Working Serration package with importable classes, KV files loaded without collision, registry pointing to real Serration classes, numSerr added to param_defs.
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-serration-screen-set/21-CONTEXT.md
@.planning/phases/21-serration-screen-set/21-RESEARCH.md

@src/dmccodegui/screens/flat_grind/__init__.py
@src/dmccodegui/screens/flat_grind/axes_setup.py
@src/dmccodegui/screens/flat_grind/parameters.py
@src/dmccodegui/screens/flat_grind/widgets.py
@src/dmccodegui/screens/base.py
@src/dmccodegui/machine_config.py
@src/dmccodegui/hmi/dmc_vars.py
@src/dmccodegui/ui/flat_grind/axes_setup.kv
@src/dmccodegui/ui/flat_grind/parameters.kv

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/dmccodegui/screens/base.py:
- class BaseAxesSetupScreen(SetupScreenMixin, Screen): controller/state ObjectProperty, jog_axis, _rebuild_axis_rows, CPM read, teach_rest_point/teach_start_point
- class BaseParametersScreen(SetupScreenMixin, Screen): controller/state ObjectProperty, build_param_cards, validate_field, _dirty tracking, apply_to_controller, read_from_controller, _rebuild_for_machine_type
- class SetupScreenMixin: _SETUP_SCREENS frozenset, _enter_setup_if_needed, _exit_setup_if_needed

From src/dmccodegui/screens/flat_grind/__init__.py:
- load_kv() -> None: deferred KV loading pattern (copy exactly)
- Exports: FlatGrindRunScreen, FlatGrindAxesSetupScreen, FlatGrindParametersScreen

From src/dmccodegui/machine_config.py:
- _REGISTRY["3-Axes Serration Grind"]: currently points to FlatGrind placeholders, needs screen_classes + load_kv updated
- _SERRATION_PARAM_DEFS: List[Dict] — already built (Flat minus D-axis vars), needs numSerr added
- get_param_defs() -> List[Dict]: returns param_defs for active type
- get_axis_list() -> List[str]: returns ["A","B","C"] for Serration

From src/dmccodegui/screens/flat_grind/parameters.py:
- class FlatGrindParametersScreen(BaseParametersScreen): pending_count, on_field_text_change, _update_apply_button, _apply_role_mode, _rebuild_for_machine_type, on_pre_enter (subscribe _btn_unsub), on_leave (unsubscribe _btn_unsub)

From src/dmccodegui/screens/flat_grind/axes_setup.py:
- class FlatGrindAxesSetupScreen(BaseAxesSetupScreen): _rebuild_axis_rows (reads mc.get_axis_list, hides D via opacity/disabled/size_hint_y=0)
- AXIS_CPM_DEFAULTS, AXIS_LABELS, AXIS_COLORS, _AXIS_ROW_IDS — module constants
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create serration package, thin subclasses, KV files, and registry update</name>
  <files>
    src/dmccodegui/screens/serration/__init__.py
    src/dmccodegui/screens/serration/axes_setup.py
    src/dmccodegui/screens/serration/parameters.py
    src/dmccodegui/screens/serration/widgets.py
    src/dmccodegui/ui/serration/axes_setup.kv
    src/dmccodegui/ui/serration/parameters.kv
    src/dmccodegui/machine_config.py
    src/dmccodegui/hmi/dmc_vars.py
  </files>
  <action>
    **Create directories:**
    - `src/dmccodegui/screens/serration/` (Python package)
    - `src/dmccodegui/ui/serration/` (KV files)

    **Create `screens/serration/__init__.py`:**
    - Copy exact pattern from `screens/flat_grind/__init__.py`
    - `_UI_DIR` points to `ui/serration/`
    - `load_kv()` loads `run.kv`, `axes_setup.kv`, `parameters.kv` (idempotent)
    - Imports: `SerrationRunScreen` from `.run`, `SerrationAxesSetupScreen` from `.axes_setup`, `SerrationParametersScreen` from `.parameters`
    - `__all__` exports all three classes + `load_kv`
    - NOTE: `run.py` does not exist yet (Plan 02 creates it), so create a minimal stub `run.py` that defines `class SerrationRunScreen(BaseRunScreen): pass` — enough for the import to succeed. Plan 02 will replace this with the full implementation.

    **Create `screens/serration/axes_setup.py`:**
    - Copy `flat_grind/axes_setup.py` as starting point
    - Rename class to `SerrationAxesSetupScreen(BaseAxesSetupScreen)`
    - Keep all the same module constants: `AXIS_CPM_DEFAULTS`, `AXIS_LABELS`, `AXIS_COLORS`, `_AXIS_ROW_IDS`
    - The class body is identical to FlatGrindAxesSetupScreen — `_rebuild_axis_rows()` already uses `mc.get_axis_list()` which returns `["A","B","C"]` for Serration, hiding D automatically
    - Do NOT import from flat_grind — copy the code for full independence per user decision

    **Create `screens/serration/parameters.py`:**
    - Copy `flat_grind/parameters.py` as starting point
    - Rename class to `SerrationParametersScreen(BaseParametersScreen)`
    - Keep identical behavior: `pending_count`, `on_field_text_change`, `_update_apply_button`, `_apply_role_mode`, `_rebuild_for_machine_type`, `on_pre_enter` (with `_btn_unsub`), `on_leave` (unsubscribe `_btn_unsub`), `on_kv_post`
    - Update the backward-compat re-export line: import `_SERRATION_PARAM_DEFS` instead of `_FLAT_PARAM_DEFS` for the module-level `PARAM_DEFS` name
    - Full copy — do NOT import from flat_grind

    **Create `screens/serration/widgets.py`:**
    - Skeleton file with `BCompPanel` class definition (enough to import)
    - `BCompPanel` inherits from `kivy.uix.boxlayout.BoxLayout` (not Widget — needs layout)
    - Properties: `num_serrations = NumericProperty(0)`
    - Constants: `BCOMP_MIN_MM = -5.0`, `BCOMP_MAX_MM = 5.0`
    - Named constant: `BCOMP_ARRAY_VAR = "bComp"` with TODO comment for hardware verification
    - Named constant: `BCOMP_NUM_SERR_VAR = "numSerr"` with TODO comment for hardware verification
    - Stub methods: `build_rows(self, values: list[float])`, `_on_save(self, index, text)` — Plan 02 fills in full implementation
    - This is the skeleton only — Plan 02 adds the full scrollable list implementation

    **Create `ui/serration/axes_setup.kv`:**
    - Copy `ui/flat_grind/axes_setup.kv`
    - Replace `<FlatGrindAxesSetupScreen>:` with `<SerrationAxesSetupScreen>:`
    - Remove the `axis_row_d` widget entirely (do NOT keep it hidden — Serration KV should not contain D-axis widgets at all; `_rebuild_axis_rows()` calls `self.ids.get("axis_row_d")` which returns None gracefully)
    - Update any `#:import` directives to reference `dmccodegui.screens.serration.*` instead of `flat_grind`

    **Create `ui/serration/parameters.kv`:**
    - Copy `ui/flat_grind/parameters.kv`
    - Replace `<FlatGrindParametersScreen>:` with `<SerrationParametersScreen>:`
    - Update any `#:import` directives to reference `dmccodegui.screens.serration.*` instead of `flat_grind`

    **Update `machine_config.py`:**
    - Add `numSerr` to `_SERRATION_PARAM_DEFS`:
      ```python
      _SERRATION_PARAM_DEFS.append({
          "label": "Num Serrations",
          "var": "numSerr",
          "unit": "",
          "group": "Geometry",
          "min": 1.0,
          "max": 200.0,
      })
      ```
    - Update `_REGISTRY["3-Axes Serration Grind"]`:
      - `"load_kv": "dmccodegui.screens.serration.load_kv"`
      - `"screen_classes"`: point all three to `dmccodegui.screens.serration.Serration*Screen`
      - Remove the TODO comment

    **Update `hmi/dmc_vars.py`:**
    - Add constants for bComp/numSerr variable names:
      ```python
      # Serration bComp array — TODO: verify name against real Serration DMC program
      BCOMP_ARRAY = "bComp"
      BCOMP_NUM_SERR = "numSerr"
      ```

    **Create minimal stub `screens/serration/run.py`:**
    - Just enough for `__init__.py` import to succeed:
      ```python
      """SerrationRunScreen — stub, replaced by Plan 02."""
      from ..base import BaseRunScreen

      class SerrationRunScreen(BaseRunScreen):
          """Placeholder — Plan 21-02 implements the full run screen."""
          pass
      ```
  </action>
  <verify>
    <automated>python -c "from dmccodegui.screens.serration import SerrationRunScreen, SerrationAxesSetupScreen, SerrationParametersScreen, load_kv; print('All imports OK')"</automated>
  </verify>
  <done>
    - screens/serration/ package exists with __init__.py, run.py (stub), axes_setup.py, parameters.py, widgets.py
    - ui/serration/ directory exists with axes_setup.kv, parameters.kv
    - All three Serration screen classes importable
    - BCompPanel importable from screens.serration.widgets
    - machine_config registry points to real Serration classes
    - numSerr added to _SERRATION_PARAM_DEFS
    - BCOMP_ARRAY and BCOMP_NUM_SERR constants in dmc_vars.py
    - No KV rule name collisions (unique class names in all KV headers)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create test scaffold for all SERR requirements</name>
  <files>tests/test_serration_screens.py</files>
  <action>
    Create `tests/test_serration_screens.py` mirroring `tests/test_flat_grind_widgets.py` and `tests/test_base_classes.py` patterns.

    **Test functions to create (all should pass immediately against the code from Task 1):**

    1. `test_serration_run_screen_importable` — import succeeds, is subclass of BaseRunScreen
    2. `test_serration_axes_setup_importable` — import succeeds, is subclass of BaseAxesSetupScreen
    3. `test_serration_axes_setup_inherits_base` — verify class MRO includes BaseAxesSetupScreen
    4. `test_serration_params_importable` — import succeeds, is subclass of BaseParametersScreen
    5. `test_serration_param_defs_has_numserr` — `mc.get_param_defs("3-Axes Serration Grind")` contains a dict with `"var": "numSerr"`
    6. `test_serration_params_no_d_axis_vars` — none of the D-axis vars (fdD, pitchD, ratioD, ctsRevD) appear in Serration param_defs
    7. `test_bcomp_panel_importable` — `from dmccodegui.screens.serration.widgets import BCompPanel` succeeds
    8. `test_registry_points_to_serration_classes` — `_REGISTRY["3-Axes Serration Grind"]["screen_classes"]` values contain "serration" not "flat_grind"
    9. `test_serration_axes_setup_kv_no_d_axis` — read `ui/serration/axes_setup.kv`, assert `axis_row_d` does not appear in file text
    10. `test_dmc_vars_bcomp_constants` — `from dmccodegui.hmi.dmc_vars import BCOMP_ARRAY, BCOMP_NUM_SERR` succeeds, values are strings

    **Setup pattern:** Use the same Kivy env setup as `test_flat_grind_widgets.py` (KIVY_NO_ENV_CONFIG, KIVY_LOG_LEVEL inside test functions or a module-level fixture). Use `mc.init()` with a temp settings path for machine_config tests.

    All tests should be lightweight (no Kivy App instantiation needed — just imports, class checks, file reads, and machine_config queries).
  </action>
  <verify>
    <automated>pytest tests/test_serration_screens.py -x -v</automated>
  </verify>
  <done>
    - 10 test functions in test_serration_screens.py
    - All 10 tests pass
    - Existing test suite still passes: pytest tests/ -x
    - test_no_duplicate_kv_rule_headers (in test_flat_grind_widgets.py) passes with new KV files
  </done>
</task>

</tasks>

<verification>
- `python -c "from dmccodegui.screens.serration import SerrationRunScreen, SerrationAxesSetupScreen, SerrationParametersScreen, load_kv"` — all imports succeed
- `pytest tests/test_serration_screens.py -x -v` — all tests pass
- `pytest tests/test_flat_grind_widgets.py::test_no_duplicate_kv_rule_headers -x` — no KV rule name collisions
- `pytest tests/ -x` — full suite green (no regressions)
</verification>

<success_criteria>
- Serration package structure mirrors flat_grind: __init__.py, run.py (stub), axes_setup.py, parameters.py, widgets.py
- KV files exist with correct class name headers (no collisions)
- Registry updated — "3-Axes Serration Grind" points to real Serration classes
- numSerr in _SERRATION_PARAM_DEFS with group "Geometry", min 1, max 200
- BCOMP_ARRAY and BCOMP_NUM_SERR constants in dmc_vars.py
- All 10 tests pass, full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/21-serration-screen-set/21-01-SUMMARY.md`
</output>
