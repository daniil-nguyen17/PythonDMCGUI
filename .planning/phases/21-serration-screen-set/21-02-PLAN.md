---
phase: 21-serration-screen-set
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - src/dmccodegui/screens/serration/run.py
  - src/dmccodegui/screens/serration/widgets.py
  - src/dmccodegui/ui/serration/run.kv
  - tests/test_serration_screens.py
autonomous: true
requirements:
  - SERR-01
  - SERR-04

must_haves:
  truths:
    - "Serration Run screen renders with 3-axis position labels (A, B, C) and no D-axis elements"
    - "bComp panel displays scrollable list of editable fields sized by numSerr"
    - "bComp panel reads array values on screen enter and writes one element at a time on Save"
    - "More/less stone arrows and startPtC value display work identically to Flat Grind"
    - "Plot area shows a placeholder stub (not matplotlib)"
  artifacts:
    - path: "src/dmccodegui/screens/serration/run.py"
      provides: "Full SerrationRunScreen implementation"
      contains: "class SerrationRunScreen"
      min_lines: 80
    - path: "src/dmccodegui/screens/serration/widgets.py"
      provides: "Full BCompPanel scrollable list widget"
      contains: "build_rows"
      min_lines: 50
    - path: "src/dmccodegui/ui/serration/run.kv"
      provides: "Serration run screen KV layout"
      contains: "SerrationRunScreen"
  key_links:
    - from: "src/dmccodegui/screens/serration/run.py"
      to: "src/dmccodegui/screens/serration/widgets.py"
      via: "BCompPanel instantiation and callback wiring"
      pattern: "BCompPanel"
    - from: "src/dmccodegui/screens/serration/run.py"
      to: "src/dmccodegui/screens/base.py"
      via: "class inheritance"
      pattern: "BaseRunScreen"
    - from: "src/dmccodegui/screens/serration/run.py"
      to: "src/dmccodegui/hmi/dmc_vars.py"
      via: "import constants"
      pattern: "BCOMP_ARRAY|BCOMP_NUM_SERR|HMI_MORE|HMI_LESS|STARTPT_C"
---

<objective>
Implement the full SerrationRunScreen with live bComp panel, more/less stone controls, 3-axis position display, and plot stub. Replace the Plan 01 run.py stub with a complete implementation.

Purpose: Deliver the core new functionality for Serration machines — the bComp panel is the primary differentiator from Flat Grind. The run screen is where operators spend most of their time.

Output: Complete SerrationRunScreen with functional bComp read/write, cycle controls, position display, and plot stub. All SERR requirements satisfied.
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-serration-screen-set/21-CONTEXT.md
@.planning/phases/21-serration-screen-set/21-RESEARCH.md
@.planning/phases/21-serration-screen-set/21-01-SUMMARY.md

@src/dmccodegui/screens/flat_grind/run.py
@src/dmccodegui/screens/flat_grind/widgets.py
@src/dmccodegui/screens/base.py
@src/dmccodegui/hmi/dmc_vars.py
@src/dmccodegui/ui/flat_grind/run.kv

<interfaces>
<!-- Key types from Plan 01 that this plan builds on -->

From src/dmccodegui/screens/serration/widgets.py (Plan 01 skeleton):
- class BCompPanel(BoxLayout): num_serrations NumericProperty, build_rows(), _on_save()
- BCOMP_MIN_MM, BCOMP_MAX_MM, BCOMP_ARRAY_VAR, BCOMP_NUM_SERR_VAR

From src/dmccodegui/screens/base.py:
- class BaseRunScreen(Screen): controller/state ObjectProperty, _state_unsub, cleanup(), on_pre_enter/on_leave lifecycle
- submit() — module-level re-export from utils.jobs for background I/O

From src/dmccodegui/hmi/dmc_vars.py:
- HMI_MORE, HMI_LESS, HMI_TRIGGER_FIRE, STARTPT_C
- STATE_GRINDING, STATE_HOMING
- BCOMP_ARRAY, BCOMP_NUM_SERR (added in Plan 01)

From src/dmccodegui/screens/flat_grind/run.py:
- FlatGrindRunScreen: reference for cycle controls, more/less stone, state subscription pattern
- Properties to keep: pos_a, pos_b, pos_c, cycle_running, motion_active, start_pt_c, status_text
- Properties to strip: pos_d, cpm_d, delta_c_offsets, _fig (matplotlib)
- Methods to keep: _on_state_change, _apply_state, _on_start_grind, _on_stop, _on_more_stone, _on_less_stone
- Methods to strip: _init_plot, _tick_plot, _read_delta_c, anything matplotlib-related
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement BCompPanel widget and SerrationRunScreen</name>
  <files>
    src/dmccodegui/screens/serration/widgets.py
    src/dmccodegui/screens/serration/run.py
  </files>
  <action>
    **Complete `screens/serration/widgets.py` — BCompPanel full implementation:**

    Replace Plan 01 skeleton with full scrollable list widget:
    - Inherits from `BoxLayout` (orientation='vertical')
    - Header area: `Label` showing "Serrations: N" (bound to `num_serrations` property) + "Read bComp" refresh `Button`
    - Scrollable body: `ScrollView` containing `GridLayout(cols=3, size_hint_y=None)` with `bind(minimum_height=grid.setter('height'))`
    - Each row: index `Label` (col 0) + `TextInput` with mm value (col 1, `multiline=False`, `input_filter='float'`) + "Save" `Button` (col 2)
    - Row height: `size_hint_y=None, height=dp(44)` for each cell
    - `build_rows(self, values: list[float])` — clears grid, rebuilds rows from values list, sets `num_serrations = len(values)`
    - `_on_save(self, index: int, text: str)` — validates value against `BCOMP_MIN_MM` / `BCOMP_MAX_MM`, calls `self.save_callback(index, value_mm)` if valid, flashes red on invalid
    - `save_callback` — a `Callable[[int, float], None]` set by the parent screen (SerrationRunScreen wires this to `_write_bcomp_element`)
    - `refresh_callback` — a `Callable[[], None]` set by the parent screen, bound to the "Read bComp" button
    - Keep constants: `BCOMP_MIN_MM = -5.0`, `BCOMP_MAX_MM = 5.0`, `BCOMP_ARRAY_VAR`, `BCOMP_NUM_SERR_VAR`

    **Replace `screens/serration/run.py` stub with full SerrationRunScreen:**

    Copy `flat_grind/run.py` as starting point, then modify:

    1. **Strip D-axis:** Remove `pos_d`, `cpm_d` properties entirely. Remove any D-axis references from `_apply_state`.

    2. **Strip matplotlib:** Remove all matplotlib imports (`Figure`, `MaxNLocator`, `pyplot`, `kivy_matplotlib_widget`). Remove `_fig`, `_axes`, `_line`, `_plot_buf_*` attributes. Remove `_init_plot`, `_tick_plot`, `_configure_plot_axes` methods. Do NOT import matplotlib at all.

    3. **Strip DeltaC:** Remove all DeltaC bar chart imports and references. Remove `delta_c_offsets`, `_read_delta_c`, and any DeltaCBarChart widget interaction.

    4. **Add bComp properties and methods:**
       - `num_serr = NumericProperty(0)` — number of serrations
       - `num_serr_str = StringProperty("Serrations: --")` — display string
       - `_bcomp_values: list[float] = []` — current values cache
       - `_bcomp_panel: BCompPanel | None = None` — reference to panel widget (found via `self.ids` from KV)

    5. **Add `_read_bcomp(self)` method:**
       - Background job via `submit()`: reads `numSerr` from controller, then reads `bComp[0]` through `bComp[numSerr-1]` one at a time
       - Uses `BCOMP_ARRAY` and `BCOMP_NUM_SERR` constants from `dmc_vars.py`
       - On completion, `Clock.schedule_once` updates: `self.num_serr = n`, `self.num_serr_str = f"Serrations: {n}"`, `self._bcomp_values = values`, `self._bcomp_panel.build_rows(values)`
       - Logs warning if read returns no data (hardware verification flag)
       - Uses `# TODO: verify bComp array name against real Serration DMC program` comment

    6. **Add `_write_bcomp_element(self, index: int, value_mm: float)` method:**
       - Background job via `submit()`: sends `bComp[{index}]={value_mm:.4f}` to controller
       - Uses `BCOMP_ARRAY` constant
       - Pattern matches deltaC individual element write in flat_grind/run.py

    7. **Keep cycle controls:** `_on_start_grind`, `_on_stop`, `_on_more_stone`, `_on_less_stone` — identical logic using `HMI_GRND`, `HMI_MORE`, `HMI_LESS`, `STARTPT_C`

    8. **Keep state subscription:** `_on_state_change` / `_apply_state` — updates `pos_a`, `pos_b`, `pos_c` (NOT `pos_d`), `cycle_running`, `motion_active`, `start_pt_c`, `status_text`

    9. **Wire bComp panel in `on_pre_enter`:**
       - After base `on_pre_enter`, find `_bcomp_panel` via `self.ids.get('bcomp_panel')`
       - Wire callbacks: `panel.save_callback = self._write_bcomp_element`, `panel.refresh_callback = self._read_bcomp_job`
       - Call `_read_bcomp_job()` to auto-read on screen enter (wraps `_read_bcomp` in `submit()`)

    10. **No matplotlib in cleanup():** `BaseRunScreen.cleanup()` already checks `if fig is not None` — since `_fig` is never set, cleanup is safe. Do NOT override cleanup.

    **CRITICAL — do NOT reuse `_BaseBarChart` from flat_grind widgets.** The bComp panel is a list, not a bar chart. Per user decision.

    **CRITICAL — all lifecycle hooks in Python only, never in KV.** Per Kivy #2565.

    **CRITICAL — all controller I/O via `submit()` / `Clock.schedule_once()`.** Per gclib comms architecture.
  </action>
  <verify>
    <automated>python -c "from dmccodegui.screens.serration.run import SerrationRunScreen; s=SerrationRunScreen(); print('pos_a' in s.properties()); print('pos_d' not in s.properties()); print('Has bcomp:', hasattr(s, '_read_bcomp'))"</automated>
  </verify>
  <done>
    - SerrationRunScreen has pos_a, pos_b, pos_c but NOT pos_d
    - SerrationRunScreen has _read_bcomp and _write_bcomp_element methods
    - No matplotlib imports in serration/run.py
    - No DeltaC references in serration/run.py
    - BCompPanel has full scrollable list with build_rows, _on_save, validation
    - BCompPanel has save_callback and refresh_callback for parent screen wiring
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Serration run.kv and complete test coverage</name>
  <files>
    src/dmccodegui/ui/serration/run.kv
    tests/test_serration_screens.py
  </files>
  <action>
    **Create `ui/serration/run.kv`:**

    Copy `ui/flat_grind/run.kv` as starting point, then modify:

    1. Replace `<FlatGrindRunScreen>:` with `<SerrationRunScreen>:`
    2. Update all `#:import` directives to reference `dmccodegui.screens.serration.*`
    3. **Remove D-axis position row:** Delete the `pos_d` label row entirely (clean 3-axis layout)
    4. **Remove DeltaCBarChart widget:** Delete the entire delta_c chart area
    5. **Remove matplotlib/MatplotFigure widget:** Replace the `ab_plot` area with a plot stub — a `BoxLayout` with a `Label` saying "Plot: pending hardware data" inside a panel-colored background box. Use `id: plot_stub`
    6. **Add BCompPanel widget:** Below the plot stub area, full width. Use `id: bcomp_panel`. Import BCompPanel: `#:import BCompPanel dmccodegui.screens.serration.widgets.BCompPanel`
    7. **Add numSerr display:** Near/in the bComp panel header area, show `root.num_serr_str` as read-only label
    8. **Keep cycle controls:** Start, Stop buttons, session/station counters (`ct_ses_kni`, `ct_stn_kni`), progress area, controller message log area — same as Flat Grind
    9. **Keep More/Less stone panel:** Up/down arrow buttons (on_release calls `root._on_more_stone()` / `root._on_less_stone()`), `startPtC` value label showing `root.start_pt_c`. No tip/heel labels (simpler than Flat Grind)
    10. **Keep position display:** 3 rows for A, B, C axes with accent colors (A=orange, B=purple, C=cyan)

    **Add remaining tests to `tests/test_serration_screens.py`:**

    Add these tests (in addition to the 10 from Plan 01):

    11. `test_serration_run_screen_no_d_axis` — `SerrationRunScreen()` has no `pos_d` property (verify `'pos_d' not in SerrationRunScreen.__dict__` and not in properties)
    12. `test_serration_run_screen_has_bcomp_methods` — verify `_read_bcomp` and `_write_bcomp_element` are callable methods
    13. `test_serration_run_screen_no_matplotlib` — verify `serration/run.py` source file does not contain `import matplotlib` (read file text and assert)
    14. `test_bcomp_panel_renders_rows` — create `BCompPanel()`, call `build_rows([0.1, 0.2, 0.3])`, verify `num_serrations == 3`
    15. `test_bcomp_panel_validation` — verify `BCOMP_MIN_MM` and `BCOMP_MAX_MM` are floats with min < 0 < max
    16. `test_serration_run_kv_no_d_axis` — read `ui/serration/run.kv` text, assert `pos_d` does not appear
    17. `test_serration_run_kv_no_matplotlib` — read `ui/serration/run.kv` text, assert `MatplotFigure` does not appear
    18. `test_serration_run_kv_has_bcomp` — read `ui/serration/run.kv` text, assert `bcomp_panel` id appears

    All tests should pass without requiring a running Kivy App. Use file reads for KV checks, simple instantiation for widget checks.
  </action>
  <verify>
    <automated>pytest tests/test_serration_screens.py -x -v</automated>
  </verify>
  <done>
    - ui/serration/run.kv exists with SerrationRunScreen rule, no D-axis, no matplotlib, bComp panel
    - 18 total tests in test_serration_screens.py, all passing
    - Full test suite green: pytest tests/ -x
    - No KV rule name collisions (test_no_duplicate_kv_rule_headers passes)
    - SERR-01 satisfied: SerrationRunScreen is distinct from FlatGrind, D-axis absent
    - SERR-04 satisfied: bComp panel is functional with read/write/validation
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_serration_screens.py -x -v` — all 18 tests pass
- `pytest tests/test_flat_grind_widgets.py::test_no_duplicate_kv_rule_headers -x` — no KV collisions
- `pytest tests/ -x` — full suite green
- `python -c "from dmccodegui.screens.serration import SerrationRunScreen; print(hasattr(SerrationRunScreen, 'pos_d'))"` prints `False`
- `python -c "from dmccodegui.screens.serration.widgets import BCompPanel; p = BCompPanel(); p.build_rows([1.0, 2.0]); print(p.num_serrations)"` prints `2`
- `grep -r 'matplotlib' src/dmccodegui/screens/serration/` returns nothing
- `grep -r 'pos_d\|cpm_d' src/dmccodegui/screens/serration/` returns nothing (except comments/TODOs if any)
</verification>

<success_criteria>
- SerrationRunScreen fully implemented: 3-axis positions, cycle controls, more/less stone, bComp panel, plot stub
- No D-axis properties or KV widgets in Serration run screen
- No matplotlib dependencies in Serration run screen
- BCompPanel: scrollable editable list, sized by numSerr, reads on enter, writes one element at a time
- bComp variable name flagged with TODO for hardware verification
- 18 tests all green, full suite regression-free
- All four SERR requirements addressed (SERR-01, SERR-02, SERR-03, SERR-04)
</success_criteria>

<output>
After completion, create `.planning/phases/21-serration-screen-set/21-02-SUMMARY.md`
</output>
