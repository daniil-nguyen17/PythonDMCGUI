---
phase: 22-convex-screen-set
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dmccodegui/screens/convex/__init__.py
  - src/dmccodegui/screens/convex/run.py
  - src/dmccodegui/screens/convex/axes_setup.py
  - src/dmccodegui/screens/convex/parameters.py
  - src/dmccodegui/screens/convex/widgets.py
  - src/dmccodegui/ui/convex/run.kv
  - src/dmccodegui/ui/convex/axes_setup.kv
  - src/dmccodegui/ui/convex/parameters.kv
  - src/dmccodegui/machine_config.py
  - tests/test_convex_screens.py
autonomous: true
requirements: [CONV-02, CONV-03, CONV-04]

must_haves:
  truths:
    - "ConvexAxesSetupScreen shows all four axes (A, B, C, D) with correct labels"
    - "ConvexParametersScreen renders grouped parameter cards from _CONVEX_PARAM_DEFS"
    - "_CONVEX_PARAM_DEFS is an explicit independent list, not a shallow copy of _FLAT_PARAM_DEFS"
    - "Registry entry for '4-Axes Convex Grind' points to real Convex classes, not Flat Grind placeholders"
  artifacts:
    - path: "src/dmccodegui/screens/convex/__init__.py"
      provides: "Deferred load_kv() and screen class exports"
      exports: ["ConvexRunScreen", "ConvexAxesSetupScreen", "ConvexParametersScreen", "load_kv"]
    - path: "src/dmccodegui/screens/convex/axes_setup.py"
      provides: "ConvexAxesSetupScreen class"
      contains: "class ConvexAxesSetupScreen"
    - path: "src/dmccodegui/screens/convex/parameters.py"
      provides: "ConvexParametersScreen class"
      contains: "class ConvexParametersScreen"
    - path: "src/dmccodegui/screens/convex/widgets.py"
      provides: "ConvexAdjustPanel placeholder widget"
      contains: "class ConvexAdjustPanel"
    - path: "src/dmccodegui/ui/convex/axes_setup.kv"
      provides: "4-axis axes setup layout"
      contains: "axis_row_d"
    - path: "src/dmccodegui/machine_config.py"
      provides: "Explicit _CONVEX_PARAM_DEFS and updated registry"
      contains: "Placeholder"
    - path: "tests/test_convex_screens.py"
      provides: "Test scaffold for CONV requirements"
      min_lines: 100
  key_links:
    - from: "src/dmccodegui/screens/convex/__init__.py"
      to: "src/dmccodegui/ui/convex/*.kv"
      via: "Builder.load_file() in load_kv()"
      pattern: "Builder\\.load_file"
    - from: "src/dmccodegui/machine_config.py"
      to: "src/dmccodegui/screens/convex"
      via: "Registry screen_classes dotted paths"
      pattern: "dmccodegui\\.screens\\.convex"
---

<objective>
Create the Convex screen package skeleton with thin subclasses (ConvexAxesSetupScreen, ConvexParametersScreen), a stub ConvexRunScreen, the ConvexAdjustPanel placeholder widget, all three KV files, the registry update in machine_config.py, the explicit _CONVEX_PARAM_DEFS, and a test scaffold covering CONV-02 through CONV-04.

Purpose: Establish the convex package structure so Plan 02 can fill in the full ConvexRunScreen implementation against a working skeleton with passing tests.
Output: Importable convex screen package, 3 KV files, updated registry, test file with ~12 tests passing.
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-convex-screen-set/22-CONTEXT.md
@.planning/phases/22-convex-screen-set/22-RESEARCH.md

@src/dmccodegui/screens/flat_grind/__init__.py
@src/dmccodegui/screens/flat_grind/axes_setup.py
@src/dmccodegui/screens/flat_grind/parameters.py
@src/dmccodegui/screens/flat_grind/widgets.py
@src/dmccodegui/ui/flat_grind/axes_setup.kv
@src/dmccodegui/ui/flat_grind/parameters.kv
@src/dmccodegui/machine_config.py
@tests/test_serration_screens.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/dmccodegui/screens/base.py:
- class BaseRunScreen(Screen) — base for all run screens
- class BaseAxesSetupScreen(SetupScreenMixin, Screen) — base for axes setup screens
- class BaseParametersScreen(SetupScreenMixin, Screen) — base for parameters screens
- class SetupScreenMixin — mixin with _enter_setup_if_needed / _exit_setup_if_needed

From src/dmccodegui/screens/flat_grind/__init__.py:
- load_kv() pattern: idempotent, loads 3 KV files from ui/{machine}/ dir
- _PKG_DIR / _UI_DIR path resolution pattern
- Exports after load_kv definition, with noqa: E402

From src/dmccodegui/machine_config.py:
- _FLAT_PARAM_DEFS: List[Dict] — 20 entries (Geometry, Feedrates, Calibration groups)
- _CONVEX_PARAM_DEFS: currently `[d.copy() for d in _FLAT_PARAM_DEFS]` — MUST be replaced with explicit list literal
- _REGISTRY["4-Axes Convex Grind"] — currently points to flat_grind classes — MUST be updated
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convex package, thin subclasses, KV files, registry update</name>
  <files>
    src/dmccodegui/screens/convex/__init__.py
    src/dmccodegui/screens/convex/run.py
    src/dmccodegui/screens/convex/axes_setup.py
    src/dmccodegui/screens/convex/parameters.py
    src/dmccodegui/screens/convex/widgets.py
    src/dmccodegui/ui/convex/run.kv
    src/dmccodegui/ui/convex/axes_setup.kv
    src/dmccodegui/ui/convex/parameters.kv
    src/dmccodegui/machine_config.py
  </files>
  <action>
    **Create `src/dmccodegui/screens/convex/` package** by copying `screens/flat_grind/` and renaming all identifiers from FlatGrind* to Convex*. Full independence — no imports from flat_grind in Python files (per user decision).

    **`__init__.py`**: Copy flat_grind/__init__.py verbatim pattern. Change _UI_DIR to point to `ui/convex`. Load 3 KV files (run.kv, axes_setup.kv, parameters.kv). Export ConvexRunScreen, ConvexAxesSetupScreen, ConvexParametersScreen, load_kv. Idempotent _kv_loaded guard.

    **`run.py`**: Stub class only — `class ConvexRunScreen(BaseRunScreen): pass`. Import from `..base`. Plan 02 replaces with full implementation.

    **`axes_setup.py`**: Copy flat_grind/axes_setup.py. Rename class to ConvexAxesSetupScreen. Keep ALL 4 axes (A, B, C, D) — Convex is 4-axis, same as Flat Grind. Update import path: `from .widgets import ConvexAdjustPanel` is NOT needed here. The only import change is: `from .parameters import PARAM_DEFS` must point to convex parameters. Otherwise identical to flat_grind/axes_setup.py. Do NOT import from flat_grind package.

    **`parameters.py`**: Copy flat_grind/parameters.py. Rename class to ConvexParametersScreen. Re-export: `from dmccodegui.machine_config import _CONVEX_PARAM_DEFS as PARAM_DEFS`.

    **`widgets.py`**: Create ConvexAdjustPanel class — a BoxLayout subclass with vertical orientation. Contains a header Label ("Convex Adjustments", cyan accent color [0.024, 0.714, 0.831, 1], 13sp bold, left-aligned) and a placeholder Label ("Pending customer specs", 12sp, subdued grey [0.396, 0.455, 0.545, 1], center-aligned). Follow the pattern from 22-RESEARCH.md Pattern 3. Include docstring with TODO about replacing placeholder after customer sign-off.

    **Create `src/dmccodegui/ui/convex/` KV files** by copying `ui/flat_grind/` KV files:

    **`axes_setup.kv`**: Copy flat_grind/axes_setup.kv. Change rule header to `<ConvexAxesSetupScreen>:`. Keep ALL 4 axis rows (A, B, C, D) including axis_row_d. Update `#:import` paths: change `flat_grind` to `convex` in any module path references. Keep same accent colors (A=orange, B=purple, C=cyan, D=yellow).

    **`parameters.kv`**: Copy flat_grind/parameters.kv. Change rule header to `<ConvexParametersScreen>:`. Update `#:import` paths from flat_grind to convex.

    **`run.kv`**: Minimal stub KV — just `<ConvexRunScreen>:` with a Label placeholder. Plan 02 replaces with full layout. Required by load_kv() calling Builder.load_file().

    **Update `machine_config.py`**:

    1. Replace `_CONVEX_PARAM_DEFS` shallow copy with explicit list literal. Write out all 20 entries as individual dict literals (copy the exact values from `_FLAT_PARAM_DEFS` above it). Add single-line comment above: `# Placeholder — mirrors Flat Grind, pending customer convex specs.`

    2. Update `_REGISTRY["4-Axes Convex Grind"]`:
       - `"load_kv": "dmccodegui.screens.convex.load_kv"`
       - `"screen_classes"` values: `"dmccodegui.screens.convex.ConvexRunScreen"`, `"dmccodegui.screens.convex.ConvexAxesSetupScreen"`, `"dmccodegui.screens.convex.ConvexParametersScreen"`
       - Remove the `# TODO: Phase 22` comment

    **CRITICAL checks before committing:**
    - Every KV rule header uses `Convex*` names — no FlatGrind or Serration headers
    - No Python import from `..flat_grind.*` in any convex Python file
    - `#:import` paths in KV files reference `convex` module, not `flat_grind` (except DeltaCBarChart which stays imported from flat_grind.widgets — but that is only needed in run.kv which is a stub here)
    - `_CONVEX_PARAM_DEFS` is a list literal, not a comprehension
  </action>
  <verify>
    <automated>python -c "from dmccodegui.screens.convex import ConvexRunScreen, ConvexAxesSetupScreen, ConvexParametersScreen, load_kv; print('All imports OK')"</automated>
  </verify>
  <done>
    - All 5 Python files exist in screens/convex/
    - All 3 KV files exist in ui/convex/
    - convex package importable without error
    - _CONVEX_PARAM_DEFS is an explicit list literal with placeholder comment
    - Registry points to real Convex classes
  </done>
</task>

<task type="auto">
  <name>Task 2: Test scaffold for CONV requirements</name>
  <files>tests/test_convex_screens.py</files>
  <action>
    Create `tests/test_convex_screens.py` mirroring the structure of `tests/test_serration_screens.py`. Include the standard Kivy env setup at the top:
    ```python
    import os, sys
    os.environ.setdefault('KIVY_NO_ENV_CONFIG', '1')
    os.environ.setdefault('KIVY_LOG_LEVEL', 'critical')
    sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'src'))
    ```

    Write these tests:

    1. `test_convex_run_screen_importable` — import ConvexRunScreen, verify issubclass(BaseRunScreen) [CONV-01 partial]
    2. `test_convex_axes_setup_importable` — import ConvexAxesSetupScreen, verify issubclass(BaseAxesSetupScreen) [CONV-02]
    3. `test_convex_axes_setup_inherits_base` — verify BaseAxesSetupScreen in MRO [CONV-02]
    4. `test_convex_params_importable` — import ConvexParametersScreen, verify issubclass(BaseParametersScreen) [CONV-03]
    5. `test_convex_param_defs_has_all_flat_grind_vars` — via mc.init(tempfile) + mc.get_param_defs("4-Axes Convex Grind"), verify same var names as Flat Grind (knfThk, edgeThk, fdA, fdB, fdCdn, fdCup, fdPark, fdD, pitchA..D, ratioA..D, ctsRevA..D) [CONV-03]
    6. `test_convex_param_defs_independent` — `_CONVEX_PARAM_DEFS is not _FLAT_PARAM_DEFS` and dicts are distinct objects [CONV-04]
    7. `test_convex_param_defs_has_placeholder_comment` — read machine_config.py source, verify "Placeholder" appears in the 2 lines before `_CONVEX_PARAM_DEFS` definition [CONV-04]
    8. `test_convex_adjust_panel_importable` — import ConvexAdjustPanel from convex.widgets, verify issubclass(BoxLayout), instantiate, verify children >= 1 [CONV-01 partial]
    9. `test_registry_points_to_convex_classes` — _REGISTRY["4-Axes Convex Grind"]["screen_classes"] values all contain "convex" and do not contain "flat_grind" [CONV-01/02/03]
    10. `test_convex_axes_setup_kv_has_d_axis` — read ui/convex/axes_setup.kv, verify it contains "axis_row_d" [CONV-02]
    11. `test_no_kv_rule_name_collisions` — glob all .kv files under ui/, extract `<ClassName>:` headers, verify no duplicates [ALL]
    12. `test_convex_load_kv_callable` — import load_kv from convex package, verify it is callable [infrastructure]

    Each test should have a descriptive docstring. Follow exact formatting conventions from test_serration_screens.py (numbered section comments, assertion messages).
  </action>
  <verify>
    <automated>pytest tests/test_convex_screens.py -x -v</automated>
  </verify>
  <done>
    - tests/test_convex_screens.py exists with 12 tests
    - All 12 tests pass
    - Full suite passes: pytest tests/ -x shows no new failures
  </done>
</task>

</tasks>

<verification>
```bash
# All convex imports work
python -c "from dmccodegui.screens.convex import ConvexRunScreen, ConvexAxesSetupScreen, ConvexParametersScreen, load_kv; print('OK')"

# Convex-specific tests pass
pytest tests/test_convex_screens.py -x -v

# No KV rule collisions across all machine types
pytest tests/test_convex_screens.py::test_no_kv_rule_name_collisions -x

# Full suite regression
pytest tests/ -x
```
</verification>

<success_criteria>
- All 5 Python files in screens/convex/ importable
- All 3 KV files in ui/convex/ exist with Convex* rule headers
- _CONVEX_PARAM_DEFS is explicit literal with placeholder comment
- Registry points to Convex classes, not Flat Grind placeholders
- 12 tests in test_convex_screens.py all pass
- No KV rule name collisions
- Full test suite passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/22-convex-screen-set/22-01-SUMMARY.md`
</output>
