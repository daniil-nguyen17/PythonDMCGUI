---
phase: 22-convex-screen-set
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/dmccodegui/screens/convex/run.py
  - src/dmccodegui/ui/convex/run.kv
  - tests/test_convex_screens.py
autonomous: true
requirements: [CONV-01]

must_haves:
  truths:
    - "ConvexRunScreen displays live A/B/C/D positions from the controller"
    - "ConvexRunScreen includes a live matplotlib A/B position plot"
    - "ConvexRunScreen includes the DeltaC bar chart with tip/heel labels"
    - "ConvexRunScreen includes more/less stone panel with startPtC offset"
    - "ConvexRunScreen includes a ConvexAdjustPanel placeholder widget"
    - "ConvexRunScreen has Start/Stop cycle controls"
  artifacts:
    - path: "src/dmccodegui/screens/convex/run.py"
      provides: "Full ConvexRunScreen implementation"
      contains: "class ConvexRunScreen"
      min_lines: 200
    - path: "src/dmccodegui/ui/convex/run.kv"
      provides: "Complete Convex run screen layout"
      contains: "ConvexRunScreen"
      min_lines: 100
  key_links:
    - from: "src/dmccodegui/screens/convex/run.py"
      to: "src/dmccodegui/screens/flat_grind/widgets.py"
      via: "DeltaCBarChart import for bar chart"
      pattern: "from.*flat_grind.*widgets.*import.*DeltaCBarChart"
    - from: "src/dmccodegui/screens/convex/run.py"
      to: "src/dmccodegui/screens/convex/widgets.py"
      via: "ConvexAdjustPanel import"
      pattern: "from .widgets import ConvexAdjustPanel"
    - from: "src/dmccodegui/ui/convex/run.kv"
      to: "src/dmccodegui/screens/convex/run.py"
      via: "KV rule header binding"
      pattern: "<ConvexRunScreen>"
---

<objective>
Replace the ConvexRunScreen stub and run.kv stub with the full implementation — a copy of FlatGrindRunScreen with all 4 axes, live matplotlib A/B plot, DeltaC bar chart, more/less stone panel, cycle controls, and the ConvexAdjustPanel wired into the layout. Add remaining tests.

Purpose: Complete CONV-01 — ConvexRunScreen created from Flat Grind base with convex-specific adjustment panel.
Output: Fully functional ConvexRunScreen and run.kv, all tests passing.
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-convex-screen-set/22-CONTEXT.md
@.planning/phases/22-convex-screen-set/22-RESEARCH.md
@.planning/phases/22-convex-screen-set/22-01-SUMMARY.md

@src/dmccodegui/screens/flat_grind/run.py
@src/dmccodegui/ui/flat_grind/run.kv
@src/dmccodegui/screens/flat_grind/widgets.py

<interfaces>
<!-- Key types from Plan 01 that this plan uses -->

From src/dmccodegui/screens/convex/widgets.py (created in Plan 01):
- class ConvexAdjustPanel(BoxLayout) — placeholder panel with header + "Pending customer specs" label
- Must be imported in run.py: `from .widgets import ConvexAdjustPanel`
- Must be imported in run.kv: `#:import ConvexAdjustPanel dmccodegui.screens.convex.widgets.ConvexAdjustPanel`

From src/dmccodegui/screens/flat_grind/widgets.py (cross-package import, intentional):
- DeltaCBarChart, _BaseBarChart, DELTA_C_WRITABLE_START, DELTA_C_WRITABLE_END, DELTA_C_ARRAY_SIZE, DELTA_C_STEP, STONE_SURFACE_MM, STONE_OVERHANG_MM, STEP_MM, STONE_WINDOW_INDICES, stone_window_for_index
- Import in run.py: `from dmccodegui.screens.flat_grind.widgets import (DeltaCBarChart, ...)`
- Import in run.kv: `#:import DeltaCBarChart dmccodegui.screens.flat_grind.widgets`

From src/dmccodegui/screens/base.py:
- class BaseRunScreen(Screen) — provides on_pre_enter, on_leave subscription lifecycle
- module-level submit import: `from dmccodegui.screens.base import submit` (patchable in tests)

From src/dmccodegui/hmi/dmc_vars.py:
- STATE_GRINDING, STATE_HOMING, HMI_GRND, HMI_MORE, HMI_LESS, HMI_TRIGGER_FIRE
- STARTPT_C, POS_BUF_IDX, POS_BUF_A, POS_BUF_B, POS_BUF_SIZE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full ConvexRunScreen and run.kv</name>
  <files>
    src/dmccodegui/screens/convex/run.py
    src/dmccodegui/ui/convex/run.kv
  </files>
  <action>
    **`src/dmccodegui/screens/convex/run.py`** — Copy `screens/flat_grind/run.py` in its entirety, then make these targeted changes:

    1. Rename class: `FlatGrindRunScreen` to `ConvexRunScreen`
    2. Update docstring: "ConvexRunScreen" with description mentioning 4-axis, ConvexAdjustPanel placeholder
    3. Add import: `from .widgets import ConvexAdjustPanel`
    4. Keep `from dmccodegui.screens.flat_grind.widgets import (DeltaCBarChart, _BaseBarChart, DELTA_C_WRITABLE_START, ...)` — this cross-package import is intentional per CONTEXT.md and RESEARCH.md
    5. Keep ALL 4 Kivy properties: pos_a, pos_b, pos_c, pos_d (Convex is 4-axis)
    6. Keep the full matplotlib A/B plot setup (_build_figure, _init_figure, etc.)
    7. Keep DeltaC bar chart logic (deltaC read/write, tip/heel, up/down arrows)
    8. Keep more/less stone panel logic (on_more_stone, on_less_stone, startPtC)
    9. Keep cycle controls (on_start_grind, on_stop)
    10. Keep position poll (_tick_pos reading _TPA, _TPB, _TPC, _TPD — all 4 axes)
    11. Keep MG reader thread (daemon=True, Event-based stop)
    12. Do NOT import from `..flat_grind.run` or `..flat_grind.axes_setup` etc. — only from `..flat_grind.widgets` for DeltaCBarChart
    13. The class should be functionally identical to FlatGrindRunScreen — Convex is Flat Grind plus a placeholder panel

    **`src/dmccodegui/ui/convex/run.kv`** — Copy `ui/flat_grind/run.kv` in its entirety, then make these targeted changes:

    1. Change rule header: `<FlatGrindRunScreen>:` to `<ConvexRunScreen>:`
    2. Update `#:import run_module` path: `dmccodegui.screens.flat_grind.run` to `dmccodegui.screens.convex.run`
    3. Keep `#:import DeltaCBarChart dmccodegui.screens.flat_grind.widgets` — intentional cross-package import
    4. Add `#:import ConvexAdjustPanel dmccodegui.screens.convex.widgets.ConvexAdjustPanel`
    5. Add `ConvexAdjustPanel:` widget instance in the left column, below the DeltaC bar chart and above the more/less stone panel. Use `id: convex_adjust_panel`, `size_hint_y: None`, `height: '80dp'` so it takes minimal space as a placeholder
    6. Keep all existing layout: matplotlib plot (top), DeltaC bar chart (mid), position rows (A=orange, B=purple, C=cyan, D=yellow), cycle controls, stone compensation panel, controller log
    7. Verify pos_d is present in position display rows

    **CRITICAL checks:**
    - KV rule header is `<ConvexRunScreen>:` not `<FlatGrindRunScreen>:`
    - `#:import run_module` points to `dmccodegui.screens.convex.run`
    - `convex_adjust_panel` id is assigned in KV
    - pos_d property and D-axis position row are present
    - No import from `..flat_grind.run` in Python (only from `..flat_grind.widgets`)
  </action>
  <verify>
    <automated>python -c "from dmccodegui.screens.convex.run import ConvexRunScreen; print('pos_d' in ConvexRunScreen.__dict__ or any('pos_d' in str(getattr(c, '__dict__', {})) for c in ConvexRunScreen.__mro__)); print('OK')"</automated>
  </verify>
  <done>
    - ConvexRunScreen is a full implementation with 4-axis positions, matplotlib, DeltaC, cycle controls
    - run.kv has ConvexRunScreen rule with ConvexAdjustPanel wired in with id: convex_adjust_panel
    - No KV rule collision with FlatGrindRunScreen
  </done>
</task>

<task type="auto">
  <name>Task 2: Complete test coverage for CONV-01</name>
  <files>tests/test_convex_screens.py</files>
  <action>
    Add these tests to the existing `tests/test_convex_screens.py` (created in Plan 01), appending after the existing tests:

    13. `test_convex_run_screen_has_all_4_axes` — instantiate ConvexRunScreen, verify pos_a, pos_b, pos_c, pos_d all in s.properties() [CONV-01]
    14. `test_convex_run_screen_has_matplotlib` — read screens/convex/run.py source, verify it contains `from matplotlib` or `import matplotlib` (non-comment lines) [CONV-01]
    15. `test_convex_run_screen_has_delta_c_import` — read screens/convex/run.py source, verify it contains `DeltaCBarChart` import from flat_grind.widgets [CONV-01]
    16. `test_convex_run_kv_has_d_axis` — read ui/convex/run.kv, verify it contains 'pos_d' [CONV-01]
    17. `test_convex_run_kv_has_convex_adjust_panel` — read ui/convex/run.kv, verify it contains 'convex_adjust_panel' [CONV-01]
    18. `test_convex_run_kv_has_matplot_figure` — read ui/convex/run.kv, verify it contains 'MatplotFigure' [CONV-01]
    19. `test_convex_run_kv_rule_header` — read ui/convex/run.kv, verify it contains '<ConvexRunScreen>' and does NOT contain '<FlatGrindRunScreen>' [CONV-01]

    Follow the same formatting conventions (numbered section comments, descriptive docstrings, assertion messages) as the existing tests in the file.
  </action>
  <verify>
    <automated>pytest tests/test_convex_screens.py -x -v</automated>
  </verify>
  <done>
    - test_convex_screens.py has 19 total tests (12 from Plan 01 + 7 new)
    - All 19 tests pass
    - Full suite passes: pytest tests/ -x shows no new failures
  </done>
</task>

</tasks>

<verification>
```bash
# All convex imports work
python -c "from dmccodegui.screens.convex import ConvexRunScreen, ConvexAxesSetupScreen, ConvexParametersScreen; print('OK')"

# ConvexRunScreen has all required properties
python -c "from dmccodegui.screens.convex.run import ConvexRunScreen; s = ConvexRunScreen(); assert all(p in s.properties() for p in ['pos_a','pos_b','pos_c','pos_d']); print('4 axes OK')"

# All convex tests pass
pytest tests/test_convex_screens.py -x -v

# No KV rule collisions
pytest tests/test_convex_screens.py::test_no_kv_rule_name_collisions -x

# Full suite regression
pytest tests/ -x
```
</verification>

<success_criteria>
- ConvexRunScreen is a full implementation, not a stub
- run.kv has complete layout with matplotlib, DeltaC, ConvexAdjustPanel, 4-axis positions
- ConvexAdjustPanel is wired into run.kv with id: convex_adjust_panel
- 19 total tests in test_convex_screens.py, all passing
- No KV rule name collisions across all machine types
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/22-convex-screen-set/22-02-SUMMARY.md`
</output>
