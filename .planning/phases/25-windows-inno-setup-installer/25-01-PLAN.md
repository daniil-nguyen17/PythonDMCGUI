---
phase: 25-windows-inno-setup-installer
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_installer.py
  - deploy/windows/BinhAnHMI.iss
  - deploy/windows/build_windows.bat
autonomous: true
requirements: [WIN-03, WIN-04, WIN-06]

must_haves:
  truths:
    - "The .iss script defines Start Menu and Desktop shortcuts pointing to BinhAnHMI.exe"
    - "The .iss script registers the app in Add/Remove Programs with publisher 'Binh An' and uninstall icon"
    - "The .iss script offers an unchecked 'Launch at Windows startup' task that writes HKCU Run key only when checked"
    - "The .iss script creates two firewall rules (UDP 60007, TCP 23) on install and removes them on uninstall"
    - "The build script extends to compile the .iss into a setup .exe after PyInstaller succeeds"
  artifacts:
    - path: "tests/test_installer.py"
      provides: "Content-inspection tests for .iss correctness"
      min_lines: 50
    - path: "deploy/windows/BinhAnHMI.iss"
      provides: "Complete Inno Setup 6 installer script"
      contains: "[Setup]"
      min_lines: 60
    - path: "deploy/windows/build_windows.bat"
      provides: "Extended build script with ISCC.exe step"
      contains: "ISCC"
  key_links:
    - from: "deploy/windows/BinhAnHMI.iss"
      to: "dist/BinhAnHMI/"
      via: "[Files] Source wildcard"
      pattern: "dist.*BinhAnHMI"
    - from: "deploy/windows/build_windows.bat"
      to: "deploy/windows/BinhAnHMI.iss"
      via: "ISCC.exe invocation"
      pattern: "ISCC.*BinhAnHMI\\.iss"
    - from: "tests/test_installer.py"
      to: "deploy/windows/BinhAnHMI.iss"
      via: "File read and content assertion"
      pattern: "BinhAnHMI\\.iss"

user_setup:
  - service: inno-setup
    why: "ISCC.exe compiler needed to build installer from .iss script"
    env_vars: []
    dashboard_config:
      - task: "Install Inno Setup 6"
        location: "https://jrsoftware.org/isdl.php — download innosetup-6.7.1.exe, run with defaults"
---

<objective>
Create the Inno Setup installer script, test scaffold, and build integration for the Windows installer.

Purpose: Produce the .iss script that ISCC.exe compiles into a single-file setup .exe, packaging the PyInstaller onedir bundle with Start Menu/Desktop shortcuts, Add/Remove Programs entry, optional auto-start, and firewall rules for Galil controller communication.

Output: `deploy/windows/BinhAnHMI.iss`, `tests/test_installer.py`, updated `deploy/windows/build_windows.bat`
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-windows-inno-setup-installer/25-CONTEXT.md
@.planning/phases/25-windows-inno-setup-installer/25-RESEARCH.md
@.planning/phases/25-windows-inno-setup-installer/25-VALIDATION.md
@.planning/phases/24-windows-pyinstaller-bundle/24-02-SUMMARY.md

@deploy/windows/build_windows.bat
@deploy/windows/version_file.txt

<interfaces>
<!-- Key assets from Phase 24 that this plan depends on -->

From deploy/windows/build_windows.bat:
- Script resolves to repo root via `cd /d "%~dp0..\.."` 
- PyInstaller build produces `dist/BinhAnHMI/BinhAnHMI.exe`
- Exits with errorlevel 1 on failure

From deploy/windows/version_file.txt:
- Version: 4.0.0.0
- CompanyName: "Binh An"
- ProductName: "Binh An HMI"
- InternalName: "BinhAnHMI"

From deploy/windows/BinhAnHMI.ico:
- Multi-resolution gear icon for EXE and installer wizard

Existing test patterns:
- pytest with pyproject.toml config
- testpaths = ["tests"]
- Tests follow test_*.py naming in tests/ root
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test scaffold and Inno Setup script</name>
  <files>tests/test_installer.py, deploy/windows/BinhAnHMI.iss</files>
  <behavior>
    - test_iss_file_exists: deploy/windows/BinhAnHMI.iss exists and is non-empty
    - test_iss_setup_section: [Setup] contains AppName="Binh An HMI", AppPublisher="Binh An", AppVersion="4.0.0", PrivilegesRequired=admin, WizardStyle=modern
    - test_iss_appid: [Setup] contains AppId with a valid GUID (double-brace escape pattern)
    - test_iss_start_menu_shortcut: [Icons] contains {group}\Binh An HMI entry pointing to {app}\BinhAnHMI.exe (WIN-03)
    - test_iss_desktop_shortcut: [Icons] contains {commondesktop}\Binh An HMI entry pointing to {app}\BinhAnHMI.exe (WIN-03)
    - test_iss_appname_publisher: [Setup] has AppName and AppPublisher matching locked decisions (WIN-04)
    - test_iss_uninstall_display_icon: [Setup] has UninstallDisplayIcon pointing to {app}\BinhAnHMI.exe (WIN-04)
    - test_iss_startup_task_unchecked: [Tasks] has startup entry with Flags: unchecked (WIN-06)
    - test_iss_hkcu_run_key: [Registry] has HKCU Run key with Tasks: startup and uninsdeletevalue (WIN-06)
    - test_iss_firewall_install_rules: [Run] has netsh commands for both "Binh An HMI - Galil DR" (UDP 60007) and "Binh An HMI - Galil TCP" (TCP 23)
    - test_iss_firewall_uninstall_rules: [UninstallRun] has netsh delete commands for both firewall rules
    - test_iss_firewall_delete_before_add: [Run] section deletes firewall rules before adding them (idempotent reinstall)
    - test_iss_files_source: [Files] has Source pointing to dist\BinhAnHMI with recursesubdirs and ignoreversion
    - test_iss_launch_after_install: [Run] has postinstall entry for BinhAnHMI.exe with nowait and skipifsilent flags
    - test_iss_output_filename: OutputBaseFilename matches BinhAn_HMI_v4.0.0_Setup
    - test_iss_64bit_mode: ArchitecturesInstallIn64BitMode=x64compatible is set
  </behavior>
  <action>
RED phase: Create tests/test_installer.py with all behavior tests listed above. Each test reads deploy/windows/BinhAnHMI.iss as text and asserts required patterns. Use a module-level fixture or helper that reads the .iss file path relative to the repo root. Use pathlib.Path(__file__).resolve().parent.parent / "deploy" / "windows" / "BinhAnHMI.iss" to locate the file. Run tests — they MUST all fail (file doesn't exist yet).

GREEN phase: Create deploy/windows/BinhAnHMI.iss with the complete Inno Setup 6 script. Use these exact values per locked decisions:

Preprocessor defines:
- MyAppName = "Binh An HMI"
- MyAppVersion = "4.0.0"
- MyAppPublisher = "Binh An"
- MyAppExeName = "BinhAnHMI.exe"

[Setup] section:
- AppId={{135F218F-F578-4416-B2FE-0CD860124D9C} (double-brace escape for literal {)
- AppName, AppVersion, AppPublisher from defines
- DefaultDirName={autopf}\Binh An HMI
- DefaultGroupName=Binh An
- OutputBaseFilename=BinhAn_HMI_v{#MyAppVersion}_Setup
- OutputDir=..\..\dist
- SetupIconFile=BinhAnHMI.ico
- UninstallDisplayIcon={app}\{#MyAppExeName}
- UninstallDisplayName={#MyAppName}
- Compression=lzma2/ultra
- SolidCompression=yes
- WizardStyle=modern
- PrivilegesRequired=admin
- ArchitecturesAllowed=x64compatible
- ArchitecturesInstallIn64BitMode=x64compatible
- DisableWelcomePage=no
- DisableReadyPage=no
- DisableProgramGroupPage=yes (no need for user to choose program group name)
- AllowNoIcons=no

[Languages]: english with compiler:Default.isl

[Tasks]: startup task with Description "Launch {#MyAppName} at Windows startup", GroupDescription "Additional options:", Flags unchecked

[Files]: Source "..\..\dist\BinhAnHMI\*", DestDir "{app}", Flags ignoreversion recursesubdirs createallsubdirs

[Icons]:
- Start Menu: Name "{group}\{#MyAppName}", Filename "{app}\{#MyAppExeName}", WorkingDir "{app}"
- Desktop: Name "{commondesktop}\{#MyAppName}", Filename "{app}\{#MyAppExeName}", WorkingDir "{app}"

[Registry]: HKCU Run key for auto-start, ValueData """{app}\{#MyAppExeName}""" (triple quotes for space-safe path), Flags uninsdeletevalue, Tasks: startup

[Run]:
1. Delete existing firewall rules (idempotent reinstall safety) — two netsh delete commands, runhidden
2. Add "Binh An HMI - Galil DR" UDP port 60007 rule — netsh add, runhidden
3. Add "Binh An HMI - Galil TCP" TCP port 23 rule — netsh add, runhidden
4. Launch app after install — postinstall nowait skipifsilent

[UninstallRun]:
1. Delete "Binh An HMI - Galil DR" firewall rule — runhidden
2. Delete "Binh An HMI - Galil TCP" firewall rule — runhidden

Run tests again — all MUST pass.
  </action>
  <verify>
    <automated>pytest tests/test_installer.py -x -v</automated>
  </verify>
  <done>All 16 .iss content tests pass. deploy/windows/BinhAnHMI.iss exists with complete Inno Setup script covering shortcuts (WIN-03), Add/Remove Programs (WIN-04), auto-start task (WIN-06), and firewall rules.</done>
</task>

<task type="auto">
  <name>Task 2: Extend build script with ISCC.exe step</name>
  <files>deploy/windows/build_windows.bat</files>
  <action>
Append an Inno Setup compilation step to the existing deploy/windows/build_windows.bat AFTER the existing PyInstaller success check block. The new block must:

1. Only run if the PyInstaller build succeeded (the existing script already exits on failure)
2. Check for ISCC.exe at the default path: "C:\Program Files (x86)\Inno Setup 6\ISCC.exe"
3. If ISCC.exe is not found, also try "where iscc" for PATH-based installs
4. If neither found, print a WARNING (not an error) with the download URL and exit /b 0 — the PyInstaller bundle is still usable without the installer
5. If found, run: %ISCC% /Q "deploy\windows\BinhAnHMI.iss"
6. Check errorlevel — if non-zero, print "INSTALLER BUILD FAILED" and exit /b 1
7. On success, print the output path: dist\BinhAn_HMI_v4.0.0_Setup.exe

The existing script already does `cd /d "%~dp0..\.."` to resolve to repo root, so paths like "deploy\windows\BinhAnHMI.iss" are relative to repo root.

Do NOT modify the existing PyInstaller section — only append after the final success echo.
  </action>
  <verify>
    <automated>python -c "p=open('deploy/windows/build_windows.bat').read(); assert 'ISCC' in p; assert 'BinhAnHMI.iss' in p; assert 'jrsoftware' in p.lower() or 'isdl' in p.lower(); print('build_windows.bat contains ISCC integration')"</automated>
  </verify>
  <done>build_windows.bat has a non-fatal ISCC.exe step that compiles the .iss into BinhAn_HMI_v4.0.0_Setup.exe when Inno Setup is installed, and gracefully skips with a warning when it is not.</done>
</task>

</tasks>

<verification>
All automated checks for Phase 25 Plan 01:
1. `pytest tests/test_installer.py -x -v` — all .iss content tests pass
2. `python -c "..."` — build_windows.bat contains ISCC integration
3. `pytest tests/ -x` — full test suite still passes (no regressions)
</verification>

<success_criteria>
- deploy/windows/BinhAnHMI.iss exists with all 8 Inno Setup sections ([Setup], [Languages], [Tasks], [Files], [Icons], [Registry], [Run], [UninstallRun])
- tests/test_installer.py has 16 passing tests covering WIN-03, WIN-04, WIN-06
- build_windows.bat includes non-fatal ISCC.exe compilation step
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/25-windows-inno-setup-installer/25-01-SUMMARY.md`
</output>
