---
phase: 28-logging-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - src/dmccodegui/controller.py
  - src/dmccodegui/hmi/mg_reader.py
  - src/dmccodegui/utils/transport.py
  - src/dmccodegui/screens/flat_grind/run.py
  - src/dmccodegui/screens/flat_grind/axes_setup.py
  - src/dmccodegui/screens/serration/run.py
  - src/dmccodegui/screens/serration/axes_setup.py
  - src/dmccodegui/screens/convex/run.py
  - src/dmccodegui/screens/convex/axes_setup.py
autonomous: true
requirements: [APP-01]

must_haves:
  truths:
    - "No print() calls remain in any src/dmccodegui/ production file (excluding __main__ blocks)"
    - "Each migrated file has a module-level logger = logging.getLogger(__name__) at top"
    - "Log levels are assigned correctly: debug for protocol traces, info for state transitions, warning for recoverable errors, error for failures"
  artifacts:
    - path: "src/dmccodegui/controller.py"
      provides: "41 print calls migrated to logger"
      contains: "logger = logging.getLogger(__name__)"
    - path: "src/dmccodegui/screens/flat_grind/run.py"
      provides: "29 print calls migrated to logger"
      contains: "logger = logging.getLogger(__name__)"
  key_links:
    - from: "src/dmccodegui/controller.py"
      to: "logging root logger"
      via: "module-level logging.getLogger(__name__)"
      pattern: "logger = logging\\.getLogger\\(__name__\\)"
---

<objective>
Migrate all remaining 135 print() calls across 9 source files to structured logging using module-level loggers. This completes the print-to-logger migration started in Plan 01 (which handled main.py).

Purpose: APP-01 requires that the rotating log file captures all app output. Print calls bypass the logging infrastructure entirely — they go to stdout/stderr only and are lost when console=False in the frozen Windows bundle.

Output: Zero print() calls in production source code (except controller.py __main__ block).
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-logging-infrastructure/28-CONTEXT.md
@.planning/phases/28-logging-infrastructure/28-RESEARCH.md
@.planning/phases/28-logging-infrastructure/28-01-SUMMARY.md

<interfaces>
<!-- Level assignment heuristic from RESEARCH.md -->
- logger.debug — Verbose trace: individual command sends, response parsing, array element reads
- logger.info — State transitions, startup events, connection success, screen loads
- logger.warning — Recoverable issues: fallback triggered, parse failure, retry
- logger.error — Operation failures that don't crash: command failed, KV load failed

<!-- Print call counts per file (verified by grep) -->
- controller.py: 41 calls (includes __main__ block ~6 calls — leave those as print)
- screens/flat_grind/run.py: 29 calls
- screens/convex/run.py: 18 calls
- screens/flat_grind/axes_setup.py: 13 calls
- screens/serration/axes_setup.py: 12 calls
- screens/convex/axes_setup.py: 12 calls
- screens/serration/run.py: 4 calls
- hmi/mg_reader.py: 4 calls
- utils/transport.py: 2 calls

<!-- controller.py __main__ block starts at line 669 — leave those print() calls -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate core infrastructure files (controller.py, mg_reader.py, transport.py)</name>
  <files>src/dmccodegui/controller.py, src/dmccodegui/hmi/mg_reader.py, src/dmccodegui/utils/transport.py</files>
  <action>
For each of the 3 files, add `import logging` and `logger = logging.getLogger(__name__)` at module level (below existing imports, above first class/function).

**controller.py** (41 print calls, ~35 in production code + ~6 in `if __name__ == "__main__":` block):
- Leave all print() calls inside the `if __name__ == "__main__":` block (line 669+) unchanged — that block is a dev-only CLI tool.
- Migrate all other ~35 print() calls using the level heuristic:
  - `print(f"[CTRL] Sending command: {command}")` → `logger.debug("Sending command: %s", command)` (protocol trace = debug)
  - `print(f"[CTRL] Response: {resp}")` → `logger.debug("Response: %s", resp.strip())` (protocol trace = debug)
  - `print(f"[CTRL] Command failed: ...")` → `logger.warning("Command failed: %s -> %s", command, e)` (recoverable = warning)
  - `print(f"[CTRL] TC1 error: ...")` → `logger.warning("TC1 error code: %s", tc1)` (recoverable = warning)
  - `print(f"[CTRL] Connected ...")` → `logger.info("Connected: %s", ...)` (state transition = info)
  - `print(f"[CTRL] Reading ...")` → `logger.debug("Reading slice %s[%d:%d]", ...)` (protocol trace = debug)
  - General pattern: strip the `[CTRL]` prefix tag — the `[module]` in the log format handles module identification

Use `%s` format specifiers (not f-strings) in all logger calls — this is the standard logging convention that avoids string formatting when the log level is filtered out.

**hmi/mg_reader.py** (4 print calls):
- `print(f"[MG] connected: ...")` → `logger.info("connected: %s", connection_string)`
- `print(f"[MG] GOpen failed: ...")` → `logger.warning("GOpen failed: %s", exc)`
- `print("[MG] handle closed")` → `logger.info("handle closed")`
- Any other print → assign level based on context

**utils/transport.py** (2 print calls):
- Assign appropriate levels based on the message content.

After migration, verify:
- `grep -n "print(" src/dmccodegui/controller.py` returns ONLY lines inside the `if __name__` block (line 669+)
- `grep -n "print(" src/dmccodegui/hmi/mg_reader.py` returns nothing
- `grep -n "print(" src/dmccodegui/utils/transport.py` returns nothing
  </action>
  <verify>
    <automated>python -c "
import pathlib, re
# controller.py: only print calls allowed are in __main__ block
c = pathlib.Path('src/dmccodegui/controller.py').read_text()
lines = c.splitlines()
main_start = next(i for i,l in enumerate(lines) if '__name__' in l and '__main__' in l)
bad = [i+1 for i,l in enumerate(lines[:main_start]) if 'print(' in l]
assert not bad, f'controller.py has print() before __main__ at lines {bad}'
# mg_reader.py: zero prints
m = pathlib.Path('src/dmccodegui/hmi/mg_reader.py').read_text()
bad2 = [i+1 for i,l in enumerate(m.splitlines()) if 'print(' in l]
assert not bad2, f'mg_reader.py has print() at lines {bad2}'
# transport.py: zero prints
t = pathlib.Path('src/dmccodegui/utils/transport.py').read_text()
bad3 = [i+1 for i,l in enumerate(t.splitlines()) if 'print(' in l]
assert not bad3, f'transport.py has print() at lines {bad3}'
print('OK: core files migrated')
" && pytest tests/ -x --timeout=30</automated>
  </verify>
  <done>All production print() calls in controller.py (excluding __main__ block), mg_reader.py, and transport.py are replaced with logger calls at correct levels. Module-level loggers defined.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate screen files (6 files across 3 machine types)</name>
  <files>src/dmccodegui/screens/flat_grind/run.py, src/dmccodegui/screens/flat_grind/axes_setup.py, src/dmccodegui/screens/serration/run.py, src/dmccodegui/screens/serration/axes_setup.py, src/dmccodegui/screens/convex/run.py, src/dmccodegui/screens/convex/axes_setup.py</files>
  <action>
For each of the 6 screen files, add `import logging` and `logger = logging.getLogger(__name__)` at module level.

**Pattern for run.py files** (flat_grind: 29, convex: 18, serration: 4):
- `print(f"[RUN] BV done ...")` → `logger.info("BV done -- all variables saved")`
- `print(f"[RUN] hmiSetp fired ...")` → `logger.info("hmiSetp fired -- entering setup")`
- `print(f"[RUN] Starting grind ...")` → `logger.info("Starting grind cycle")`
- `print(f"[RUN] Read failed ...")` → `logger.warning("Read failed: %s -> %s", var, e)`
- Verbose position/state reads → `logger.debug(...)` (protocol trace)
- Button press / state change actions → `logger.info(...)` (operational events)
- Failures that don't crash → `logger.warning(...)` (recoverable issues)

**Pattern for axes_setup.py files** (flat_grind: 13, serration: 12, convex: 12):
- `print(f"[SETUP] CPM values ...")` → `logger.info("CPM values from controller: %s", cpm_updates)`
- `print(f"[SETUP] teach_rest_point: empty axis_list ...")` → `logger.warning("teach_rest_point: empty axis_list, aborting")`
- `print(f"[SETUP] Read failed ...")` → `logger.warning("Read failed: %s -> %s", command, e)`
- Jog commands → `logger.debug(...)` (protocol trace)
- Teach point results → `logger.info(...)` (operational events)

Strip all `[RUN]`, `[SETUP]`, `[CTRL]`-style prefix tags — the `[%(module)s]` in the log format already identifies the source module.

Use `%s`-style format specifiers (not f-strings) in all logger calls.

After migration, verify zero print() calls remain in any of the 6 files.
  </action>
  <verify>
    <automated>python -c "
import pathlib
files = [
    'src/dmccodegui/screens/flat_grind/run.py',
    'src/dmccodegui/screens/flat_grind/axes_setup.py',
    'src/dmccodegui/screens/serration/run.py',
    'src/dmccodegui/screens/serration/axes_setup.py',
    'src/dmccodegui/screens/convex/run.py',
    'src/dmccodegui/screens/convex/axes_setup.py',
]
for f in files:
    t = pathlib.Path(f).read_text()
    bad = [i+1 for i,l in enumerate(t.splitlines()) if 'print(' in l]
    assert not bad, f'{f} has print() at lines {bad}'
    assert 'logging.getLogger(__name__)' in t, f'{f} missing module-level logger'
print('OK: all 6 screen files migrated')
" && pytest tests/ -x --timeout=30</automated>
  </verify>
  <done>All 88 print() calls across 6 screen files replaced with logger calls at correct levels. Module-level loggers defined in all files. Full test suite passes. Combined with Plan 01 and Task 1, zero print() calls remain in production code (only controller.py __main__ block excluded).</done>
</task>

</tasks>

<verification>
- Zero print() in production code: `grep -rn "print(" src/dmccodegui/ --include="*.py"` returns only controller.py __main__ block lines
- All 10 migrated files have `logger = logging.getLogger(__name__)`
- `pytest tests/ -x` — full suite green
</verification>

<success_criteria>
- All 135 print() calls across 9 files migrated to logger calls (controller.py __main__ block excluded)
- Each file has module-level `logger = logging.getLogger(__name__)`
- Log levels assigned per heuristic: debug=trace, info=events, warning=recoverable, error=failures
- No f-strings in logger calls (use %s format specifiers)
- No [TAG] prefixes in log messages (module identified by format)
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/28-logging-infrastructure/28-02-SUMMARY.md`
</output>
