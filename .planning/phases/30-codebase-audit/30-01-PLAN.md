---
phase: 30-codebase-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .vulture_allowlist.py
  - scripts/gen_vulture_allowlist.py
  - tests/test_lint.py
  - src/dmccodegui/main.py
  - src/dmccodegui/__init__.py
  - src/dmccodegui/__main__.py
  - src/dmccodegui/app_state.py
  - src/dmccodegui/controller.py
  - src/dmccodegui/machine_config.py
  - src/dmccodegui/theme_manager.py
  - src/dmccodegui/auth/auth_manager.py
  - src/dmccodegui/hmi/data_record.py
  - src/dmccodegui/hmi/dmc_vars.py
  - src/dmccodegui/hmi/mg_reader.py
  - src/dmccodegui/hmi/poll.py
  - src/dmccodegui/screens/__init__.py
  - src/dmccodegui/screens/base.py
  - src/dmccodegui/screens/diagnostics.py
  - src/dmccodegui/screens/pin_overlay.py
  - src/dmccodegui/screens/profiles.py
  - src/dmccodegui/screens/setup.py
  - src/dmccodegui/screens/status_bar.py
  - src/dmccodegui/screens/tab_bar.py
  - src/dmccodegui/screens/users.py
  - src/dmccodegui/screens/flat_grind/run.py
  - src/dmccodegui/screens/flat_grind/axes_setup.py
  - src/dmccodegui/screens/flat_grind/parameters.py
  - src/dmccodegui/screens/flat_grind/widgets.py
  - src/dmccodegui/screens/serration/run.py
  - src/dmccodegui/screens/serration/axes_setup.py
  - src/dmccodegui/screens/serration/parameters.py
  - src/dmccodegui/screens/serration/widgets.py
  - src/dmccodegui/screens/convex/run.py
  - src/dmccodegui/screens/convex/axes_setup.py
  - src/dmccodegui/screens/convex/parameters.py
  - src/dmccodegui/screens/convex/widgets.py
  - src/dmccodegui/utils/jobs.py
  - src/dmccodegui/utils/transport.py
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02

must_haves:
  truths:
    - "ruff check src/ exits with zero violations"
    - "vulture src/ .vulture_allowlist.py --min-confidence 80 exits with zero violations"
    - "Import ordering is stdlib -> third-party -> local in every file"
    - "No unused imports remain in any source file"
  artifacts:
    - path: "pyproject.toml"
      provides: "ruff and vulture configuration + dev deps"
      contains: "[tool.ruff]"
    - path: ".vulture_allowlist.py"
      provides: "Kivy KV-referenced symbol allowlist for vulture"
      min_lines: 20
    - path: "scripts/gen_vulture_allowlist.py"
      provides: "Script that auto-generates .vulture_allowlist.py from KV files"
      min_lines: 30
    - path: "tests/test_lint.py"
      provides: "CI gate running ruff and vulture as subprocess checks"
      min_lines: 15
  key_links:
    - from: "scripts/gen_vulture_allowlist.py"
      to: ".vulture_allowlist.py"
      via: "script parses all .kv files and writes allowlist"
      pattern: "vulture_allowlist"
    - from: "pyproject.toml"
      to: "ruff check src/"
      via: "[tool.ruff] config section"
      pattern: "tool\\.ruff"
---

<objective>
Set up ruff and vulture tooling, configure lint rules, auto-generate the KV-reference vulture allowlist, then run ruff --fix to auto-fix all safe issues (import ordering, unused imports, whitespace) across all 44 source files.

Purpose: Establishes the linting infrastructure and resolves all safe auto-fixable violations in one pass. This must happen BEFORE dead code analysis (Plan 02) so that vulture operates on clean imports.
Output: Zero ruff violations, vulture allowlist generated, all imports correctly ordered.
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-codebase-audit/30-CONTEXT.md
@.planning/phases/30-codebase-audit/30-RESEARCH.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure ruff and vulture tooling + generate KV allowlist</name>
  <files>pyproject.toml, .vulture_allowlist.py, scripts/gen_vulture_allowlist.py, tests/test_lint.py</files>
  <action>
1. Update pyproject.toml:
   - Add ruff and vulture to `[project.optional-dependencies] dev` group: `"ruff>=0.15"`, `"vulture>=2.16"`
   - Add `[tool.ruff]` section: `line-length = 120`, `target-version = "py310"`
   - Add `[tool.ruff.lint]` section: `select = ["E", "F", "I", "N", "W"]`, `ignore = []`
   - Add `[tool.ruff.lint.per-file-ignores]`:
     - `"src/dmccodegui/hmi/dmc_vars.py" = ["N"]` (hardware contract names)
     - `"src/dmccodegui/screens/flat_grind_widgets.py" = ["F401"]` (backward-compat re-exports)
     - `"src/dmccodegui/screens/run.py" = ["F401"]` (backward-compat re-exports)
     - `"src/dmccodegui/screens/parameters.py" = ["F401"]` (backward-compat re-exports)
     - `"src/dmccodegui/screens/axes_setup.py" = ["F401"]` (backward-compat re-exports)
     - `"src/dmccodegui/screens/__init__.py" = ["F401"]` (package public API re-exports)
   - Add `[tool.ruff.lint.isort]` section: `known-third-party = ["kivy", "matplotlib", "kivy_matplotlib_widget", "gclib"]`

2. Create `scripts/gen_vulture_allowlist.py`:
   - Parse all `.kv` files in `src/dmccodegui/ui/` recursively
   - Extract all `root.<method_or_property>` and `app.<method_or_property>` references using regex
   - Track the current KV rule class name (from `<ClassName>:` headers) to qualify references
   - Also extract Kivy property observer methods (`on_<property_name>`) from Python source files that have matching Kivy properties
   - Include the backward-compat shim modules: `screens/flat_grind_widgets.py`, `screens/run.py`, `screens/parameters.py`, `screens/axes_setup.py`
   - Include `machine_config._REGISTRY` (accessed dynamically)
   - Write output to `.vulture_allowlist.py` with header comment: "Auto-generated by scripts/gen_vulture_allowlist.py -- DO NOT EDIT MANUALLY"
   - The allowlist uses the vulture format: `ClassName.method_name` as bare expressions after importing the class

3. Run the script to generate `.vulture_allowlist.py`

4. Create `tests/test_lint.py`:
   - Test `test_ruff_check`: runs `ruff check src/` as subprocess, asserts returncode == 0
   - Test `test_vulture_check`: runs `vulture src/ .vulture_allowlist.py --min-confidence 80` as subprocess, asserts returncode == 0
   - Both tests capture and print stdout/stderr on failure for debugging

5. Install ruff and vulture: `pip install "ruff>=0.15" "vulture>=2.16"`
  </action>
  <verify>
    <automated>cd "C:/Users/danii/OneDrive/Desktop/PythonDMCGUI" && python scripts/gen_vulture_allowlist.py && python -m vulture src/ .vulture_allowlist.py --min-confidence 80</automated>
  </verify>
  <done>pyproject.toml has ruff+vulture config, .vulture_allowlist.py exists and is generated from KV files, vulture passes with allowlist at 80% confidence</done>
</task>

<task type="auto">
  <name>Task 2: Run ruff --fix on all source files and resolve remaining violations</name>
  <files>All 44 .py files in src/dmccodegui/</files>
  <action>
1. Run `ruff check --fix src/` to auto-fix all safe issues:
   - Import ordering (I rules): reorders stdlib -> third-party -> local with blank lines between groups
   - Unused imports (F401): removes imports not referenced in the file
   - Whitespace issues (W): trailing whitespace, blank line normalization

2. Run `ruff check src/` to see remaining violations that require manual fix:
   - For any remaining F-violations (undefined names, unused variables): fix manually
   - For any remaining E-violations: fix manually (likely minimal)
   - For N-violations: these will be addressed in Plan 02 (naming task), skip for now

3. Handle known special cases:
   - `src/dmccodegui/utils/transport.py`: has a `try/except ImportError` block for `GalilDriverProtocol`. If ruff flags the bare except or import order, add targeted `# noqa` comments with explanation
   - `src/dmccodegui/screens/flat_grind/run.py`: `sys` import may be unused — let ruff --fix remove it if so
   - Backward-compat shim files (`flat_grind_widgets.py`, `screens/run.py`, `screens/parameters.py`, `screens/axes_setup.py`): already have per-file-ignores for F401, verify they pass

4. Run `ruff check src/` again — must exit 0 (ignoring N rules which are Plan 02)

5. Run existing test suite to confirm no behavioral changes: `pytest tests/ -x -q`
  </action>
  <verify>
    <automated>cd "C:/Users/danii/OneDrive/Desktop/PythonDMCGUI" && ruff check src/ && pytest tests/ -x -q</automated>
  </verify>
  <done>ruff check src/ exits 0 with zero violations. pytest suite passes unchanged. All imports are correctly ordered (stdlib -> third-party -> local). No unused imports remain.</done>
</task>

</tasks>

<verification>
- `ruff check src/` exits 0
- `ruff check --select I src/` exits 0 (import ordering specifically)
- `ruff check --select F401 src/` exits 0 (unused imports specifically)
- `vulture src/ .vulture_allowlist.py --min-confidence 80` exits 0
- `pytest tests/ -x -q` passes (no behavioral changes)
- `pytest tests/test_lint.py -v` passes
</verification>

<success_criteria>
- Zero ruff violations across all 44 source files
- Zero vulture violations with KV allowlist at 80% confidence
- Import ordering standardized in every file
- All unused imports removed
- Existing test suite passes without modification (no behavior change)
</success_criteria>

<output>
After completion, create `.planning/phases/30-codebase-audit/30-01-SUMMARY.md`
</output>
