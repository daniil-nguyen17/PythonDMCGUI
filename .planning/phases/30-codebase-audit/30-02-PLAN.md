---
phase: 30-codebase-audit
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - src/dmccodegui/controller.py
  - src/dmccodegui/hmi/poll.py
  - .vulture_allowlist.py
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-04

must_haves:
  truths:
    - "No dead code remains — vulture at 80% confidence reports zero violations"
    - "Logger variable is 'logger' in every module — no file uses 'log' as the logger variable name"
    - "Ruff N-rules pass clean across all files (with dmc_vars.py exempted)"
    - "Borderline dead code is commented out with # DEAD_CODE marker, not deleted"
  artifacts:
    - path: "src/dmccodegui/controller.py"
      provides: "Renamed logger variable (log -> logger)"
      contains: "logger = logging.getLogger"
    - path: ".vulture_allowlist.py"
      provides: "Updated allowlist covering any new suppressions needed"
      min_lines: 20
  key_links:
    - from: "src/dmccodegui/controller.py"
      to: "ruff check --select N src/"
      via: "logger rename satisfies N-rules"
      pattern: "logger = logging"
---

<objective>
Resolve dead code findings from vulture, rename the controller.py logger variable from `log` to `logger` for consistency, and ensure ruff N-rules (naming) pass clean across all modules.

Purpose: Closes AUDIT-01 (dead code removal) and AUDIT-04 (naming consistency). After Plan 01 cleaned imports, this plan tackles the substantive code quality issues.
Output: Zero vulture violations, zero ruff N-rule violations, consistent `logger` naming everywhere.
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-codebase-audit/30-CONTEXT.md
@.planning/phases/30-codebase-audit/30-RESEARCH.md
@.planning/phases/30-codebase-audit/30-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dead code analysis and resolution</name>
  <files>src/dmccodegui/hmi/poll.py, .vulture_allowlist.py</files>
  <action>
1. Run `vulture src/ .vulture_allowlist.py --min-confidence 80` and capture all reported violations.

2. For each vulture finding, categorize:
   - **True dead code (confidence >= 90%, clearly unreachable):** Remove the dead function/class/variable entirely
   - **Borderline dead code (80-90% confidence OR unclear reachability):** Comment out with `# DEAD_CODE: <reason>` marker and leave in place. Do NOT delete.
   - **False positives (KV-referenced, test-only, dynamic dispatch):** Add to `.vulture_allowlist.py`

3. Specific known items to investigate:
   - `hmi/poll.py` — `ControllerPoller` class: Check if ANY test file imports `ControllerPoller` directly (not just via `MachineState`). If tests import it directly, it stays and goes in allowlist. If no test imports it directly, comment out with `# DEAD_CODE: replaced by DataRecordListener, tests may still reference via MachineState`
   - Orphaned KV rules: Check if `<RunScreen>:`, `<AxesSetupScreen>:`, `<ParametersScreen>:` in `ui/run.kv`, `ui/axes_setup.kv`, `ui/parameters.kv` are loaded at runtime. Trace through `main.py` + `_REGISTRY`. If the backward-compat classes (in `screens/run.py`, `screens/axes_setup.py`, `screens/parameters.py`) are instantiated via the registry, the KV rules are live. If not, flag as dead UI code with a comment in the KV file.
   - Any other vulture findings: apply the categorization rules above

4. Regenerate `.vulture_allowlist.py` by running `python scripts/gen_vulture_allowlist.py` if any new false positives were identified.

5. If any false positives require manual addition beyond what the generator script produces, append them to `.vulture_allowlist.py` with clear comments explaining why.

6. Run `vulture src/ .vulture_allowlist.py --min-confidence 80` — must exit 0.

7. Run `pytest tests/ -x -q` — must pass (any dead code removal must not break tests).
  </action>
  <verify>
    <automated>cd "C:/Users/danii/OneDrive/Desktop/PythonDMCGUI" && python -m vulture src/ .vulture_allowlist.py --min-confidence 80 && pytest tests/ -x -q</automated>
  </verify>
  <done>Vulture reports zero violations at 80% confidence. All true dead code removed. Borderline code marked with # DEAD_CODE. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Naming consistency — rename controller.py logger + verify N-rules</name>
  <files>src/dmccodegui/controller.py</files>
  <action>
1. Open `src/dmccodegui/controller.py`:
   - Find `log = logging.getLogger(__name__)` (approximately line 12)
   - Rename to `logger = logging.getLogger(__name__)`
   - Find-and-replace ALL usages of `log.` to `logger.` throughout the file (log.info, log.debug, log.warning, log.error, log.exception)
   - Be careful NOT to replace `log` inside string literals or comments that reference DMC log concepts

2. Run `ruff check --select N src/` to verify naming rules pass:
   - All function/variable names are snake_case
   - All class names are PascalCase
   - `dmc_vars.py` is exempted via per-file-ignores (confirmed in Plan 01)
   - If any N-violations remain, fix them (expected: none after controller.py rename)

3. Run full `ruff check src/` — must exit 0.

4. Run `pytest tests/ -x -q` — must pass (the rename is internal, no public API change).

5. Verify no file in src/ still uses `log = logging` pattern:
   ```bash
   grep -rn "^log = logging\|^log=logging" src/
   ```
   Must return empty.
  </action>
  <verify>
    <automated>cd "C:/Users/danii/OneDrive/Desktop/PythonDMCGUI" && ruff check --select N src/ && grep -rn "^log = logging" src/ && echo "FAIL: log pattern found" && exit 1 || ruff check src/ && pytest tests/ -x -q</automated>
  </verify>
  <done>controller.py uses `logger` consistently. ruff N-rules pass clean. No file in src/ uses `log = logging` pattern. All tests pass.</done>
</task>

</tasks>

<verification>
- `vulture src/ .vulture_allowlist.py --min-confidence 80` exits 0
- `ruff check --select N src/` exits 0
- `ruff check src/` exits 0 (full suite still clean)
- `grep -rn "^log = logging" src/` returns empty
- `pytest tests/ -x -q` passes
- All `# DEAD_CODE` markers have explanatory reasons
</verification>

<success_criteria>
- Zero vulture violations at 80% confidence threshold
- Zero ruff N-rule violations (naming)
- Logger variable is `logger` in every module without exception
- Borderline dead code has `# DEAD_CODE` markers (not deleted)
- Existing test suite passes without modification
</success_criteria>

<output>
After completion, create `.planning/phases/30-codebase-audit/30-02-SUMMARY.md`
</output>
