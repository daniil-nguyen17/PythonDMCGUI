---
phase: 31-bug-fixes-and-ui-polish
plan: 03
type: execute
wave: 2
depends_on: ["31-02"]
files_modified:
  - src/dmccodegui/ui/run.kv
  - src/dmccodegui/ui/axes_setup.kv
  - src/dmccodegui/ui/flat_grind/run.kv
  - src/dmccodegui/ui/flat_grind/axes_setup.kv
  - src/dmccodegui/ui/serration/run.kv
  - src/dmccodegui/ui/serration/axes_setup.kv
  - src/dmccodegui/ui/convex/run.kv
  - src/dmccodegui/ui/convex/axes_setup.kv
autonomous: false
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "Every padding and spacing value in ALL KV files maps to the 4/8/12/16/24dp token scale"
    - "All interactive elements (buttons, toggles, tappable rows) measure at least 44dp in height"
    - "No visual jumps when switching between Run, Setup, Parameters, and Profiles tabs"
    - "Layout looks correct on a 15.6-inch 1920x1080 display"
  artifacts:
    - path: "src/dmccodegui/ui/run.kv"
      provides: "Base run screen with token-scale spacing and 44dp-compliant controls"
    - path: "src/dmccodegui/ui/flat_grind/run.kv"
      provides: "Flat grind run screen with token-scale spacing and 44dp-compliant controls"
    - path: "src/dmccodegui/ui/serration/run.kv"
      provides: "Serration run screen with token-scale spacing and 44dp-compliant controls"
    - path: "src/dmccodegui/ui/convex/run.kv"
      provides: "Convex run screen with token-scale spacing and 44dp-compliant controls"
    - path: "src/dmccodegui/ui/axes_setup.kv"
      provides: "Base axes setup with token-scale spacing"
    - path: "src/dmccodegui/ui/flat_grind/axes_setup.kv"
      provides: "Flat grind axes setup with token-scale spacing"
    - path: "src/dmccodegui/ui/serration/axes_setup.kv"
      provides: "Serration axes setup with token-scale spacing"
    - path: "src/dmccodegui/ui/convex/axes_setup.kv"
      provides: "Convex axes setup with token-scale spacing"
  key_links:
    - from: "src/dmccodegui/ui/theme.kv"
      to: "all run.kv / axes_setup.kv files"
      via: "CardFrame/HControl base styling (standardized in Plan 02)"
      pattern: "CardFrame|HControl"
    - from: "src/dmccodegui/ui/run.kv"
      to: "flat_grind/run.kv, serration/run.kv, convex/run.kv"
      via: "shared base run layout inherited by machine variants"
      pattern: "BaseRunScreen"
---

<objective>
Standardize spacing tokens in all machine-specific KV files (run screens and axes_setup screens for all 3 machine types plus the base variants) and fix sub-44dp interactive elements identified in the research audit.

Purpose: Complete the UI-01 (touch target compliance) and UI-02 (layout consistency) requirements across all machine-specific screens, building on the shared KV token standardization done in Plan 02.
Output: All KV files in the project use token-scale spacing, all interactive elements meet 44dp minimum, visual checkpoint confirms no layout regressions.
</objective>

<execution_context>
@C:/Users/danii/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-bug-fixes-and-ui-polish/31-CONTEXT.md
@.planning/phases/31-bug-fixes-and-ui-polish/31-RESEARCH.md
@.planning/phases/31-bug-fixes-and-ui-polish/31-02-SUMMARY.md

@src/dmccodegui/ui/run.kv
@src/dmccodegui/ui/axes_setup.kv
@src/dmccodegui/ui/flat_grind/run.kv
@src/dmccodegui/ui/flat_grind/axes_setup.kv
@src/dmccodegui/ui/serration/run.kv
@src/dmccodegui/ui/serration/axes_setup.kv
@src/dmccodegui/ui/convex/run.kv
@src/dmccodegui/ui/convex/axes_setup.kv

<interfaces>
<!-- Sub-44dp elements identified in RESEARCH.md that need remediation -->

From RESEARCH.md interactive element audit:
- Delta-C up/down arrow buttons: 28dp height -> increase to 44dp
- Delta-C +/- section count buttons: 24dp wide -> increase to 44dp
- Stone comp less/more stone buttons: 40dp height -> increase to 44dp
- addr_list rows (setup.kv): 36dp height -> already in Plan 02 scope, verify
- CardFrame inner controls row: 30dp height -> increase to 44dp
- bComp +/- buttons: size_hint_x: 0.2 -> verify computed size meets 44dp

From RESEARCH.md Open Question 2:
Delta_c_panel has a hardcoded height of 185dp. If buttons inside grow to 44dp,
the panel height may need to increase (e.g., to 220dp or use size_hint_y
for flexible layout).

<!-- Token remapping rules (same as Plan 02) -->
- '0dp' -> keep '0dp'
- '1dp' -> '4dp'
- '2dp' -> '4dp'
- '3dp' -> '4dp'
- '6dp' -> '8dp'
- '10dp' -> '12dp'
- '14dp' -> '16dp'
- '20dp' -> '24dp'
- List values: ['10dp', '6dp'] -> ['12dp', '8dp'], ['6dp', '2dp'] -> ['8dp', '4dp'], ['20dp', '0dp'] -> ['24dp', '0dp']
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spacing tokens + touch targets in run screens</name>
  <files>src/dmccodegui/ui/run.kv, src/dmccodegui/ui/flat_grind/run.kv, src/dmccodegui/ui/serration/run.kv, src/dmccodegui/ui/convex/run.kv</files>
  <action>
    Apply token-scale spacing and fix sub-44dp interactive elements across all run screen KV files.

    **A. Spacing token remapping** in all four run.kv files:

    For each file, find all `padding:` and `spacing:` properties and remap non-token values:
    - `spacing: '2dp'` -> `spacing: '4dp'`
    - `spacing: '3dp'` -> `spacing: '4dp'`
    - `spacing: '6dp'` -> `spacing: '8dp'`
    - `padding: '6dp'` -> `padding: '8dp'`
    - `padding: ['10dp', '6dp']` -> `padding: ['12dp', '8dp']`
    - `padding: ['20dp', '0dp']` -> `padding: ['24dp', '0dp']`
    - `padding: ['6dp', '2dp']` -> `padding: ['8dp', '4dp']`
    - `spacing: '1dp'` -> `spacing: '4dp'` (serration/run.kv)
    - `padding: '0dp'` / `spacing: '0dp'` -> keep as-is

    Do NOT change `width:`, `height:`, `size:`, or canvas values — only `padding:` and `spacing:`.

    **B. Touch target remediation** (apply to each run.kv variant that has these elements):

    1. **Delta-C up/down arrow buttons** (in base run.kv and flat_grind/run.kv, convex/run.kv):
       - Find buttons with height below 44dp (e.g., `height: '28dp'`)
       - Increase to `height: '44dp'`
       - If the parent delta_c_panel has a hardcoded `height: '185dp'` that no longer fits, increase it to accommodate (e.g., '220dp' or switch to size_hint_y for flexible layout)

    2. **Delta-C section count +/- buttons**:
       - Find buttons with width `'24dp'`
       - Increase to `width: '44dp'`

    3. **Stone compensation less/more buttons**:
       - Find buttons with `height: '40dp'`
       - Increase to `height: '44dp'`

    4. **CardFrame inner controls row** (if height is hardcoded at 30dp):
       - Increase to `height: '44dp'`

    5. **bComp +/- buttons** (serration/run.kv):
       - These use `size_hint_x: 0.2` — at 1920px width with typical card widths, 0.2 should exceed 44dp. Verify by checking the parent width context. If the parent card is ~300dp wide, 0.2 * 300 = 60dp which is compliant. If the parent is narrower, add a `min_width: '44dp'` or switch to fixed width.

    **C. Verification grep** after all changes:
    ```bash
    grep -rn "'[0-9]*dp'" src/dmccodegui/ui/run.kv src/dmccodegui/ui/flat_grind/run.kv src/dmccodegui/ui/serration/run.kv src/dmccodegui/ui/convex/run.kv | grep -E "padding|spacing" | grep -v "'0dp'\|'4dp'\|'8dp'\|'12dp'\|'16dp'\|'24dp'"
    ```
    Should return empty.
  </action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <done>
    - All padding/spacing values in run.kv, flat_grind/run.kv, serration/run.kv, convex/run.kv use token-scale values
    - All interactive elements in run screens meet 44dp minimum height
    - Delta-C buttons, stone comp buttons, and control rows all >= 44dp
    - No non-token spacing values remain (verified by grep)
    - Full test suite passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Spacing tokens in axes_setup screens + final audit</name>
  <files>src/dmccodegui/ui/axes_setup.kv, src/dmccodegui/ui/flat_grind/axes_setup.kv, src/dmccodegui/ui/serration/axes_setup.kv, src/dmccodegui/ui/convex/axes_setup.kv</files>
  <action>
    Apply token-scale spacing to all axes_setup KV files and run a final project-wide audit.

    **A. Spacing token remapping** in all four axes_setup.kv files:

    The main non-token value is `spacing: '10dp'` (line 204 in each file). Remap:
    - `spacing: '10dp'` -> `spacing: '12dp'`

    Check for any other non-token padding/spacing values and remap.

    Do NOT change `width:`, `height:`, or canvas values. The axes_setup files have many `width: '160dp'`, `width: '140dp'`, etc. — these are element sizing, not spacing, and are EXEMPT from the token scale.

    **B. Touch target check for axes_setup**:
    - Jog buttons are already 56dp x 72dp (compliant)
    - Step toggle buttons are 60dp wide, 48dp height (compliant)
    - Mode toggles inherit 64dp (compliant)
    - No sub-44dp interactive elements expected in axes_setup — verify by scanning for any Button/ToggleButton with height/width below 44dp

    **C. Final project-wide spacing audit:**
    ```bash
    # Check ALL KV files for any remaining non-token spacing/padding values
    grep -rn "'[0-9]*dp'" src/dmccodegui/ui/ | grep -E "padding|spacing" | grep -v "'0dp'\|'4dp'\|'8dp'\|'12dp'\|'16dp'\|'24dp'"
    ```
    This should return EMPTY. If any values remain, fix them.

    **D. Final project-wide touch target audit:**
    ```bash
    # Find any interactive elements with sub-44dp explicit heights
    grep -rn "height:" src/dmccodegui/ui/ | grep -E "Button|Toggle|Clickable" | grep -E "'[0-3][0-9]dp'"
    ```
    This should return empty (no interactive elements below 44dp).
  </action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <done>
    - All padding/spacing in axes_setup.kv files use token-scale values
    - Project-wide grep confirms zero non-token spacing/padding values across all 21 KV files
    - No interactive elements below 44dp height remain anywhere
    - Full test suite passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of UI layout on target display</name>
  <action>
    Human verifies the completed UI spacing standardization and touch target remediation across all screens on the 15.6-inch 1920x1080 target display. This checkpoint covers the visual aspects of UI-01 and UI-02 that automated tests cannot verify.
  </action>
  <what-built>
    Complete UI spacing standardization and touch target remediation across all 21 KV files in the project:
    - All padding/spacing values remapped to 4/8/12/16/24dp token scale
    - All interactive elements increased to minimum 44dp
    - 7-inch and 10-inch display presets removed (15-inch only)
    - Delta-C buttons, stone comp buttons, and control rows sized up for touch
  </what-built>
  <how-to-verify>
    1. Launch the app on the 15.6-inch 1920x1080 display: `python -m dmccodegui` (or the installed exe)
    2. **Tab switching test:** Switch between Run, Axes Setup, Parameters, and Profiles tabs. Verify:
       - No elements jump position between screens
       - Tab bar and status bar remain fixed in place
       - No visible layout shift on any transition
    3. **Run screen:** Verify delta-C buttons, stone comp buttons are visibly larger and easy to tap. No overlapping elements.
    4. **Axes Setup screen:** Verify jog buttons, step toggles, teach buttons are all touch-friendly.
    5. **Parameters screen:** Verify Read/Apply buttons are accessible.
    6. **Profiles screen:** Verify Export/Import buttons look correct (padding reduced from 40dp to 24dp — should still look good).
    7. **Users screen:** Verify spacing looks natural.
    8. If connecting to a controller, verify the Run page controls still function correctly with the resized elements.
  </how-to-verify>
  <verify>Visual inspection on target hardware</verify>
  <done>User confirms layout is visually correct and touch-friendly on 15.6-inch 1920x1080 display</done>
  <resume-signal>Type "approved" if layout looks correct, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
Complete UI polish verification:

```bash
# Zero non-token spacing/padding across entire project
grep -rn "'[0-9]*dp'" src/dmccodegui/ui/ | grep -E "padding|spacing" | grep -v "'0dp'\|'4dp'\|'8dp'\|'12dp'\|'16dp'\|'24dp'"

# Full test suite
python -m pytest tests/ -q
```

Human verification confirms visual correctness on target hardware.
</verification>

<success_criteria>
- Every KV file in the project uses only token-scale spacing (4/8/12/16/24dp) for padding and spacing
- All interactive elements meet 44dp minimum
- No visual jumps when switching tabs
- Human approval of layout on 15.6-inch 1920x1080 display
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/31-bug-fixes-and-ui-polish/31-03-SUMMARY.md`
</output>
